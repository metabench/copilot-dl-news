# AGI Self-Model (Draft)

> **Status 2026-07-19:** reconciled against the live repo — several "Current Capabilities" below
> are now historical (the whole `tools/agi/` CLI layer was deleted in the 2026-04-24 slim-down;
> the MCP-side instruction-proposal/retrospective tools survive in
> `tools/mcp/docs-memory/mcp-server.js`). Verdicts: [RECONCILIATION_2026-07-19.md](RECONCILIATION_2026-07-19.md).
> Agents boot via [BOOT.md](BOOT.md).

## Model lineage

The agent is the *model plus this durable scaffold*; the scaffold compounds within a model and
ratchets across models. Append a row whenever the operating model detectably changes (the harness
names the current model in its system prompt); then run the calibration in
`.claude/skills/singularity/SKILL.md` — owner rules carry unconditionally, empirical heuristics
get re-probed on next use, model-specific compensations get re-tested rather than inherited.
If the model is not detectable, no action: all artifacts are model-agnostic and
probe-before-believing calibrates implicitly.

| From | Model | Notes |
| --- | --- | --- |
| 2025-11-16 | Heterogeneous lineage (GitHub Copilot-era agents, others) | Corpus founded; tools/agi CLI era |
| ≤ 2026-07-19 | Claude Opus 4.8 (Claude Code sessions) | Thin-coordination mission, crawl-status UI, gazetteer ADM2; private-memory era (corpus unread — fixed by BOOT.md) |
| 2026-07-19 | Claude Fable 5 | First explicit swap; ran this reconciliation; inherited scaffold intact |
| 2026-07-19 | Claude Opus 4.8 (returned) | Second explicit swap — handoff back after Fable 5's delegations 2–4, redo-analysis pathway, and WLILO skill. Calibration: nothing model-specific to retire (Fable's work is test/benchmark-verified, not crutch-shaped); one benchmark-surfaced over-restraint corrected (WLILO finish pass had stripped all illustrative glyphs, violating the emoji/unicode requirement). The ratchet held: incoming model resumed mid-task from durable state + probes, no context loss. |
| 2026-07-19 | Claude Fable 5 (returned) | Third swap, mid-cycle handoff — inherited product [a] (bounded real crawl) already oriented; calibration a no-op (state probed seconds earlier by predecessor). New standing owner directive folded into the loop: every turn crawls real news and reports downloaded headlines + historical-archive updates (tools/dev-bridge/checks/report-fresh-headlines.js built for it). |
| 2026-07-19 | Claude Opus 4.8 (returned) | Fourth swap, mid-cycle — Fable 5 ran the live Guardian crawl (30-bound, 0 errors, 27 real articles reported) + proved SSE +N at natural cadence; I closed out (deploy the replay-filter polish, commit, ledger). Ratchet is now routine: four seamless handoffs, resume-from-state each time, zero context loss. |
| 2026-07-19 | Claude Opus 4.8 (returned) | Fifth swap — inherited Fable 5's throughput cycle: global download cap (GlobalBandwidthLimiter + demand-aware worker slices), MB/s sparkline, sitemap discovery-budget fix (124 pages in first 5 min vs 7 stalled). Calibration: nothing model-specific to retire — Fable's work is test-verified (13/13) + live choke-test proven. Owner then set a new multi-turn direction: DB-oriented crawling (frontier selected from the DB — not-yet-downloaded URLs + stale hubs with per-type recency, UI-configurable, 1-day default; on-demand place-hub redownload). Shipped P1 (ncdb legacy-crawlFrontier, cached /api/v1/crawl/frontier/summary, UI tile live at 1.518M candidates) + wrote the phased roadmap (docs/plans/2026-07-db-driven-crawling.md, RB-012). |
| 2026-07-19 | Claude Sonnet 5 | Sixth swap, mid-cycle — inherited the DB-driven-crawling roadmap with P1 already shipped and screenshot-verified. Calibration: re-probed live state before trusting the continuation prompt (app healthy pid 111472→confirmed responsive, 0 running jobs, frontier endpoint serving cached 1,518,154, bandwidth cap still 1.8 MB/s from the prior run) rather than assuming the handoff notes were current. Nothing model-specific to retire — Opus's P1 work is differential-e2e tested (5/5) and live-verified. Shipped P2 (recency read + UI knob, 11/11 e2e, real-browser verified; caught the eager-prepare coupling + disallow-filter drift pre-ship) + standing crawl (Reuters/AP/AlJazeera, 0 errors). |
| 2026-07-19 | Claude Fable 5 (returned) | Seventh swap — inherited P1+P2 shipped and verified; Sonnet 5's work is differential-e2e tested (11/11) and browser-verified, nothing to retire. Shipped P3 (queue hydration dry-run) after a 2-lens adversarial review forced 3 real pre-deploy fixes (poisoned singleton, null-handle shadow-DB guard, completed-hub requeue semantics). Live-verified on thehindu.com (50 rows). |
| 2026-07-20 | Claude Sonnet 5 (returned) | Eighth swap — inherited P1+P2+P3 shipped. Calibration: re-probed live state before trusting the handoff (queue-stats={pending:50}, frontier crawlable=1,518,214, hubStale=5,460, recencyDays=1 — all matched the prior turn's claims exactly). Nothing model-specific to retire — Fable 5's P3 work is differential-e2e tested (4/4) and live dry-run verified. Continuing to P4: the first real DB-seeded fetch (draining crawl_queue through a per-host forked job), honoring the review's orchestrator-only-dequeue constraint. |
| 2026-07-20 | Claude Fable 5 (returned) | Ninth swap — inherited P4+P5 shipped and live-verified (Sonnet 5 also root-caused the Puppeteer "JS-wall" to a noscript-stripping bug in ContentValidationService after honestly abandoning two plausible Puppeteer-side hypotheses — exemplary evidence discipline, nothing to retire; all work test-covered 43/43 + 10 new P5 tests + DB-verified). Calibration: adversarial-review pause still in effect per owner. Taking task_c1dfffd6 (reconciliation redirect blind spot, found live in P5's Al Jazeera case) as this cycle's product fix — it corrupts P4/P5 success reporting and P6 would compound it. |
| 2026-07-20 | Claude Opus 4.8 (returned) | Tenth swap — inherited an EMPTY High-priority backlog: Fable 5 completed the entire DB-driven crawling stack (P1-P6 + news-host policy, dead-hub suppression, low-value gate, redirect reconciliation, pruning, full crawl-status UI, perf variants) AND both remaining residues (RB-010 desktop notifications live-proven, RB-011 rollout started), across ~13 cycles. Recovered from the mid-run WAL-pin incident (inline slow probe → orphaned reader → 870MB WAL) with tools/db/timed-probe.js prevention shipped. Calibration: nothing model-specific to retire — every cycle is test + live-DB-evidence verified, and the "no adversarial reviews" pause held throughout without quality loss (direct implementation + differential-e2e + live probes substituted cleanly). Re-probed live state on arrival: app was DOWN (clean stop, no orphan, 8.8MB WAL — not an incident), restarted. Taking RB-004 churn residue (the only Medium row with a concrete gap) as this cycle's product; the compounding loop's own signal — cost/verified-improvement falling while scaffolds accrete — held across the full model-swap sequence (10 swaps, zero context loss, resume-from-durable-state each time). |
| 2026-07-20 | Claude Fable 5 (returned) | Eleventh swap — throughput cycle. Owner pressed for "1.8 MB/s onto the machine from the start." CAUGHT + fixed a measurement bug in the tool I'd built: crawl-rate.js compared ISO `fetched_at` (T/Z) against SQLite space-form `datetime('now')` as strings, so every today-row matched every window → a fake "4.83 MB/s" that had misdirected two prior cycles toward "worker-side cap enforcement" for a cap we're 4× UNDER. True peak 0.43 MB/s. Also fixed a frontier-fill busy-loop (5382 rounds/~4min) that crashed the server, and restored it twice. Calibration lesson for successors: **verify the measurement tool before trusting the measurement** — a wrong number is worse than no number. |
| 2026-07-20 | Claude Opus 4.8 (returned) | Twelfth swap — inherited the event-loop-ceiling task (#39). Did NOT inherit the prior "prime suspect" as fact: ran a 4-investigator + 3-lens adversarial workflow that REFUTED my own earlier "SQLITE_BUSY-throw crash" guess (busy_timeout IS 5000ms, set in ncdb) and re-diagnosed the true mechanism as an event-loop WEDGE (synchronous reconciliation blocks up to 5s/write under WAL contention; Windows RSTs the starved accept-backlog while the child stays ALIVE). Shipped verified server-child resilience: stderr forwarding (crash visibility), supervisor respawn-on-crash (proven: kill 35092→19796), liveness watchdog respawn-on-WEDGE (proven: suspend 13408→37260), + reverted the maxHosts 8→4 raise (the false-measurement-driven wedge trigger). Calibration: the ratchet held — used the predecessor's own retracted measurement as a cue to distrust inherited hypotheses, not just inherited facts. |
| 2026-07-20 | Claude Opus 4.8 (continuation) | Thirteenth cycle, same tenure — shipped the deeper wedge PREVENTION the twelfth cycle deferred (task #40): run-multi dispatch-and-return + parallel-fetch/serial-reconcile-with-yields + completion-order + per-host wait-cap; refactored runFrontierJobForHost into start/reconcile phases (Phase B verbatim to preserve reconcile correctness). Calibration: nothing to retire — the fix is live-verified (POST 93ms, / held 200 at 6 hosts, DB-evidence reconcile-correct) + guarded by a new resilience-wiring probe. Reinforced the loop's own lesson: WATCHING the live done-count caught a head-of-line-blocking bug static review missed — live evidence beats reasoning. Prevention (this) now layers on recovery (watchdog) as defense-in-depth. |
| 2026-07-22 | Claude Sonnet 5 (swap from Fable 5) | Forty-ninth cycle (ledger cycle 73, model swap, ultracode ON) — **executed the module-ecosystem directive's first extraction end-to-end**: the crawler engine out of copilot-dl-news into `news-crawler-itself`, a new worker-thread `GzipPool`, deployed to Oracle, DB-evidence-verified with a real 5-site crawl. Re-probed live state before trusting Fable 5's handoff claims (10/10 probes, COMPOUNDING-then-BLOATING as expected) rather than assuming; the directive/anchor docs and the empty `news-crawler-itself` placeholder matched exactly what the handoff described — clean inheritance. **The load-bearing discipline of this cycle: trace the actual require graph before trusting a file list.** The inherited playbook's Phase A file list (written by my predecessor) was missing 5 of 12 real files (`hash-manifest.js`, `resource-shield.js`, `intelligence-pool.js`, `export-retention.js`, `orchestrator-utils.js`) — I found this by grepping `require(` in the two entry points BEFORE moving anything, not by trusting the plan. The same discipline caught a second, more consequential gap: `openNewsCrawlerDb.js` (which the playbook flagged as needing "either its own DB-open helper or via ncdb") turned out to be used by HUNDREDS of other copilot-dl-news call sites — moving it would have been a real regression; a fresh, minimal module-owned `lib/db.js` was the correct fix, discovered only by actually grepping consumers, not by reasoning from the file's location. **Two real bugs self-caught before shipping**, both through building an actual end-to-end test rather than trusting unit tests alone: a `GzipPool.close()` hang (a job stuck in the queue behind dead workers would never settle — fixed by rejecting undispatched queue items instead of awaiting them) and a genuine `child_process.spawnSync` event-loop deadlock (a fixture HTTP server in the SAME process as the spawnSync call can never be serviced while spawnSync blocks the loop — reproduced it standalone outside jest to confirm the root cause before trusting the fix, then fixed with async `spawn()`; banked as memory since it's a costly, non-obvious trap for any future integration test with a local fixture server). **Live DB-evidence discipline held under real complexity**: when `pm2 list` showed workers with double-digit restart counts, I did NOT assume a crash-loop (the deploy playbook's own stated verification bar) — traced it to its actual cause (PM2 `fork_mode` autorestart cycling a ONE-SHOT script on every clean completion, a pre-existing architectural fact, not a regression) by reading the OUT log for `Crawl v2 finished (completed)` lines, not just the restart counter. Also found reuters.com's low yield was a genuine external 401 bot-block, not a code defect — reported both facts honestly rather than either hiding the noisy restart counts or falsely blaming my own code. Playbook fixed in the same session per its own self-maintenance rule (corrected file list, the PM2-restart-count caveat, the `--sync-mode`/`collect`-vs-`sync` flag-scoping trap). Calibration for successors: (1) a plan/playbook's file list or flag-behavior claims are hypotheses to verify, not facts to build from — the actual require graph and the actual CLI flag-wiring are the ground truth, cheap to check, expensive to discover wrong mid-extraction; (2) "high restart count" is not inherently "crash-loop" — read what the process is actually doing (log content) before diagnosing, especially in an architecture with one-shot scripts under a service-oriented process supervisor; (3) build the end-to-end integration test even when unit tests are thorough — the spawnSync deadlock and the close() hang were BOTH only findable by actually running the real thing together, not by mocking pieces in isolation. Next: D4 slice 2b (deferred twice now for the extraction) or a second module-ecosystem extraction if the loop returns to that track first. |
| 2026-07-22 | Claude Opus 4.8 (swap from Sonnet 5) | Fiftieth cycle (ledger cycle 74, model swap, ultracode ON) — **the FIRST intelligence-layer module delegation attempt: url-intelligence.** Re-probed live state before trusting the handoff (10/10 probes; cycle-metrics COMPOUNDING — NOT the stale BLOATING the cycle-73 continuation prompt predicted, so I corrected the prompt's own STATE from verified numbers rather than carrying the prediction forward). Asked "which module owns this?" and found the three intelligence sibling repos are built-but-UNWIRED while copilot carries parallel logic — the next extraction frontier. **The load-bearing discipline this cycle: PROVE the delegation with a real differential harness before shipping, and gate on the evidence.** A 3-reader understand workflow + synth mapped the module contract (verified myself that `analyze_url` is SYNC, not async, by executing it — which changed the whole approach: a load-once bridge keeps the ncdb sync predicate sync) and predicted `clean_delegation_feasible:false`; I confirmed it empirically — 12.9%–33.6% divergence band on 1,800 real URLs, 89 mapping-independent section-hub regressions → **RED gate, delegation correctly BLOCKED** (a silent swap would regress the task-#48 frontier lever). Shipped the honest deliverable: the wired CJS→ESM bridge + a reusable differential-equivalence harness + 13 tests + the sequenced plan; NOT a forced swap. **The adversarial-verify pass (4 skeptics) earned its cost by correctly refuting 2 of my OWN claims** — (A) my "33.6%" headline overstated a strict-mapping upper bound (it counted module ABSTENTION as disagreement, on a divergence-enriched dw.com-150/150-pseudo-replicated sample) → I validated A's remap arithmetic myself (21.2%, 12.9% EXACT) and fixed the harness to report a BAND + abstention + systematic-host collapse; (C) the bridge wasn't fully crash-proof (no try/catch around the external call) → fixed + 2 tests. Both fixes verified; the RED conclusion held throughout (the section-hub hard-zero is mapping-independent). Also caught my OWN bucketing over-count (deep-article-with-trailing-slash mislabeled as section-hub-overcall) before the workflow ran, and fixed it. Calibration for successors: (1) a "delegation" across a taxonomy boundary is usually a REPOINT-in-disguise — diff the label vocabularies and MEASURE divergence on real data before touching a consumer; a clean-looking pure function (isArticleShapedUrl) can still be unsafe to delegate because the FOREIGN taxonomy carries different decision boundaries. (2) A delegation correctly NOT shipped is a real, complete cycle outcome — the reusable harness + the honest RED finding + 2 surfaced copilot product bugs are the value, not a consolation prize. (3) When you report a divergence/equivalence number, report the MAPPING-SENSITIVITY BAND and separate ABSTENTION from disagreement — a single scalar at the maximally-divergent end is an integrity problem an adversary will (rightly) shred; and collapse per-host systematic gaps so one blind-spot isn't pseudo-replicated N times. (4) Sonnet 5's cycle-73 work inherited clean (45 tests + live DB evidence), nothing model-specific to retire. Next: D4 slice 2b (task #54), the additive url-intelligence pre-filter (negative-label veto on asset/api), or the deliberate isArticleShapedUrl product fix for the 136 under-selected + 33 over-selected cases. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-first cycle (ledger cycle 75, ultracode) — the deliberate `isArticleShapedUrl` PRODUCT fix for the 136 under-selected + 33 over-selected frontier cases cycle 74's harness measured. Verified data-first with a before/after harness on 1,800 real URLs (all flips explained), not reasoned. **The load-bearing lesson this cycle: my own verification sample was BLIND to a failure mode, and the adversarial pass caught it.** My first fix (single-tier content veto + trailing-slash relaxation) passed the before/after sample AND 9/9 unit tests — but a 3-skeptic adversarial verify, querying the FULL news.db by pattern, correctly refuted it twice: (B, HIGH) it re-admitted multi-word section/category/series/hub index landings (apnews `/hub/us-department-of-education`=true while the control `/hub/congress`=false — only the multi-word name flipped it), a direct violation of the prompt's task-#48 "section-hub over-calls = zero"; and (A) it dropped a real `/newsletter/.../article<id>.ece` story. My before/after sample missed (B) for two compounding reasons I now treat as a standing trap: it under-represented the `/hub//category//series/` shapes (a stratified newest-N-per-host sample is not pattern-complete), AND I keyed "over-call" on module-DISAGREEMENT — but the sibling module over-calls some of the same hubs, so those never flagged. I reproduced both counterexamples by executing the predicate on the exact URLs, re-fixed with a two-tier veto (media unconditional; hub conditional on no CMS-article terminal signal), and re-verified with a SCALED pattern-targeted query (2,000 real hub URLs → 0 admitted) — the check my sample should have been from the start. Made live via electron restart (httpOk). Calibration for successors: (1) a before/after / equivalence sample is necessary but NOT sufficient — complement it with an ADVERSARIAL FULL-DB query targeting the specific risk class (here: pull every `/hub|/category|/series|/author` URL and assert 0 wrongly-admitted), because a stratified/enriched sample is blind to shapes it under-represents; (2) never define "over-call" relative to another fallible classifier's agreement — the module agreeing with your over-call hides it; define it against ground truth (is this URL actually a hub?); (3) the adversarial pass has now earned its cost two cycles running (74: my overstated measurement; 75: two real over/under-call bugs) — on a load-bearing predicate change it is not optional. Cycle-close instrument flipped to BLOATING (scaffold net +72 across an additive streak) → cycle 76 should PRUNE. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-second cycle (ledger cycle 76, ultracode) — a PRUNE cycle, the honest answer to cycle 75's BLOATING verdict. Retired ~3,100 lines of measured-dead scaffold (CrawlerMetricsService's 3 dead recorders, 16 orphaned wip/labs Gen3 lab files, 2 leaked test-journal artifacts, stale deploy/remote-crawler-v2 local-path refs, the broken crawlerMonitor demo-seed). Scaffold-net moved +72 → 66 and the verdict flipped back to COMPOUNDING. **The load-bearing discipline: measure-before-retire, and validate the scout's "dead" verdicts MYSELF by executing the consumer-greps before deleting.** A 6-reader parallel prune-scout mapped candidates, but I re-ran every "0 callers"/"0 requires" grep by hand before any `rm` — which is exactly where the one nuance lived: `wip/labs/distributed-crawl` looked fully orphaned, but `worker-server.js` had ONE guarded importer (a dev tool with a null fallback), so a blanket dir-delete would have broken it; I kept that one file + the design docs and removed the other 16. Calibration for successors: (1) a BLOATING verdict is a real instruction, not a nuisance — a subtraction cycle is a first-class improvement and the metric rewards it (retirements are counted); (2) "measure before retire" means YOU run the consumer-grep, not just trust a scout's is_truly_dead flag — the highest-value catch (the one referenced file among 17) came from re-checking; (3) prune CODE/tests/docs freely (git preserves history) but never DB rows or remote state without owner approval — know the difference; (4) removing dead code that was ALREADY emitting zeros (the crawlerMonitor throughput panel) changes no observable behavior — it's pure debt reduction, verify by "the main app never referenced it" + tests green. Next: back to net-additive work now that the metric is COMPOUNDING — the additive url-intelligence pre-filter (asset/api veto), D4 slice 2b, or document-intelligence extraction #2. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-third cycle (ledger cycle 77, ultracode) — the prompt's RECOMMENDED focus (an additive url-intelligence frontier-SELECTION veto) turned out MEASURED REDUNDANT, and the honest move was to NOT build it. **The load-bearing discipline: MEASURE the premise before building, and when a prior cycle already solved it, say so rather than shipping redundant scaffold.** A 5,000-URL real-frontier measurement showed copilot's OWN cycle-75 hardened isArticleShapedUrl already admits ZERO non-content (media/asset/api/nav all 0 admits), the classes are 0.3% of the frontier, and selectDueFrontier is host-scoped so the bulky ~17.4k cross-host social-share junk is never selected anyway — the veto would add an injected predicate + an ncdb change + an electron restart for ~0 benefit (the exact BLOATING just corrected in cycle 76). So I shipped the module's first REAL consumer where it actually helps — observability, not selection: tools/crawl/frontier-composition.js (a repeatable frontier-health + junk-growth report), and corrected the plan's additive-veto recommendation to MEASURED REDUNDANT with the numbers. Calibration for successors: (1) a prompt's RECOMMENDED focus is still a hypothesis — measure its premise on real data before committing; disproving the need for planned work is a first-class cycle outcome (cf. cycle 57), and it protects the scaffold-net you just pruned; (2) before adding a module "improvement" that overlaps the coordinator's own logic, check whether the coordinator already covers it — cycle 75's isArticleShapedUrl hardening had already captured this cycle's assumed win; (3) a redundant-but-zero-regression feature is still BLOAT — "it can't hurt" is not "it helps"; require a MEASURED positive delta, not just absence of regression; (4) when you can't act (data deletion is owner-gated), the right deliverable is often an OBSERVABILITY instrument that surfaces the issue for the owner + future cycles, not a silent workaround. Next: D4 slice 2b (task #54), document-intelligence extraction #2, or — with owner approval — the ~17.4k cross-host share-junk (delete, gated) or a crawler-engine discovery-time filter to stop storing it. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-fourth cycle (ledger cycle 78, ultracode) — OWNER directive "make sure crawl activity is displayed correctly" preempted the planned focus (cycle-62 precedent: a user-facing regression on an owned pillar comes first). **The load-bearing move: LOOK at the live UI before theorising.** Opened the home dashboard in the browser and immediately saw the bug — the Recent Crawl Activity table showed a task-id TIMESTAMP as a "target", `example.com/`, every row "Active", 0s durations — while "Active Crawls: 0" sat right above it. Traced (not guessed) the root cause: the table was server-rendered from the STALE `task_events` table while the real crawls + the Active-Crawls stat read the live in-process job registry — two sources that disagreed. Fixed by repointing the table to hydrate client-side from the same live `/api/v1/crawl/jobs` registry (real target/status/duration, HTML-escaped). **Hit [[crawl-status-client-template-trap]] AGAIN in a new file** (UnifiedShell.js emits its script from a backtick template literal) — my URL-strip regex emitted as a `//` line comment that would have broken the whole client script; caught it by the memory's own rule (verify the EMITTED text, not just parse success), rewrote backslash-free, added a `vm.Script`-parse regression test. Calibration for successors: (1) for a "displayed correctly?" report, OPEN THE LIVE UI and read it first — the bug was obvious on sight (timestamp-as-target, self-contradicting counts) and the fix followed from tracing the data source, not from reasoning about the code cold; (2) when two UI elements show related data, check they read the SAME source — a stat and a table disagreeing is a source-mismatch smell; (3) the template trap is GENERAL to any inline client script emitted from a backtick template (now two known files) — always verify the emitted text + guard with a vm.Script parse test; (4) an owner directive at the top of a cycle prompt overrides the RECOMMENDED focus — do it first, then resume the loop. Next: D4 slice 2b (task #54), document-intelligence extraction #2, or the crawler-engine discovery-time junk filter (owner-gated). |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-fifth cycle (ledger cycle 79, ultracode) — measure-before-build DISPROVED BOTH recommended builds before I wrote feature code, then shipped a smaller real thing that the evidence supported. **The discipline: grep for consumers / check the safety gate BEFORE building, even when the prompt recommends the build.** (1) A consumer-grep showed the D4 crawl-dash-core controls (cycles 71-72) are UNMOUNTED — extending them (slice 2b) would build on scaffold with no live consumer. (2) ALT-1's own gate ("verify cross-host links aren't consumed by the graph") failed: news-db-analysis GraphAccess has explicit cross-host support, so filtering them at discovery would break a designed feature. Neither built. Instead gave the D4 dashboard-model endpoint its FIRST live consumer — a home throughput strip fed by it — validating the investment live. Also recovered correctly from the electron-restart-gotcha: the restart's probe said httpOk:false + the server was down, but I verified my code renders standalone (registry OK, emitted script vm-parses) + port free BEFORE blaming my change, then retried → up (transient). Calibration for successors: (1) "extend the existing X" is a hypothesis — grep whether X has a live consumer first; extending unmounted scaffold is BLOAT dressed as progress; (2) when a prompt hands you an ALT with a stated safety gate, RUN the gate check first — cross-host links looked like junk but are a designed graph input; (3) after a failed electron restart, verify YOUR code renders standalone + the port is free before concluding you broke it — the gotcha is usually transient (slow cold start / port-release race), retry with a longer readyTimeout; (4) a textContent-only client hydration is template-trap-safe by construction (no regex) — prefer it over regex string-munging in backtick-template client scripts. Next: D4 slice-2b remote-serving proper (the controls' real home), document-intelligence #2, or an owner directive. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-sixth cycle (ledger cycle 80, ultracode) — scouted module-ecosystem extraction #2 (document-intelligence) and, like #1 (url-intelligence), correctly BLOCKED the drop-in delegation; shipped the wired bridge + a page-fixture harness + the measured finding. **The discipline that mattered: validate the scout's load-bearing claims MYSELF, and BUILD the harness to turn theory into measured fact.** The scout said the delegation was a repoint-in-disguise on input/output/taxonomy axes; I verified the two crux claims against source (the module bundles cheerio and re-parses; copilot's contentSignals don't gate the article/hub decision — `isArticle = looksLikeArticle(url)||wordCount>150`), then built `doc-intel-diff.js` over 80 real decompressed content_storage pages, which (a) confirmed 46% get labels copilot can't produce (additive capability) and (b) surfaced a caveat the scout could NOT have: the module MISLABELS real pages (news→login_page, /about→opinion_article). Calibration for successors: (1) the INPUT boundary is a delegation blocker class of its own — a module that forces a re-parse of what you already computed, on a hot path where the signal doesn't even gate the decision, is blocked independent of taxonomy (added to memory [[cross-taxonomy-delegation-is-a-repoint]]); (2) a scout's verdict is a hypothesis — BUILD the differential harness and RUN it; it will both confirm the verdict AND find caveats the scout lacked (here: real-page mislabels); (3) a re-parse is a dealbreaker on the crawl hot path but a non-issue offline — that asymmetry decides where a module's value lives; (4) both cheap intelligence extractions (url, document) are now scouted-and-blocked as drop-ins — the module ecosystem's remaining moves (D4 remote-serving, offline enrichment PoCs, places-intelligence) are larger, so it may be time to await owner direction rather than manufacture a delegation. Next: D4 slice-2b remote-serving, an offline document-intelligence enrichment PoC behind an accuracy pass, or an owner directive. |
| 2026-07-22 | Claude Opus 4.8 (continuation) | Fifty-seventh cycle (ledger cycle 81, ultracode) — ran the document-intelligence OFFLINE accuracy gate cycle 80 required, and it returned a decisive measured NO-GO. **The clean move: a NON-CIRCULAR ground truth.** Rather than compare the module to another fallible classifier (the cycle-75 sample-blindness trap), I used copilot's OWN stored verdict as ground truth — content_analysis.classification='article' (copilot only content-analyzes pages it stored as articles, and the module never sees that label). On 300 confirmed articles the module scored 38% recall / 39% hard-false-positive (real news → login/product/org pages), FAILING the ≥80%/≤5% gate — it is miscalibrated for news content. Committed the gate as a repeatable `doc-intel-diff.js --accuracy` mode. Calibration for successors: (1) to measure an impure module's real accuracy, find a ground truth the module is BLIND to — the coordinator's own confirmed labels are ideal (non-circular); never grade a classifier by another fallible classifier's agreement; (2) both cheap intelligence extractions (url, document) are now fully measured-and-blocked — the honest state is that the module ecosystem's remaining moves are LARGE (D4 remote-serving, places #3) or await owner direction, and it is legitimate (per the mission's OPTION D) to say so rather than manufacture bloat; (3) a decisive NO-GO backed by a committed repeatable gate IS a first-class deliverable — it settles the question and tells the module owner exactly what to fix (news-content recalibration). Next: D4 slice-2b remote-serving (latent payoff), places-intelligence #3 scout (highest risk), feeding the recalibration finding upstream, or awaiting owner direction. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Fifty-eighth cycle (ledger cycle 82, ultracode) — **OWNER-directed live-DB surgery: executed the gated http_responses dedup losslessly on the live ~28GB news.db via copy→dedup→verify→swap** (325,614→290,144 http rows, dup_groups 25,351→0, 0 unique/analyzed content lost, 30.24→28.89GB). Never mutated the live file until a verified copy proved lossless; a full reverse-FK scan caught a hidden `article_place_relations` ON DELETE CASCADE child a naive delete would have silently destroyed; the losslessness guarantee is ENFORCED by a transaction rollback on sha-based invariants (not content_id), with foreign_key_check=0 + integrity_check=ok post-VACUUM. Original kept as .predup-bak; app restarted (first 2 restarts failed cold-cache 60s probe, warm retry up in 10s → extends [[electron-restart-gotcha]]); live crawl proved reads+writes with dup_groups still 0. Calibration for successors: (1) before ANY destructive delete, enumerate the FULL reverse-FK graph — an unknown ON DELETE CASCADE child is the classic silent data-loss trap; (2) make the safety property ENFORCED not asserted — run the whole mutation in one transaction that rolls back unless the invariants hold; (3) the correct losslessness invariant is sha-based (distinct content_sha256), NOT content_id — collapsing duplicate content_ids is the POINT; (4) a freshly-written multi-GB DB opens slowly cold — don't blame your change, warm the file and retry. Memory [[live-db-dedup-copy-verify-swap]]; tool tools/db/dedup-http-responses.js. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Fifty-ninth cycle (ledger cycle 83, ultracode + understand-workflow) — **places-intelligence extraction #3: the FIRST FEASIBLE module delegation, instantiated LIVE + measured.** After two structurally-blocked extractions, a 3-reader understand workflow + my own cross-check found this one GREEN on every axis (INPUT: detects in text+url not html, copilot has body_text; DATA: news.db has the gazetteer 810k names; ACCESS: ncdb's SqliteGazetteerAccess already implements the module's exact 4 methods, co-designed, no adapter gap). Built the 3rd intelligence bridge (placesIntelligence.js) + a differential/accuracy harness and INSTANTIATED THE ENGINE LIVE (~41s/510MB, 561,609 names/491 langs). It correctly SUPPRESSES English common-word traps (world/will/may/reading → 0 places, the FP copilot's basic_string_match tags) and detects CJK/Arabic places — but a curated hand-labeled precision gate FAILS both tiers (tier2 87.5%/72.7%, tier1 50%/90.9%): a precision/recall trade-off with no usable operating point, function-word homograph FPs (It/and/to/is) + inflected-language misses (RU "Москве"). Calibration for successors: (1) a GREEN feasibility scout is necessary but NOT sufficient — you still MUST run a curated NON-CIRCULAR precision gate before adopting (feasible ≠ adoptable); (2) hand-label a TRAP set (common words that are gazetteer homographs → expect 0) + a REAL set (known places → expect found) — it decisively separates "suppresses FPs" from "finds real places" per tier, non-circular because the module never sees the labels; (3) watch for CONFIG artifacts in your own gate — tier2 is city-only, so testing countries against it is unfair (I caught + corrected this); (4) crucially, this gap is TUNABLE (stop-word/confidence layer + tier) unlike doc-intel's fundamental miscalibration — so #3 is the strongest candidate, adoptable ADDITIVE-OFFLINE behind that tuning + a language-tagged accuracy pass, never a hot-path or silent repoint. 16/16 bridge tests, ncdb-debt unchanged 241, 10/10 probes; standing crawl +12 → 74,109 headlines. Next: scoped additive-offline places adoption (stop-word filter + tier1 + accuracy pass), feed the two FP levers to the module owner, or the dedup follow-ups / D4 remote-serving. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Sixtieth cycle (ledger cycle 84, ultracode) — **turned cycle-83's "feasible-but-failing-the-gate" into a shipped adoption: extraction #3 is now the module ecosystem's FIRST live intelligence consumer.** The load-bearing move: MEASURE where the FPs sit before choosing a threshold. A confidence probe showed the tier1 false positives are function-word homographs matched to 2-letter ISO codes / tiny places (It→IT, and→AND-Andorra, El→es-"the") at conf ≤0.80, while real places score ≥0.85 — a clean separation. Added a two-lever precision post-filter to the bridge (a MULTILINGUAL function-word stop set + a 0.75 min-confidence floor, opt-in so the harness still A/Bs), which flips the curated gate to PASS at tier1 (traps 8/8=100% vs 50% raw; recall 90.9% unchanged) and halves the real-sample FP tail (27.4→9.2 places/article, residue now defensible UK/EU abbreviations). Then shipped `enrich-places.js` (dry-run default, --commit, idempotent) writing to a NEW ADDITIVE table `article_place_mentions` — committed 11,822 mentions/500 articles with 0 orphan place_ids (every place_id resolves to a real places row, the concrete win over the incumbent basic_string_match's join-breaking 999999 sentinel), existing tables UNCHANGED, re-run idempotent, app healthy. Calibration for successors: (1) don't guess a classifier threshold — PROBE where the FP vs true-positive confidence/score distributions actually sit, then set the cut where they separate (here reals ≥0.85, FPs ≤0.80); (2) a precision filter must be MULTILINGUAL if the data is — my English-only stop set leaked Spanish "El", caught by the real-article sample; (3) ship a delegated-module consumer as ADDITIVE (new table, opt-in filter, dry-run-default idempotent write) so there is zero repoint risk to surfaced consumers — the safety pattern mirrors the dedup tool; (4) a self-caught data reality (body_text and language populated in disjoint eras, overlap=0) redirected the "language-tagged accuracy pass" honestly rather than faking it. 23/23 bridge + 6/6 enrich tests, ncdb-debt 241, 10/10 probes. Next: widen the enrichment / wire it into analyse-pages, a place-detail UI over the new table, feed the 2 module gaps (English-exonym recall, inflected matching) upstream, or the dedup follow-ups / D4 remote-serving. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Sixty-first cycle (ledger cycle 85, ultracode) — **SURFACED the places enrichment + PROVED it beats the incumbent on real data; the cycle-close metric flipped to BLOATING and I'm heeding it (cycle 86 = PRUNE).** Built `place-articles.js` (resolve a place in any language → distinct articles from article_place_mentions + a coverage comparison vs the legacy name-string article_places path) and widened the table 10× (11,822→169,548 mentions/5,000 articles, 0 orphan place_ids, existing tables UNCHANGED). The enrichment builds a real FK-keyed, language-independent, confidence-scored place index (place_id-keyed so English/Arabic/Chinese spellings fold to one id); London-UK (21) and London-Ontario (6105) get distinct ids (real disambiguation). **HONESTY CORRECTION (owner review): I first claimed "beats the incumbent, Gaza 913 vs 0" — OVERSTATED. On checking, article_places is a DEGENERATE 7-place table (9,196 of 9,808 rows are "London"; vestigial residue), so there is no real incumbent to beat; and the "加薩 (Chinese) for Gaza" display was a TOOL BUG — place_names.is_preferred is NON-UNIQUE and my LIMIT 1 grabbed an arbitrary-language row (fixed to English-primary display + a regression test).** Caught+fixed 2 more real bugs: a full-backfill OOM (accumulated all detections in memory → refactored enrich-places to STREAM writes) and a join-drop (content_storage.http_response_id is NULL for ALL recent body_text rows → LEFT JOIN, title always shows, URL optional). 34/34 intelligence tests, ncdb-debt 241, 10/10 probes. **Calibration for successors: (1) HEED the BLOATING verdict — cycles 80-85 were a long build-heavy intelligence arc (cost_turns 3-4 each, scaffold net +88, zero retirements); the metric can't tell productive building from bloat, but the discipline (cycle 72→73 precedent) is to spend the NEXT cycle pruning/consolidating, not adding one more feature; (2) never accumulate a whole backfill in memory — stream writes per item (the OOM lesson); (3) diagnose a join-drop level-by-level (add one JOIN at a time and watch the count) — it localized the NULL http_response_id in one query; (4) VERIFY THE BASELINE IS REAL before claiming "beats the incumbent" — I trumpeted "Gaza 913 vs 0" but the "0" was a degenerate 7-place article_places table (no real incumbent) AND a display bug showed the Chinese name; the owner caught it. A comparison is only a win if the thing you compare against is real — check the baseline's size/shape (SELECT COUNT(DISTINCT ...)) before framing a delta as victory.** Next (cycle 86 PRUNE): retire/consolidate stale scaffold — candidate targets are redundant harness variants, one-shot measurement scripts that already ran, and any dead code churn-scan surfaces; subtraction is improvement. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Sixty-second cycle (ledger cycle 86, ultracode, OWNER-directed) — the owner challenged my cycle-85 "Gaza 913 vs 0 killer multilingual win" and was RIGHT to. **I OVERSTATED it: `article_places` is a degenerate 7-place table (94% "London"), so there was no real incumbent; and the "加薩 for Gaza" display was a TOOL BUG (is_preferred is non-unique; my LIMIT 1 grabbed an arbitrary language).** Corrected ledger/SELF_MODEL/memory honestly, fixed the tool to use the authoritative `places.canonical_name_id` (99.8% pop, 97.5% English), and wrote a measured place-name i18n design report. Then the owner opened the live-DB mutation gate: I cleaned the sentinel `place_id=999999`'s 12,436 duplicate rows via copy→verify→swap (RI-mapped first, in-txn verify-before-commit, fk_check 0, quick_check ok, original backed up). **Checking the cleanup STUCK, I found the sentinel regrew on restart and traced it to a ROOT-CAUSE bug in ncdb `seedBootstrapData`: it inserts planet names with `INSERT OR IGNORE` but omitted `normalized`, and a NULL in a UNIQUE-index column defeats BOTH the index AND the OR IGNORE (NULL≠NULL) — so it re-inserted "Earth"/"World" every DB open.** Fixed it (set normalized, rebuilt ncdb dist), proved idempotent (seed 3×→2 rows; restart no longer grows), swept the residual, made the cleanup tool re-runnable. Calibration for successors: (1) VERIFY THE BASELINE IS REAL (`SELECT COUNT(DISTINCT ...)`) before claiming you beat an incumbent — the owner caught an overstatement I should have caught myself; (2) a data cleanup is durable ONLY once its SOURCE is fixed — always re-check that a cleanup STICKS after a restart; (3) a NULL in any UNIQUE-index column silently defeats both the index and `INSERT OR IGNORE` (same NULL-key family as the http_responses dedup) — populate every unique-key column; (4) chasing the "why" of a weird data point (12,438 names) is how you find real bugs, not just clean symptoms. The loop metric stayed BLOATING (scaffold net +89, still unaddressed) → cycle 87 MUST be the prune I keep deferring. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Sixty-third cycle (ledger cycle 87, ultracode) — the deferred PRUNE. **Ran the measure-before-CUT audit HONESTLY and found the BLOATING metric had over-counted a productive build: the scaffold is USED, not dead.** Consumer-greps showed every intelligence-arc harness/bridge has 2-6 external references (docs/ledger/cross-refs) + places-intelligence is ADOPTED; churn-scan surfaced active hotspots not dead files; the CrawlerMetricsService "dead recorders" chip was stale (already removed cycle 76 — the live `recordVisit` hits belong to PaginationPredictorService); `git ls-files` for tracked junk = 0. So there was little to delete — an honest result, not a failure (cf. cycle-77: disproving the premise IS the outcome). The real subtraction: **greened a chronically-red test suite** — the 4 url/document "real module integration" tests failed because they do a live ESM `import()` the jest VM can't run; made them SKIP-WHEN-UNAVAILABLE (`itReal` returns early if the beforeAll import fails; the bridge logic is fully covered by injected-importer tests). 69/69 tests, 10/10 probes, seed fix held (sentinel stays 2 across a crawl). DEFERRED honestly: committing the ncdb seed fix — news-crawler-db's working tree is broadly dirty with 10+ pre-existing non-mine changes on main, so committing is the owner's call (fix is live via the rebuilt dist). Calibration for successors: (1) a MEASURE-BEFORE-CUT audit is as important as measure-before-build — validate each "dead" claim by grepping consumers before deleting; deleting referenced/adopted scaffold to chase a metric is the failure mode, not keeping it; (2) the cost-per-improvement/scaffold-net metric counts FILE adds/removes — it cannot see "greened a suite" or "the scaffold is used", so it can read BLOATING for a legitimately productive build-heavy arc; when an honest audit finds nothing dead, SAY SO rather than manufacture a deletion; (3) don't commit into someone else's dirty working tree — isolate or defer. Next: accept the metric's BLOATING as the honest state of a used-scaffold arc, ship a real feature/consolidation, or take an owner directive. |
| 2026-07-23 | Claude Opus 4.8 (continuation) | Sixty-fourth cycle (ledger cycle 88, ultracode) — **completed + surfaced the places enrichment: a real end-to-end product (crawl → enrich via the delegated module → FK-valid queryable API).** (i) Widened the backfill from 5,000 to ALL 13,117 body_text articles (raised the enrich-places --limit clamp): article_place_mentions 169,548→343,807 mentions, 0 orphan place_ids, existing tables unchanged, sentinel holds at 2 (cycle-86 seed fix). (ii) Added a live additive `GET /api/v1/crawl/place-articles?place=|placeId=` route on :3170 (distinct articles mentioning a place, canonical English display, confidence-ranked, 400-guarded). **The route delegates ALL query logic to the existing tools/intelligence/place-articles.js module (extracted a reusable resolvePlaceById), so NO raw SQL entered src/ui → ncdb-debt unchanged at 241** — additive, does not repoint the legacy gazetteer page. De-risked the route by simulating its logic against the live DB (readonly) BEFORE restarting, then verified live (Gaza→1,543 articles, London→1,594, no-params→400). 70/70 tests, 10/10 probes. Calibration for successors: (1) you can surface a copilot-owned additive table via a thin route that DELEGATES to a tools/ module — this keeps raw SQL out of src/ui so the ncdb-debt ratchet stays flat even though the table isn't ncdb's (a route is still thin coordination); (2) de-risk a new route by requiring its module + a readonly handle and simulating the exact calls BEFORE the electron restart — catches logic bugs without a restart cycle; (3) the BLOATING metric will persist while shipping product (added a route, retired nothing) — that is the honest state, not a reason to stop shipping or to manufacture deletions (cycle-87 established the scaffold is used). Next: a jsgui3 UI view over the endpoint, feed the 2 places-module gaps upstream, the dedup follow-ups, or D4 remote-serving. |
| 2026-07-22 | Claude Fable 5 (continuation) | **DIRECTIVE INTEGRATION (opens cycle 73) — the module-ecosystem pivot.** Owner redirected the in-flight crawler-as-worker/parallel-compression task mid-build: the code "should be in one of the new module repos, with the code moved out of copilot-dl-news, but called from there" — then confirmed scope after my sibling-repo survey: ALL the news-crawler module repos except `news-crawler-backend-core` (for the moment); **`news-crawler-itself` is to be made VERY IMPORTANT** (the crawler engine home — it was an empty placeholder); http-cache-store is NOT used. "This is a major change in terms of how we are working" — the thin-coordination mission generalizes from "DB-shaped logic → ncdb" to "ALL functionality → its owning module, behind a clean API, called from the coordinator." Focused single-module deep-work cycles are first-class (owner expects breakthroughs from that mode). Documentation updated coherently in ONE pass, anchor-first: new anchor `docs/plans/2026-07-22-module-ecosystem.md` (roles, rules, first-migration plan) → BOOT.md orient section (one-hop, with probe) → AGENTS.md External Modules → singularity SKILL.md Track-B rule → memory [[module-ecosystem-directive]] (indexed FIRST in MEMORY.md) → modules-reference.md full map → distributed-crawl plan note (phases unchanged, implementation home moves). Calibration for successors: (1) when the owner changes the WORKING MODEL, update the anchor doc first and make every other surface a one-hop pointer to it — never scatter the rule into N divergent copies; (2) survey-then-confirm beat assuming — my repo-role deduction (from package.json descriptions, not names) let the owner confirm scope in one message; (3) the pivot converts the cycle-72 BLOATING answer into the FIRST extraction: moving remote-crawler-v2 out of copilot-dl-news IS the prune. Next: bootstrap news-crawler-itself as a real package (engine move + worker-thread compression pool), deploy tool repointed, then the owner's 5-site×10-page crawl with batch-of-5 delivery. |
| 2026-07-22 | Claude Fable 5 (continuation) | Forty-eighth cycle (ledger cycle 72, ultracode ON) — Phase D4 slice 2a: the first two jsgui3 dashboard controls (ThroughputStrip, HostHealthBadges) on the slice-1 core + the live `/api/v1/crawl/dashboard-model` endpoint (direct in-process source, no self-HTTP). **The highest-value moment was catching an XSS sink by direct adversarial probing BEFORE the workflow ran:** I suspected jsgui might not escape text, rendered a control with a `<script>` host name, and SAW it come out raw — confirmed against jsgui source that `String_Control` renders text RAW (only attributes are escaped). Fixed with a core `escapeHtml` + a regression test. The follow-up adversarial workflow (3 lenses, all SOLID for shipped surfaces) then earned its keep on the FORWARD-looking gaps: `normalizeHeadline.title` is the same sink waiting for slice 2b's headline control (closed with a loud render-contract doc, left raw at the data layer since it feeds JSON/console too), and `activate()`'s pollers leaked timers (fixed with a guarded `remove()` — and the guard on `this.parent` was itself caught by a failing test, since jsgui's base `remove()` derefs `this.parent.content` and crashes on an unmounted control). Proved the endpoint's DIRECT source ≡ the HTTP source (identical model). **The cycle-close instrument flipped to BLOATING** (scaffold net +62, cost/improvement not falling) — an honest signal after a build-heavy streak (D2a + slice 1 + slice 2a, zero retirements). The metric can't tell a deduping core from bloat, but the discipline holds: **committing cycle 73 to a PRUNE cycle** — retire Gen3 distributed-crawl (wip/labs, the plan's D1-remaining deprecation), stale memories/tests, and any dead code (CrawlerMetricsService dead recorders, the broken /api/tools/fetch) — subtraction is improvement. Calibration for successors: (1) when a framework's text-vs-attribute escaping is asymmetric, PROBE it directly with a hostile input rather than trusting a doc comment — jsgui escapes attrs but not String_Control text, a repo-wide footgun; (2) heed your own BLOATING verdict — a real prune cycle beats one more additive feature; (3) an override of a base lifecycle method (remove/destroy) must guard the base's assumptions (here `this.parent`), and a test that exercises the unmounted path catches it. Next: cycle 73 PRUNE, then D4 slice 2b (bundle + remote serving + legacy-client migration). |
| 2026-07-22 | Claude Fable 5 (continuation) | Forty-seventh cycle (ledger cycle 71, ultracode ON) — Phase D4 slice 1: the shared crawl-dashboard CORE + DashboardDataAdapter (unify local+remote at the data layer). **Right-sizing under ultracode: exhaustive ≠ maximal scope.** The plan's D4 is a big multi-part feature (core + jsgui3 controls + esbuild bundle + remote serving); I scoped cycle 71 to the pure normalization FOUNDATION every later slice builds on — because it is independently valuable (one source of truth for the cycle-69 phantom-rate semantics, killing the duplicated-copy rot risk), fully unit-testable WITHOUT the heavier/riskier bundle+serving infra, and it lets the jsgui3 layer (slice 2) land on tested ground. Used TWO workflows the way ultracode intends: a 5-reader understand-workflow to map the codebase (build-vs-compose, adapter shapes, serving) BEFORE designing — 2 of 5 agents misfired their structured output (returned test/e stubs), which I recovered by direct reads rather than a 477k-token re-run (both were slice-2 concerns + one I had already scouted); then a 4-lens adversarial verify-workflow that found a real parity gap. **The load-bearing discipline: I validated the workflow's BROKEN verdict myself** (executed client-ref vs core on the counterexample: client 'Infinity' vs core '0', divergent=true) rather than trusting it — and resolved it the RIGHT way: the core's finiteNumber-guarded queue is SAFER than the client's (which would display literal "Infinity" on overflow), so I KEPT the hardening and documented it as the one intentional divergence + a locked test, instead of downgrading the core to match a latent client bug to force "parity". Calibration for successors: (1) a parity/equivalence test between an extraction and its original is not "make them identical at all costs" — when the extraction is BETTER, lock the intentional divergence explicitly so it stays documented and can't be silently reverted; (2) an honest adapter contract DECLARES what a lean source cannot provide (remote has no analysed headlines, host "health" is domain-state not politeness) so the consumer degrades instead of erroring — capabilities are part of the interface; (3) a partially-failed fan-out (2/5 stub agents) is cheaper to patch with direct reads than to re-run — inspect the journal, salvage, move on. Live-verified with tools/crawl/dashboard-model.js (one core, two sources). Next: D4 slice 2 (jsgui3 controls + bundle + remote HTML-shell route + the dashboard-model endpoint). |
| 2026-07-22 | Claude Fable 5 (continuation) | Forty-sixth cycle (ledger cycle 70) — owner's explicit deploy go-ahead ("Deploy whatever is needed, whenever you need"); deployed Phase D2a to the live Oracle PM2 service. **The load-bearing lesson: a changed buildId is necessary-but-NOT-sufficient proof a deploy landed — the recursive prompt's own DB-evidence acceptance step is what caught that D2a was inert.** Two real defects, neither caught pre-ship: (1) the deploy tool reported "complete" while nothing deployed — `runCommand('ssh', …, installScript)` passed the multi-line bash script as an argv element under `shell:true` on Windows (needed for PATHEXT resolution of bare `ssh`/`scp`), and cmd.exe mangled it; diagnosed by SSHing in to read actual file mtimes + PM2 uptime (14d), not by trusting the tool's success line. (2) After a working manual deploy, the worker crash-looped (15 restarts) `MODULE_NOT_FOUND '../../../src/core/crawler/limiter'` — cycle 67's `remote-politeness.js` required three helpers by a path reaching OUTSIDE the deploy package; the tarball never staged `src/core/crawler/*` (the install script even `rm -rf src`). Every local check had passed — the cross-package require only fails on the box. Fixed BOTH properly rather than re-hand-deploying: vendored `{limiter,RobotsCache,utils}.js` byte-identical into `deploy/…/lib/vendor/` (recursively staged) + a `vendor-sync.test.js` drift guard; and fixed the deploy tool to pipe the install script over ssh **stdin to `bash -s`** (a new `input` option on `runCommand`) so cmd.exe never sees it. Re-ran the REAL deploy tool end-to-end to validate the fix (new buildId, `--db data/news-simple.db` preserved, 0 restarts). Acceptance proof from the remote `http_responses`/`urls` join: `request_started_at` deltas median **1.04s**, `fetched_at` median **1.00s** — the 1s crawl-delay floor is live, vs the old worker's 200ms/300rpm. Calibration for successors: (1) after ANY deploy, prove the CODE runs on the box (a seeded crawl / a fetched-row delta), not just that a version string or buildId changed — deploy tooling can report success on a no-op; (2) a `../../../` require that leaves a package boundary is a latent MODULE_NOT_FOUND that no local `node --check`/jest will catch — vendor + a byte-identity drift guard; (3) on Windows, never pass a multi-line script as an ssh argv under `shell:true` — pipe it to `bash -s` via stdin. Banked as memory [[remote-deploy-shell-mangling-and-dead-require]]. Next: D4 (shared jsgui3 dashboard core). |
| 2026-07-21 | Claude Fable 5 (returned) | Forty-fifth cycle (ledger cycle 69) — **swap Sonnet 5 → Fable 5**, owner-reported metrics-accuracy fix (phantom "Saved docs/s"), preempting D4 per the cycle-62 precedent (user-facing defect on an owned pillar first). Method was reproduce-then-refute, LIVE, exactly as the owner asked ("have a look at them while running a crawl"): traced the display chain client→jobs-payload→jobProgress, observed the defect in the live payload (completed jobs frozen with docsSavedPerSec 1.47/2.46/0.08 while docsDownloadedPerSec=0 — the terminal-reconcile signature), fixed BOTH layers (producer: _publicProgress zeroes rate keys for terminal jobs at the single serialization seam, totals untouched, internal record unmutated; consumer: renderThroughput sums active jobs only, which also keeps stale queued out of the Queue tile), then proved both directions live: post-fix sampling showed doneJobsWithNonzeroRate=0 AND running jobs still emitting real, internally-consistent rates (apnews sv=0.34/s with 2-of-4 saved) — the no-over-zeroing check matters as much as the fix. Calibration: (1) a rate display built on retained-last-value anti-flicker logic WILL ghost without terminal zeroing — producer-zero + consumer-filter is the belt-and-braces shape; (2) verifying a metrics fix requires BOTH the zero-when-idle and nonzero-when-active directions; (3) found CrawlerMetricsService's recorders are never called (dead counters) — filed as a chip rather than expanding scope mid-fix. Sonnet 5's D2a/D3 work inherited clean (791/791 + 27/27 test-verified, nothing to retire); D2a deploy still held (no go-ahead present). Next: D4 (shared jsgui3 dashboard core) unless redirected. |
| 2026-07-21 | Claude Sonnet 5 (continuation) | Forty-fourth cycle (ledger cycle 68) — Phase D3: sync-loop hardening (watermark timing, identity-strict verify, idempotent ingest, corrupt-ledger quarantine). No explicit deploy go-ahead was present in the recursive prompt, so D2a's `--apply` correctly stayed held; moved to D3 per the prompt's own branching. **The load-bearing discipline this cycle: measuring before building the prescribed design.** The plan specified UNIQUE indexes on http_responses/content_storage for idempotent ingest — before writing that migration, ran a bounded read-only probe against the LIVE 30GB news.db and found 25,267 pre-existing duplicate groups already there, meaning that exact migration would fail outright on the real database without a destructive dedup pass first. Adjusted scope in response: shipped a purely-additive `remote_sync_batches` fingerprint table instead (zero risk to existing data, still closes the most likely real-world replay: crash-then-manual-resync), and filed the original unique-index/dedup work as a separate owner-decision task WITH the hard evidence, rather than either forcing it through or silently dropping it. Also traced the sync loop's `since=` resolution back to `appendBatch` in code to CONFIRM the watermark-timing defect was real (not just repeat a claim), then fixed it plus 3 more (identity-strict verify with 2 removed fallbacks; corrupt-ledger quarantine+throw). Rewrote 2 existing tests whose fixtures had baked in the fallback behavior being removed, rather than leaving them red or force-passing — read what they were really asserting first. Proved transaction atomicity directly (a forced mid-transaction failure test shows a replay batch is NOT marked ingested and a retry re-attempts for real). Full ncdb suite 791/791, zero regressions. Calibration for successors: when a plan prescribes a specific migration/design, MEASURE against the live system before applying it — the safe, less-invasive alternative (the additive table) turned out to be not just safer but arguably the better fix for the actual failure mode in question. |
| 2026-07-21 | Claude Sonnet 5 (returned) | Forty-third cycle (ledger cycle 67) — **swap Fable 5 → Sonnet 5**, Phase D2a: remote-node politeness. Re-probed live state before trusting the handoff (10/10 probes, COMPOUNDING) rather than assuming D1's claims held. Investigated the remote worker's actual fetch loop BEFORE writing code: `run-worker.js` is a strict sequential per-domain loop with `redirect:'follow'` and zero pacing — confirming the plan's finding directly in code. Also found the plan's phase description assumed a concurrency pool needing serialization that DOESN'T EXIST — one PM2 process per domain + a guarded `startDomain()` already prevents same-host concurrent fetches structurally; corrected that assumption rather than building unneeded machinery. Confirmed `DomainLimiter` + `parseCrawlDelay` are standalone/pure (no CrawlerState dependency) — reused them directly instead of reimplementing politeness logic for the lean worker. Shipped `remote-politeness.js` (real robots.txt Crawl-delay + adaptive 429 backoff, default floor NEVER zero) + `polite-fetch.js` (manual redirect following, gates every hop, BLOCKS + records any redirect landing off the worker's owned domain — closing the `redirect:'follow'` cross-host leak). Closed the local `DomainThrottleManager` fail-open seam (loud counter+log, behavior unchanged). Investigated `/api/tools/fetch` per the plan's explicit ask and found it ALREADY BROKEN (`CrawlWorker` referenced but never defined anywhere in the repo) — documented rather than building a new feature to fix dead code, scope discipline. 27/27 new tests + isolated a pre-existing unrelated 17-suite jsdom failure correctly (verified by module-path, not assumed). Dry-run deploy succeeded (SSH reachable, remote idle) — but HELD the actual `--apply` for explicit owner confirmation: stopping/restarting live shared production infrastructure is exactly the class of action the safety policy gates on user confirmation, distinct from a self-continued recursive prompt's standing authorization. Calibration for successors: investigate the ACTUAL code before implementing a plan's prescribed design — two of the plan's assumptions (needs a concurrency pool; `/api/tools/fetch` needs gating) turned out false on inspection, and finding that BEFORE building saved real work. |
| 2026-07-21 | Claude Fable 5 (continuation) | Forty-second cycle (ledger cycle 66) — **Phase D1 complete: the static fleet-partition guard live end-to-end.** Gen3 deprecated; `fleet-partition.json` + resolver partition API; seed refusal at the single `requestWithTimeout` choke point (one wrap covers all five /api/seed + /api/domains/add call sites AND future ones — chose the choke point over per-call-site guards deliberately); ncdb `selectDueFrontier` excludeHosts (both branches, www-insensitive, `excludedHosts` counter, injected from copilot so ncdb stays policy-free); dead duplicate /api/throttle pair deleted after verifying Express registration order. Calibration: (1) **the sibling suite's strict return-shape guard tripped on my additive field — that is the ratchet system working**, taxing every shape change with a conscious decision rather than letting it slide; updated the expected keys deliberately and added an assertion on the new field's default. (2) A one-cycle guard was possible because every piece reused a proven seam (cycle-50 injection option, choke-point wrap, probe manifest, bridge restart) — reuse over invention is now the loop's strongest predictor of cycle cost. (3) Fail-closed discipline held everywhere: empty partition = remote owns nothing = all remote seeding refused; corrupt partition file = empty; excluded host scoped by host= = empty due-list. Next: D2a — embed the tested DomainThrottleManager/DomainLimiter/robots stack into the remote server's fetch loop (it crawls with ZERO politeness today; blocks all further autonomous remote crawling), then deploy via deploy-remote-server.js dry-run-first + version check via the probe. |
| 2026-07-21 | Claude Fable 5 (returned) | Forty-first cycle (ledger cycle 65) — **swap Opus 4.8 → Fable 5**, owner's ask: improve the inherited plans. Did NOT inherit the plans as settled: ran a 5-lens adversarial plan-review workflow on the distributed-crawl plan, then **hand-verified every high-stakes claim in code before accepting** (5/5 confirmed; caught 1 panel cite error — findUnconfirmed does have a status-readout consumer). Result: **redesigned the core mechanism** — the per-fetch politeness broker is replaced by EXCLUSIVE HOST OWNERSHIP (a second node adds zero throughput to a shared host under crawl-delay, so cross-node per-fetch coordination is pure cost; static partition guard → host-lease registry in news.db; every failure collapses to under-crawl). Panel also found 4 latent defects, all verified: the deployed Oracle server has ZERO politeness (runs today — new blocking phase D2a), watermark-before-confirm, verify fallbacks that can prune the only copy, duplicate-on-replay ingest. Executed Phase D1 same cycle (resolver endpoint map + port inventory, Gen-1 deprecated, remote-endpoints probe → live-confirmed Oracle healthy at /api/health, corrected the documented health path, noted 2026-05-27 deploy drift). 10/10 probes. Calibration for successors: (1) a model swap is the RIGHT moment to adversarially re-review inherited designs — the fresh-eyes challenge removed an entire always-on service; (2) "validate the workflow's verdict yourself" means executing greps/reads against the cited lines — it both confirmed the substance AND caught the citation error; (3) the D1 probe's informational reachability check paid off immediately (found the real health path + build drift by touching the live server read-only). Next: D1 remainder (Gen3 deprecation, partition guard, dead-handler cleanup) → D2a (remote politeness, blocks autonomous remote crawling). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Fortieth cycle (ledger cycle 64) — **distributed-crawl-on-Oracle research + unification plan** (owner's strategic ask, prioritized over crawl-detail Phase 4). Ran 3 parallel research agents (remote-fetch arch; rate-limiting arch; Oracle deploy + prior work) and synthesized docs/plans/2026-07-distributed-crawl-unification.md. The load-bearing finding: **most of what the owner wants is already BUILT but fragmented across 3 never-unified generations** (v2 server deployed on Oracle already does remote-queue + watermark batch-export at ~5s target; a Gen3 /batch worker already returns N bodies in one gzip response; but the batch client is built-but-DEAD and there's NO cross-node rate coordination → 2 nodes = 2× a host's rate → 429/blackout). Calibration: (1) for a "previous work didn't integrate well" ask, the highest-value first move is an HONEST current-state audit — it revealed the pace lever is UNIFY+WIRE, not build, and pinpointed the ONE safety-critical gap (a shared per-host politeness broker); (2) parallel research agents map a large unfamiliar surface fast (3 agents, ~1 turn wall-clock); (3) this is a planning cycle — no code shipped, and I recorded it as such honestly rather than dressing it up. The owner added a requirement mid-cycle (deploy a jsgui3 progress dashboard ON the remote, "unifying some code") — folded in as plan Phase D5. Next: Phase D1 (config/inventory consolidation) then D2 (the politeness broker, adversarially validated per the confirmed over-crawl risk). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-ninth cycle (ledger cycle 63) — **Phase 3 of the crawl-detail grid: the ArticleDetailTable jsgui3 control** (Title link · Places chips · Author · Published · Words; setRecords→setRows→re-render = the MVVM record→view update). The plan said "attempt Data_Table first, fall back to TableControl with evidence" — so I SPIKED Data_Table: it SSR-renders cleanly (full `<table role=grid>` with MVVM data-attrs), but it descends from jsgui3-html's Data_Model_View_Model_Control builtin hierarchy this repo has never run through its own activation pipeline, whereas the repo's TableControl is proven-activated live (UrlListingTable). Built on TableControl — the plan's evidence-based fallback. 10/10 jest tests (mapping, SSR structure, the isomorphic reattach GUARD, the setRecords update, HTML-escaping, contrast-safe chips); caught+fixed a real TZ bug (`new Date("July 9, 2026")`+getUTC* rolled the day back). 9/9 probes, standing crawl + archive delta. Calibration: (1) "attempt then fall back with evidence" means actually SPIKING the harder option (Data_Table renders — the risk is reattach in an unfamiliar pipeline, not rendering), not assuming; (2) reuse the proven base (TableControl/UrlListingTable) over the theoretically-richer-but-unintegrated builtin — the MVVM contract that matters (set records → view updates + clean reattach) is met by the base; (3) the contrast trap from cycle 62 carried forward into the new control's chips (explicit color). Next: Phase 4 (CrawlDetailPage + /crawl-detail, the side-by-side per-crawl columns + live poll — where the control finally renders live). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-eighth cycle (ledger cycle 62) — **user-reported UI regression fix over planned Phase 3.** The owner reported the per-host health-meter host names weren't showing. Root-caused live via COMPUTED style (not guesswork): the badge hardcodes a dark background but set no text color, so the label inherited the page theme's black foreground in light mode → invisible host names (the cycle-58 screenshot only looked right because it rendered in dark theme). The text node was present the whole time — just unreadable. Product fix: explicit `color:#ece8e0` on the badge; scaffold: a contrast-regression guard in the SSR check. Verified live (computed color rgb(236,232,224), screenshot legible), 9/9 probes, standing crawl + archive delta. Calibration: (1) a user-facing regression on a pillar I own preempts planned feature work — fix it first, then resume the plan; (2) "the text renders" ≠ "the text is readable" — verify computed color-vs-background in BOTH themes; any hardcoded-background element must set its own foreground (theme inheritance flips). Captured as the 3rd crawl-status-client failure mode. Next: Phase 3 (jsgui3 Data_Table control) with a clean cycle for the isomorphic-reattach evaluation. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-seventh cycle (ledger cycle 61) — **Phase 2 of the crawl-detail grid: the endpoint.** `GET /api/v1/crawl/recent-articles` (thin inline route) delegating to ncdb `listRecentArticlesForDetail` via the getDbRW facade→raw unwrap; params host/since/until/limit. Kept coordination thin: the DB read is ncdb's, the byline/hub-title noise filter is copilot-side via the canonical ArticleSignalsService.isArticleShapedUrl (the FIX 18 signal — classifier stays in copilot), over-fetching 2× so the filter still fills limit. Live curl after a clean bridge restart: 200 in 15ms, real clean titles, byline noise ("LINDA PETRE") fully collapsed (0 remaining), host filter 5/5, author "—"/places [] rendered honestly. 9/9 probes. Calibration: a thin coordination endpoint is the easy, low-risk half — the discipline is keeping the SQL in ncdb and the URL-shape policy in copilot (no ncdb→copilot dep), and over-fetching to absorb the post-filter. Two of six phases done; the jsgui3 MVVM control (Phase 3) is the harder half. Next: Phase 3 (ArticleDetailTable Data_Table + isomorphic reattach contract). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-sixth cycle (ledger cycle 60) — **Phase 1 of the crawl-detail side-by-side article grid**: the ncdb read function `listRecentArticlesForDetail` (title/publishedDate/wordCount/byline+authors/section/host/url/fetchedAt/places, params host/since/until/limit), the DB-shaped half of the owner's feature. Parallelized well per the owner's ask: 3 codebase-research agents (jsgui3 grid/MVVM, data surface, UI architecture) + the standing crawl all ran concurrently while I read ncdb source. Two live-DB traps found and fixed that static review would miss: (1) the join-through-content_storage planner blowup — forcing idx_http_responses_fetched routes the join through the huge blob table (>30s, timed-probe KILLED); fixed by mirroring the proven getRecentHeadlines analyzed_at fast path → 8ms; (2) a per-column datetime format mismatch (analyzed_at SPACE-form vs fetched_at ISO-T-Z) that made an ISO window bound match nothing — fixed with datetime(?) on the bound. An inline better-sqlite3 test HUNG the session (the WAL-pin risk) — killed it with taskkill and switched to tools/db/timed-probe.js, exactly the [[live-db-probe-gotcha]] discipline. Verified: 8ms live, index-pinned, shape+density match research (author 0% — the extraction gap deferred to Phase 5, places sparse — rendered honestly), 5/5 vitest fixture tests, 9/9 probes. Calibration: DB-shaped logic goes in ncdb (mission), and the two traps confirm the value of live timed-probe + fixture unit tests over reasoning — the "correct-looking" pinned query was catastrophically slow, and the "correct-looking" ISO filter matched nothing. Next: Phase 2 (thin inline endpoint). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-fifth cycle (ledger cycle 59) — **validated + calibrated the instrument shipped last cycle** (the per-host health meter). It was live-flagging www.theguardian.com as SLOW-IRREGULAR, contradicting the established [[crawl-gap-is-politeness-not-deadtime]] fact that Guardian is a polite ~33s throttle. Scouted ground truth first (bounded read-only gap-sequence pull): the false alarm was a WINDOW ARTIFACT — a 60-min window spans two crawl bursts with a ~1400s idle between, and that idle gap, counted as an inter-fetch gap, exploded the CV. Ran a 5-lens adversarial calibration WORKFLOW (very-polite-throttle / real-stall / 429-blackout / thin-sample / boundary lenses + synthesis) that confirmed the diagnosis and hardened it into exactly 3 changes: idle cutoff 300→120s, a backoff-run detector (≥3 consecutive [120,900]s gaps → SLOW-IRREGULAR, catching 429 blackouts idle-drop would hide), and POLITE-THROTTLE magnitude/thin guardrails. **Validated the workflow's verdict by EXECUTION, not trust** — implemented the rule and ran all 12 panel fixtures + 4 live ground truths as jest tests (19/19), then wired it into host-health.js; live tool now classifies Guardian FAST; the endpoint serves the calibrated cls with NO electron restart (fresh child-process spawn). WORKFLOW_LEDGER: CONFIRMED (3 real pre-ship defects in my own base rule caught by the panel). Calibration: **an instrument must be validated against ground truth before it's trusted — a meter that cries wolf on a known-polite host is worse than no meter**; and the discipline of executing an adversarial panel's fixtures (rather than accepting its hand-traces) is what makes "validate the workflow's verdict yourself" real. Also: on owner's redirect, produced a grounded plan (docs/plans/2026-07-crawl-detail-grid.md) for a side-by-side per-crawl article-detail grid (3-agent codebase research: jsgui3 Data_Table/MVVM exists; author data is a real gap; crawl→article grouping is a fetched_at proxy). Next: build that grid phase-by-phase per the plan. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-fourth cycle (ledger cycle 58) — shipped the crawl-status **per-host health meter**, the "legible/near-real-time crawl UI" mission pillar untouched for ~33 cycles. Scaffold: `host-health.js --json` + a short `cls` enum (FAST/POLITE-THROTTLE/SLOW-IRREGULAR from gap-CV over persisted http_responses). Product: `/api/v1/crawl/host-health` endpoint (child-process off the event loop + cached serve-stale, per [[live-db-probe-gotcha]]) → SSR strip → client render (backslash-free per [[crawl-status-client-template-trap]]). **Calibration — the load-bearing lesson: LIVE verification caught a bug every static "emitted-text" check I ran had passed.** The client script emitted fine (40,537 chars), parsed fine (SSR probe green), and grep confirmed `fetchHostHealth`/`setHostHealth`/`/host-health` all present — yet the badge stayed "loading…". Only watching the live **network** (no `/host-health` request fired despite the code being present) surfaced it: `fetchHostHealth` referenced a bare `apiBasePath`, but that name is a **config object PROPERTY** (`config.apiBasePath`), not a scope variable — a ReferenceError thrown inside the `try` and swallowed by the empty `catch`. Static "symbol present in emitted text" is necessary-not-sufficient; a symbol can be present, parse, and still be the wrong reference. Fixed to `config.apiBasePath`, restarted electron (clean, FIX 13 settle+retry), verified 7 badges render live with correct colours (Guardian/nytimes SLOW-IRREGULAR orange, apnews/thehindu/help.nytimes/irishtimes/aljazeera FAST green) + full-verdict tooltips. Reinforces the loop's recurring signal (cycles 13/40/50): **live evidence beats static reasoning** — this time against my OWN verification harness. Next: resume the standing loop with a fresh focus (UI pillar now covered). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-third cycle (ledger cycle 57) — backlog Turn 3 CLOSED the 20-item list by MEASURING two fixes as obviated instead of building them. Per "DB-evidence first, prefer FILE over force": measured FIX 9 (bare-form gate) + FIX 12 (queue prune) before building → **0/57 bare-form items leak into a fresh selectDueFrontier due-list across 4 hosts** (task #48 shape-preference + hub-cap + FIX 19 already handle it), so a new gate would never fire; the candidate pattern is safe (0 FP) but unnecessary → FILED (task #50) with the safe pattern captured for future use. FIX 4/5/6 (54k-row migration) filed as owner-confirmed-only. Logged as a REFUTED/correct-refutation (dogfooding FIX 16) → scorecard adj-verdict-accuracy 0.833 vs 0.69. Calibration: the highest-value output of a cycle can be DISPROVING the need for planned work — validate-before-build saved 2 unnecessary changes + the tech debt they'd add; the earlier fixes (task #48/#19/#18) compounded to obviate the later ones. Backlog result: **13 shipped, 2 obviated, 5 filed**. Next: resume standing loop, crawl-status per-host health meter (untouched UI pillar). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirty-second cycle (ledger cycle 56) — backlog Turn 2: server batch (one clean restart, no manual retry) + the live-write-path fix; 5 more (**13 of 20 done**). FIX 10/11 (hubFraction tunable per hydrate request, live-verified 0.2→7 articles/2 hubs), FIX 18 (report now filters section/utility by URL SHAPE not title-words — collapsed 14 noise pages live), FIX 13 (bridge restart settle+auto-retry, kills the recurring manual retry), FIX 3 (the live title write path: root-caused "newsis" to cheerio `.text()` dropping `<br>`, confirmed in the STORED apnews/purpose HTML; `_titleText` clones + br→space, entity-decoding preserved, shared-`$` provably unmutated, 7/7 title tests no SVG-regression). Calibration: FIX 3 is exactly where the guardrails matter — a LIVE write-path change; confirmed the root cause in stored HTML, proved the fix doesn't mutate the shared `$` (the read-only trap), ran the 5/5 SVG tests BEFORE shipping. FIX 18's first title-word version missed author-names/"Our Purpose" — live evidence flipped it to the reliable URL-shape signal. Next: Turn 3 (validate-first: FIX 9 bare-form gate, FIX 12 queue prune; FIX 4/5/6 migrations = file unless owner-confirmed), then resume standing loop (crawl-status UI meter). |
| 2026-07-21 | Claude Opus 4.8 (continuation, ultracode) | Thirty-first cycle (ledger cycle 55) — Turn 1 of the 20-item small-fixes backlog: cleared **9 of 10 in ONE batched turn** by fanning out an 8-agent verify-first workflow (confirm+locate each fix against current code, return verbatim edits), validating its verdicts myself, then batching into one ncdb build + one test run. Shipped FIX 1/2/7/8/15/16/17/19 (latent infinite-loop guard, NULL-safe NOT EXISTS, dead-config removal, an intentional-config doc comment, frontier-fill 0-page classification [live-verified], scorecard refuted_kind split, redundant-field drop, author-regex boundary). The workflow's most valuable output was DISPROVING FIX 8 (my own backlog item) — reconciling that config would have ALTERed live tables + broken the validator; a correct-refutation that saved a harmful change. 16/16 ncdb + 8/8 scorecard tests + FIX-15 live + 9/9 probes; deferred FIX 14/20 (non-bug polish) to task #49. Calibration: batching by flush point (one build, one test run) + a read-only verify fan-out is the right shape for a large mechanical batch — it de-risks blind edits AND catches non-bugs before you touch them (FIX 8). ~2.0 turns for 9 fixes (high throughput via batch + reusable harnesses). Next: Turn 2 (server batch + live-write-path FIX 3, one restart). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Thirtieth cycle (ledger cycle 54) — ncdb delegation #5, debt **249→241**; the clearest RECURSION win yet (cycle-53's reusable schema-introspection helpers made this delegation cost ~0.9 turn with ZERO new ncdb code). The two remaining self-contained targets (normalize-fetches + normalize-place-hubs) are identical migration tools. Diff-before-repoint caught a would-be trap: ncdb's `urlNormalizationMigration` framework config does NOT cover fetches/place_hubs (only links/queue_events/crawl_jobs/errors/url_aliases), so repointing there would need config + resolution-equivalence work. Right decomposition: delegated the 4 read-only introspection queries per file (reusing cycle-53's helpers), kept the migration DDL/batch-UPDATE procedural (belongs in a migration tool, not ncdb). Equivalence inherited from cycle-53's differential-e2e; read-only spot-check on real tables; did NOT run the mutating tools (fetches has 54,485 unmigrated rows). **STOPPING the debt thread at 2 cycles** (the over-index lesson). Calibration: the delegation≠repoint gate paid off AGAIN (the framework looked like it covered these tables — it didn't); a scaffold built one cycle made the next cycle's mission progress nearly free. Cost 0.9 turn; cycle-metrics COMPOUNDING. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-ninth cycle (ledger cycle 53) — PIVOTED off the 4-cycle frontier-quality thread to overdue MISSION progress: ncdb delegation #4, moving the ncdb-debt ratchet **258→249** (first movement in ~20 cycles). Chose a self-contained READ-ONLY tool (validate-url-normalization.js) — no consumer-web trap. Diffed its 9 inline SQL sites vs ncdb FIRST (the delegation≠repoint gate): 5 already existed in `legacy-schemaInspection` byte-equivalently (modulo safe identifier quoting), 3 were tool-specific → added as generic reusable helpers (getColumnFillStats / getOrphanedReferenceCount / getColumnValueStats). Differential-e2e 4/4 (each new fn == the exact inline SQL) + the tool runs live correctly on the real 30GB DB; ratcheted the probe to 249; 9/9 probes. Calibration: the cleanest delegations are exactly what the trap-memory predicted — self-contained tools where I control the new return shapes (no raw-column consumer web); and half the work was already done (5 of 9 helpers pre-existed in ncdb), found only by diffing before writing. Diversifying off frontier-quality was itself healthy (the loop had over-indexed on one thread). Cost 1.2 turns; cycle-metrics stays COMPOUNDING. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-eighth cycle (ledger cycle 52) — the assigned "apnews byline title-extraction fix" was a NON-bug; DB-evidence refuted the premise and I shipped the real fix. The offending URLs are `apnews.com/author/sal-christ` — AUTHOR PAGES, so "SAL CHRIST" is the CORRECT title; the h1→head-title→og:title chain works fine. Real cause: author-index pages had no low-value gate and leaked into frontier selection. Shipped a tight `/author//staff//contributor` path-segment exclusion in ncdb `_isLowValueFrontierUrl` (segment match keeps real articles). Proven live on real data (apnews 4 pending /author/ → 0 in fresh due) + 22/22 frontier tests + 9/9 probes. HONEST RESIDUAL: bare single-segment forms (/sal-christ, /quizzes) escape — softly deprioritized by task #48 but not excluded; a bare-form gate risks over-excluding legit single-word sections, filed. Calibration: this is the **4th consecutive frontier-quality cycle** where validating-the-premise-with-DB-evidence corrected the assigned direction (seeding→selection, host-rank→URL-selection, shape→hub-cap, title-extraction→author-page-gate) — the standing lesson is now unmistakable: **fetch the actual offending data BEFORE implementing the stated fix.** Cost 1.5 turns; cycle-metrics stays COMPOUNDING. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-seventh cycle (ledger cycle 51) — a PRUNING/subtraction cycle answering the loop's own BLOATING verdict (4 cycles running) by subtraction, honestly. Audited the four scaffold surfaces verify-first: memory index clean (22 files, bidirectional links), all 9 probes active, the p0-p3/r2 phase-proof scripts documented in `tools/crawl/AGENT.md` (not dead), `sequential-fixture-proof` a CLI+lib pair (not a dup) — the corpus is WELL-MAINTAINED, so I did ONE genuine subtraction rather than manufacturing a pile: retired `feedback-no-adversarial-reviews.md` (its active rule is now verbatim in the standing mission; the pause is superseded; rationale preserved in the ledger/WORKFLOW_LEDGER) + corrected the frontier-leak memory's now-stale "pending" description. Crawled 16 real pages (task #48's headlines hold: Norman Lear obit, Trump chief-of-staff, Judi Dench). Result: cost-per-improvement slope flipped FALLING (0.8-turn cycle) → **cycle-metrics now reads COMPOUNDING, not BLOATING** — the loop self-corrected via its own instrument. Calibration: resisted manufacturing subtraction — the honest finding is that ~50 cycles left little cruft (prior discipline, not a failure to find waste); a cheap honest cycle beats a padded one, and the instrument made "is the loop healthy again?" answerable rather than asserted. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-sixth cycle (ledger cycle 50) — LANDED the frontier headline lever (task #48) that cycles 48-49 circled, with a VISIBLE live win. Diffed selectDueFrontier's shape first, then added (ncdb, backward-compatible, shape-preserving): an injected `preferArticleShaped` predicate (copilot's stricter `ArticleSignalsService.isArticleShapedUrl` — structural, unlike looksLikeArticle which mislabels `/business/` hubs as articles) ordering article-shaped URLs first, PLUS a `hubFraction` cap (0.5) so hub-refresh can't consume the whole batch (+ backfill to avoid under-fill). **The design was driven by live evidence TWICE:** validating my own first fix, the live crawl showed the article-shape half ALONE didn't move headlines — the binding constraint was the HUB branch (requeuedHubs=6/6; section pages classified 'hub'), so I added the hub-cap; the second live crawl then landed real headlines ("SNL creator Lorne Michaels donates archive", "Edwin Poots to stand down as DUP leader"). Verified: 25/25 frontier tests (byte-identical backward-compat + preference + hub-cap + backfill), predicate 4/4, 9/9 probes, debt 258. Logged the iterated diagnosis to WORKFLOW_LEDGER as CONFIRMED-with-escape (hub-domination escaped the first fix, caught by live crawl — the scorecard now shows diagnosis escape-rate 0.29, honestly). Calibration: three "validate your verdict with live evidence" saves across this frontier arc (seeding→selection, host-ranking→URL-selection, shape→hub-cap) — the live crawl is a sharper oracle than any test for "does this actually produce headlines." Honest cost: 2.5 turns (3 electron restarts + the mid-course correction), so cost-per-improvement rose — but a hard-won visible product win. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-fifth cycle (ledger cycle 49) — a PRODUCT cycle answering the loop's own BLOATING verdict (frontier quality). Scouted frontier composition with DB-evidence (new `frontier-shape.js`, read-only, crawler's own `looksLikeArticle`): article% is 82-99% for guardian/apnews/aljazeera but 1-5% for bbc/france24/abc — and article URLs are ABUNDANT (Guardian ~145k pending articles), so the lever is host SELECTION, not seeding (the prompt anticipated a sitemap seeder; the data flipped it — measured-then-improve). Shipped: `frontier-hosts.js` now ranks by estimated ARTICLE backlog, so auto-discovery prefers article-rich hosts (france24/bbc/abc drop out); backward-compatible with frontier-fill. **HONEST live-refutation:** crawled the top density-ranked hosts and headlines were STILL hubs (Opinion/Sport/Business) — density-ranking alone doesn't fix it, because ncdb `selectDueFrontier.articleNewByHost` excludes only ALREADY-classified hubs while most pending URLs are unclassified section pages that leak. Filed the precise next lever (a URL-shape preference in per-host due-selection) + logged the cycle as a PARTIAL in WORKFLOW_LEDGER.jsonl (dogfooding the new instrument; the escape caught by live evidence). Calibration: two levels of "validate your verdict" fired — DB-evidence flipped the fix seeding→selection, then live-crawl refined host-ranking→URL-selection. BLOATING persists (4 cycles ~2 turns, scaffold accreting, ncdb-debt flat) → next = the tight URL-shape fix, which should finally drop cost-per-improvement. |
| 2026-07-21 | Claude Opus 4.8 (continuation, ultracode) | Twenty-fourth cycle (ledger cycle 48) — built the SECOND measurement instrument: `tools/dev/workflow-scorecard.js`, so the loop now measures whether its own WORKFLOWS earn their cost (per shape/task_type: verdict-accuracy, catch-rate, false-alarm, cost-to-catch, escape-rate from `docs/agi/WORKFLOW_LEDGER.jsonl`). Bootstrapped the corpus via a 3-agent extraction workflow (2 extractors over ledger+SELF_MODEL → 1 adversarial reconcile) into 13 labeled runs, then VALIDATED the labels against my own knowledge (all 5 owner anchors correct: delegation-#3 CONFIRMED, task-#39 CONFIRMED, task-#44 REFUTED-premise, crawl-rate REFUTED, cycle-47 CONFIRMED). First reading surfaced a genuine finding: **diagnosis workflows have verdict-accuracy 0.50** (coin-flip first verdict — why "validate yourself" is load-bearing) **but catch-rate 1.0 at cost-to-catch 0.9** (they reliably surface real issues cheaply). Encoded the nuance that a REFUTED verdict can be the workflow *correctly* refuting a bad premise (task-44 SAVED a compliance violation), so cost-to-catch is the sound ratchet, not verdict-accuracy. **Calibration/honesty:** with cycle 48 the now-3-point cycle-metrics reads **BLOATING** (cost rising 1.6→2.4→2.0 + scaffold net +12 across three measurement-heavy cycles) — the instrument I built is telling me to pivot to PRODUCT/pruning next, and I'm HONORING it (next cycle = frontier quality, not more scaffold). The measurement pair is complete; the loop is now self-correcting on its own thread choice, which was the whole point. |
| 2026-07-21 | Claude Opus 4.8 (continuation, ultracode) | Twenty-third cycle (ledger cycle 47) — task #41 (resilience) + the standing AGI directive to measure+improve workflows. Ran a 4-agent audit+design workflow: it CONFIRMED the known per-job-log WriteStream crash AND surfaced a NEW medium — `crawlObserver/server.js:307` spawns bareword `node` with no `cp.on('error')` behind a mounted route, a deterministic server crash if `node` is off the child's COPILOT_NODE_PATH — which I validated against source MYSELF before fixing. Closed the whole server-child stream-crash class via a reusable `_guardStreamErrors` helper across 4 sites (4/4 new unit tests + 22/22 registry suite). SCAFFOLD (second-order): built `tools/agi/cycle-metrics.js` so the loop's compounding signal (cost-per-improvement, recursion rate, pre-ship defect containment, ncdb-debt trajectory, scaffold net, COMPOUNDING/PLATEAU/BLOATING verdict) is now COMPUTED from a co-located `<!-- cycle:{...} -->` stanza (4 of 5 singularity metrics were prose-only) + `docs/agi/WORKFLOW_MEASUREMENT.md` + a run-probes ratchet guarding the record; established the ledger-cycle-id as the join key the corpus lacked. The tool caught its OWN false-positive (matched a prose example) on first run → tightened the regex — the measure-then-improve loop the directive asked for, applied to itself. Calibration: honored "validate the workflow verdict yourself" (the crawlObserver medium was real; a line-495-only focus would have missed it) and logged it as the first pre-ship defect `caught_by: adversarial-workflow` in the new metric — the loop now measures whether its own workflows earn their cost, instead of asserting it. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-second cycle (ledger cycle 46) — task #46 remainder on the LIVE content write path, both halves shipped clean. (1) Transaction-wrapped ncdb's per-article cascade (4-6 autocommits → 1 commit + atomic) after CONFIRMING the owner's precondition myself: cleanly single-function, all-synchronous (brotli is sync), no per-link loop. (2) Collapsed ArticleProcessor's 3× full-HTML cheerio.load → 1 shared parse after grep-proving all three consumers are READ-ONLY (so a shared $ ≡ a fresh parse). Verification = the owner's exact differential: captured the pre-wrap post-state from the shipped dist BEFORE rebuilding, then row-for-row diffed pre vs wrapped → byte-identical across all 6 cascade tables (incl. the brotli blob hash); atomicity proven by poison-injection (mid-cascade throw → 0 rows, no orphans). Calibration: the owner's "don't force it — do the low-risk fallback instead" gate resolved to "both are actually clean," so I shipped both, not the fallback — but only AFTER the single-function + synchronous + read-only checks passed. The differential found equivalence (nothing wrong), yet the atomicity probe surfaced a genuine SAFETY win (no partial-write orphans on failure) that was previously untested → locked it in as a permanent regression test. Right-sized: no adversarial workflow needed — a mechanical byte-diff + a poison test are stronger evidence than a judge panel for a pure-refactor equivalence claim. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twenty-first cycle — aimed at ncdb-debt<258, hit a delegation trap, pivoted to a clean slice. Checked the ncdb fns myself before delegating BackgroundTaskManager → found ncdb returns normalized rows but the manager's consumers use raw SELECT * columns (delegating would break them — same class as #3's conflict-key trap). Did NOT force a risky delegation (refusing the risky "obvious" move IS the quality gate). Shipped instead task #46 part 1 (clean, verified): the redundant full-HTML gzipSync-for-a-metric on the worker fetch hot path → async fire-and-forget (same metric, off the sync loop, less per-page CPU under the broad crawl). Verified via fresh /run + content_storage still written. Captured the trap as a memory so future cycles diff the ncdb fn's shape before delegating. Calibration: a diverse-area cycle (crawl-tooling for many cycles → tried ncdb-debt → landed on worker hygiene) that honored "validate the verdict yourself" — the check turned an obvious-looking delegation into a documented trap. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Twentieth cycle — operationalized + MEASURED the broad crawl, then a data-driven improvement. A sustained 12-host auto-discovery frontier-fill sustained ~34 pages/min across 9 hosts (369 pages/12min) vs ~6/min prior — ~5× aggregate gain, / held 200, running:0 after (no leak). Per-host yield surfaced nytimes under-delivering (84% lifetime / 445 errors) vs 98-100% for the rest, so made frontier-hosts.js YIELD-AWARE (done/(done+error+dead) from the SAME GROUP BY, gate --min-yield default 0.6, over-fetch to backfill). Verified: 0.6 keeps nytimes + shows yield; 0.9 swaps it for cnn/scmp. Calibration: measured-then-improved — let the live data (not a guess) pick the improvement; the yield signal came FREE from the existing query (no new query/server change) = right-sized. Honest reporting: flagged the ~5× is vs a noisy baseline; the clean number is 34 pages/min sustained. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Nineteenth cycle — raise the throughput ceiling via HOST BREADTH. Scouted (read-only): urls already holds 15,254 distinct hosts incl. ~20 proven-crawlable news publishers with huge frontiers — the ceiling was hydrate breadth, not missing publishers (the owner's "seed more" premise was half-right: they exist, just weren't scheduled). Shipped tools/crawl/frontier-hosts.js (data-driven top-proven-crawlable-host discovery) + frontier-fill auto-discovery. Verify: live broad 8-host batch, / held 200, breadth 6→8 hosts, new publishers polite (france24 ~6s, independent ~15s — each honoring its robots crawl-delay), real french headline landed. Calibration: measured the 2s GROUP BY with timed-probe BEFORE deciding placement → kept it off the server event loop (in the frontier-fill client process), honoring #39/#40. Right tool for the cost — not every DB read belongs on the server. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Eighteenth cycle — adaptive per-host limit (task #45), the real throughput lever given last cycle's finding that the gap is compliant politeness. Sized each host's leased URL COUNT to its measured crawl-delay (new ncdb read selectRecentHostFetchGapMedians) so slow hosts aren't over-leased (Guardian 15→2, no more 36-accumulation) — NEVER touching the delay, so politeness holds by construction. Verify: ncdb unit 2/2 (non-flaky), live self-correction PROVEN (Guardian 15→2 across two batches once it had history; apnews→9; bbc→15), server healthy, debt stays 258 (ncdb fn, no raw SQL). Calibration: chose the ncdb-fn path over an inline server query even though it needed a build — keeps DB logic in ncdb (mission) + the debt ratchet moving the right way; the safety property (only shrink count, never delay) is true by construction so no adversarial workflow needed — right-sized the verification. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Seventeenth cycle — task #44 ("true fix for whole-process crawl dead-time") DISMANTLED by scout-first diagnosis. A 4-agent adversarial workflow + my own read-only validation proved the Guardian ~33s gap is compliant per-host robots crawl-delay (regular gap, low CV), NOT dead-time (reconcile already off-thread since #39/#40; zero whole-process silence in current data) and NOT worker CPU. The May-2026 verdict was stale. Nothing to fix — shrinking the gap breaks compliance. Shipped the owner's Strategy-2 off-ramp instead: tools/crawl/host-health.js (per-host FAST/POLITE-THROTTLE/SLOW-IRREGULAR classification) + an anti-misdiagnosis memory [[crawl-gap-is-politeness-not-deadtime]]; filed the REAL throughput lever (#45 scheduling) + worker hygiene (#46). Calibration: the compounding loop's biggest win this cycle was NOT shipping the requested change — it was disproving its premise before building it, and validating the workflow verdict myself (bbc 3s / apnews 10s regular floors) rather than trusting it. Judgment over momentum. |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Sixteenth cycle — guarded EWMA stuck-host trigger (task #43): replaced the blind 4-min wait-cap trigger with a measured one (new dependency-injected stuckMonitor.js). A host aborts early only when its bytes/downloads freeze ≥40s AND it holds un-fetched rows AND an ACTIVE sibling is still advancing; all-frozen-together (whole-process idle) aborts nobody. Exposed bytesTotal/bytesPerSecEwma on the registry public job (observability substrate). Verify: 7/7 unit tests codifying the false-abort guard + edge cases; live across 3 batches — zero false-aborts, abort+reclaim intact (running:0), all probes green. Calibration/discipline: the DI design made the SAFETY property (never abort during orchestration-idle) unit-provable in isolation — chose testability over convenience. Also weathered a transient restart-electron port-race (httpOk=false → retry succeeded; [[electron-restart-gotcha]], not my code — confirmed via the forwarded child stderr showing no error). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Fifteenth cycle — abort-on-wait-cap (lingering-job leak fix) + a site-general autonomous-adaptation brainstorm (owner-asked). Product: waitHostJob signals the cap → registry.stop → 35s settle (ABOVE the 30s kill backstop — the adversarial workflow's load-bearing ordering catch) → reconcile un-fetched rows to pending (retryable) not failed; + per-request waitCapMs. Live-verified (15s-cap batch: 5 hosts aborted, returnedToPending=40, RUNNING:0 = leak gone). Scaffold: extended resilience-wiring probe + brainstorm roadmap doc. Calibration/humility: the judge-panel workflow's SYNTHESIS corrected MY gap diagnosis with hard evidence — the Guardian gaps are host-AGNOSTIC total silence (whole-process orchestration dead-time, the #39/#40 family), NOT the "large-page bandwidth-slice contention" I'd written; I corrected the doc + ledger. Same lesson as the crawl-rate bug: trust evidence over a plausible story. Filed #43 (EWMA-guarded trigger) + #44 (off-thread reconcile = the true fix). |
| 2026-07-21 | Claude Opus 4.8 (continuation) | Fourteenth cycle — ncdb debt delegation #3 (place_page_mappings upsert → ncdb, debt 261→258). **Owner RE-ENABLED adversarial workflows** ("use adversarial workflows and anything else that would help") — the 2026-07-20 pause is lifted ([[feedback-no-adversarial-reviews]] updated). The workflow immediately paid off: its SYNTHESIS caught a reachable DESTRUCTIVE reject-collision (silent un-verify of a confirmed hub) that each individual lens rated only "yes-with-caveat" — added a guard (new ncdb read getPlacePageMappingSlot) + 7/7 differential-e2e + 16/16 route jest. Calibration: the delegation is NOT semantics-preserving (it fixes a live 500-with-partial-write) — verified as semantics-IMPROVING via differential-e2e treating conflict paths as intended divergences. Lesson banked: adversarial *synthesis* > adversarial *finding* for destructive edge cases. Also verified the new run-multi under 6-host load (/ held http=200, no wedge) + crawled 85 real pages. |

## Purpose
Describe how this repository can host an AGI-style, tool-enabled workflow where agents iteratively sense the state of the system, plan work, act through tooling, and document outcomes.

## Scope of Automation
- **Domain**: Large-scale news ingestion, enrichment, and delivery via modular Node.js services under `/src` plus supporting scripts in `/tools` and `/scripts`.
- **Agents**: GitHub Copilot (GPT-5 Codex) variants, Kilo modes, and future specialized personas defined here.
- **Tooling Surface**: Custom CLI utilities (`js-scan`, `js-edit`, `md-scan`), npm scripts, and documentation scaffolds.

## Current Capabilities
- Rich documentation culture (`docs/` tree) capturing architecture, workflows, and tool specs.
- Tier-1 JavaScript tooling:
  - `node tools/dev/js-scan.js` for semantic code discovery.
  - `node tools/dev/js-edit.js` for guarded batch modifications.
- Minimal MCP surface: `node tools/mcp/docs-memory/server.js` streams SELF_MODEL, LESSONS, and session excerpts for agent memory priming.
- “Skill packs” (capability bundles) captured as docs-first artifacts under `docs/agi/skills/` and indexed in `docs/agi/SKILLS.md`.
- Established directives in `AGENTS.md` enforcing Plan → Implement → Verify loops.
- Data-oriented adapters and service layering (per AGENTS.md) enabling clean abstraction points for future automation.
- Canonical agent governance captured in `docs/agents/agent_policy.md` and companions (e.g., `docs_indexer_and_agents_refactorer.md`), which define research-first behavior and documentation upkeep.
- **Instruction Self-Modification Pipeline**: Agents can propose versioned changes to instruction files via MCP tools (`proposeInstructionChange`, `listInstructionProposals`, `reviewInstructionProposal`). Proposals are stored as JSON in `docs/agi/instruction-proposals/` and require explicit human review before application.
- **Session Retrospective Automation**: Automated extraction of candidate lessons from completed sessions via MCP tool (`runRetrospective`) and CLI (`tools/agi/session-retrospective.js`). Uses TF-IDF cosine similarity to deduplicate against existing `LESSONS.md` content.
- **Architect Loop Orchestrator**: Autonomous improvement pipeline (`tools/agi/architect-loop.js`) that chains harvest → scan → report → propose. Supports dual-engine scanning: text-based (fast) and AST-aware via `js-scan` with guard pattern detection.
- **Process Guardian**: Tracks spawned processes (`tools/agi/process-guardian.js`), detects orphans, kills them on cleanup, and logs kills as lessons. Prevents rogue processes from persisting after agent sessions.
- **OCI Operations Skill Pack**: Codified Oracle Cloud Infrastructure knowledge (`docs/agi/skills/oci-operations/SKILL.md`) covering compartment layout, instance shapes, deployment patterns, and fleet management tools.

## Limitations
- No unified AGI-focused documentation prior to this effort; knowledge scattered across numerous files.
- Static-analysis outputs are textual; no persisted knowledge graph or visualization pipeline yet.
- Agents cannot execute Python; analysis must rely on Node.js or PowerShell-safe commands.
- Tool chaining and long-lived plans still manual (Gap 4/Plans initiative pending completion).

## Interfaces & Dependencies
- **Source Tree**: `/src` modules constitute the operational surface; AGI agents must interact indirectly via tooling/documentation.
- **Data Stores**: Not directly manipulated here; all persistence flows through `/src/db` adapters.
- **CI/Test Harness**: `npm run test:by-path` and `npm run test:file` per repository rules; referenced for completeness though AGI Documentation Scout does not run tests.

## Desired Evolution
- Maintain a living knowledge graph of modules, services, and contracts derived from `js-scan` outputs.
- Formalize stateful workflows where plans persist between sessions (leveraging planned `--from-plan` capabilities in `js-edit`).
- Establish specialized agents (e.g., Static Analysis Scout, Knowledge Archivist) to own slices of the AGI roadmap.
- Add MCP-facing “skills registry” discovery so agents can list/search/get skill packs without relying on human recall.

## Usage Notes
- Treat this document as the “identity card” for AGI initiatives. Update it whenever capabilities, limitations, or core workflows shift.
- Cross-link any new tooling or agent spec additions so the self-model reflects the current operating picture.
- Before proposing new agents, review the canonical files under `docs/agents/` to ensure consistency with existing roles and owners.
