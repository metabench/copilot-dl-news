# AGI Research Backlog

| id | state | question | priority | status | owner | last_update | links |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RB-001 | partial | How can `js-scan` output be converted into a persistent knowledge graph (nodes: modules, edges: imports/calls)? | High | Partially delivered — `js-scan --build-index` exists. Remaining: a persisted graph consumer. | Agents | 2026-07-19 | TOOLS.md#proposed-tooling-extensions |
| RB-002 | done | What minimal schema enables `js-edit --from-plan` to store/resume multi-day refactors safely? | High | Delivered (`js-edit --from-plan/--from-token/--match-snapshot` verified in --help) | Agents | 2026-07-19 | WORKFLOWS.md#refactor--architecture-reshaping |
| RB-003 | open | Which documentation diffing strategy keeps `/docs/agi` aligned with legacy workflow docs? | Medium | Open — partially answered in practice by probe-based reconciliation sweeps | Agents | 2026-07-19 | RECONCILIATION_2026-07-19.md |
| RB-004 | done | How can agents quantify code hotspots (fan-in/out, churn) without external SaaS tools? | Medium | **Delivered** — both axes now covered locally: `js-scan --ripple-analysis/--call-graph` (coupling/fan-in-out) + `tools/dev/churn-scan.js` (2026-07-20: git-history churn — commits, lines, author spread, recency, composite score; `--since/--path/--ext/--top/--json`; pure `parseNumstatLog` core, 5/5 jest incl. rename/binary/CRLF/filter cases). Hotspot = high churn AND high fan-in; the tool's footer points to the js-scan half. Live: top src/*.js hotspot over 30d = unifiedApp/server.js (11 commits, matches the coordination-point role) | Agents | 2026-07-20 | LIBRARY_OVERVIEW.md |
| RB-005 | superseded | What cadence should journal entries follow to avoid gaps when no AGI tasks run for weeks? | Low | Superseded — per-turn LOOP_STATE lines + docs/sessions write-ups + reconciliation sweeps replace fixed cadence | Agents | 2026-07-19 | BOOT.md |
| RB-006 | done | How can `js-scan` relationship queries emit continuation tokens that `js-edit` can ingest without re-running locate steps? | High | Delivered (`--ai-mode --json` emits continuation tokens + jsEditHint payloads) | Agents | 2026-07-19 | tools/JS_SCAN_DEEP_DIVE.md#continuation-tokens-phase-2-plan |
| RB-007 | partial | How can a "Proactive Architect Agent" autonomously monitor `LESSONS.md` and propose refactors to align code with new patterns? | High | Partially realized — `.claude/skills/singularity/SKILL.md` (local) runs a bounded improvement cycle. Remaining: nightly automation. | Agents | 2026-07-19 | WORKFLOWS.md#proactive-refactoring |
| RB-008 | done | Can we implement "Instruction Compliance Tests" (meta-tests) to verify agents actually follow directives (e.g., did they run session-init)? | Medium | **Answered YES + first meta-test Delivered** (2026-07-27, cycle 128) — `tools/dev/checks/ritual-compliance.check.js`, probe `ritual-compliance`. Checks the close-the-cycle ritual completed (stanza committed at HEAD · record files clean · HEAD level with origin/main · snapshot covers newest stanza) + a GATED directive (crawler concurrency default ≤ 3). **Empirically grounded, not speculative:** `--history` audit found 77/84 stanzas landed ≥1 day after their ledger date, only 7 same-day, worst 6 days — the commit step was deferred for a week until the owner asked. Design rules learned: check the AGENT not the artifact; an unreadable gate must FAIL (not silently pass); state which directives are unverifiable rather than implying full coverage. **Completed 2026-07-27 (cycle 130)** — the per-turn/standing half now checks hooks + skills against `config/gated-surfaces.json`, both ~30 GB backups still existing, the 429 backoff escalation intact, and that every stanza records a `verification[]`. Three directives are UNVERIFIABLE by nature and are printed as such every run rather than dropped: prompt regeneration (no artifact), whether BOOT.md was read (unobservable), and agent-vs-crawler attribution of live-DB writes. Nothing actionable left. | Verification | 2026-07-27 | AGENTS.md#compliance-testing |
| RB-009 | partial | How can agents autonomously design UI dashboards from task output patterns — observe data shape → generate jsgui3 components → self-test in headless browser? | High | Largely delivered in practice (2026-07 crawl-status phase badges + per-sitemap panels, built agent-side and self-verified via the dev-bridge ui-screenshot loop). Remaining: the generalized observe→generate→self-test pipeline. | Agents | 2026-07-19 | SELF_MODEL.md#desired-evolution |
| RB-010 | done | What is the minimal integration required for crawl tasks to auto-open a progress dashboard and send desktop notifications on completion? | High | **Delivered** — crawl-status UI (port 3170) + desktop notifications on crawl completion (2026-07-20: `src/ui/electron/unifiedApp/crawlNotifications.js` watcher polls the jobs API from the Electron MAIN process — the only place OS toasts exist; announces witnessed running→terminal transitions once, 6/6 jest; live-proven via `[notify] shown:` lines in electron-app.log on real run-multi completions; `--notify-test` flag for manual toast checks) | Agents | 2026-07-20 | RECONCILIATION_2026-07-19.md |
| RB-011 | done | Probe-stamped knowledge: what format lets every durable claim carry its own re-verification command + expiry, so staleness is machine-detectable? Roll out across LESSONS/memory. | Medium | **Operationalized** (2026-07-20) — format settled (per-file Probes block + inline `**Probe (RB-011):**` stamps) AND now EXECUTABLE: `tools/dev/run-probes.js` + `tools/dev/probes.json` manifest run every registered probe at orient (4 live: frontier API, SSR/template-trap tripwire, churn tool, DB-probe incident guard), server-dependent ones skip-not-fail when :3170 is down; wired into BOOT's working-loop table. 7/7 jest on the pure skip/fail policy. Convention → executed check. **Closed 2026-07-27 (cycle 131)** — 14 probes registered; the newest, `progress-surface`, converts three prose-only claims from cycles 124/127/128.5 into runnable contracts. Broadening the manifest is now ROUTINE PRACTICE folded into closing a cycle, not standing research: leaving this row open would re-offer it as a candidate forever, which is exactly the noise the c129 state field removed. | Agents | 2026-07-20 | BOOT.md |
| RB-012 | done | DB-driven crawling: make the crawler fetch not-yet-downloaded URLs + stale hubs selected from the DB (urls⋈http_responses), per-type recency (hubs 1-day, UI-configurable), on-demand place-hub redownload — folding outlinks back as known urls. | High | **Delivered** — P1-P6 all shipped + live-verified (frontier reads, recency knob, hydration, DB-seeded fetching, place-hub redownload, concurrent multi-host runs, auto-hydration default-off) PLUS the quality layer the live runs demanded: news-host policy, dead-hub suppression (hubDead=54), low-value URL gate, redirect-aware reconciliation, terminal pruning, full crawl-status UI. Optional edge-graph/provenance deliberately skipped (no consumer). autoHydrate enable = owner toggle | Agents | 2026-07-20 | plans/2026-07-db-driven-crawling.md |
| RB-013 | done | Repo scope as an executable probe: manifest + checker so agent scope is machine-verified at orient instead of specified in drifting prose | High | **Delivered** (2026-07-27) — `config/repo-scope.json` (owner-confirmed vs agent-INFERRED entries, knownIssue=loud-WARN) + `tools/dev/check-repo-scope.js`, registered in the RB-011 probe manifest. Born from two real drift incidents: "EMPTY placeholder" stale months, and a .git-based survey skipping the one unversioned repo | Agents | 2026-07-27 | CAPABILITY_RESEARCH_2026-07-27.md |
| RB-014 | done | Next-turn prompt generation as tooling: assemble the recursive-prompt grammar (REFERENCE/CONSTRAINT/PROGRESS/THIS CYCLE/METHOD/GATED/ON COMPLETION) mechanically from the latest cycle stanza + doc §0 summaries | High | **Delivered** (2026-07-27) — `tools/agi/next-prompt.js`: PROGRESS/owed/decisions/METHOD/candidates all DERIVED (reuses buildStatus + stanzas' second_order, so prompt/page/SVG share one source); the ▶ selection stays CURATED and the output says so (`[CURATE:]`). 5/5 jest. Honest limits: rough slug humanization; option filter is a status-text heuristic | Agents | 2026-07-27 | CAPABILITY_RESEARCH_2026-07-27.md |
| RB-015 | blocked | Progress SVG workflow v2: stanza-schema probe, CI staleness byte-compare, per-repo lanes, optional /progress skill + PostToolUse hook (owner-installed) | Medium | v2 **Delivered** (2026-07-27, cycle 126) — `stanza-schema` probe (typed-field validation; the "2/78 unparseable" premise was FALSE — parser false-positives on documentation placeholders — but the probe's first run caught 2 REAL typed-field defects parse-only checking missed: cycle 58's string defect entry mis-counted post-ship, 73b's unsortable string id; records repaired) + `progress-svg-staleness` probe (byte-compare; a CI job can invoke the same script). v1 Delivered earlier same day (renderer, 5/5 jest, BOOT-wired). Per-repo lanes **Delivered** (cycle 127, first Workflow v3 cycle) via git-history snapshot (`tools/agi/repo-activity.js` → committed repo-activity.json → lanes in progress.svg; keeps the render a pure function of committed inputs so the staleness byte-compare survives). Remaining: /progress skill + hook is owner-gated (no hooks/skills without approval) | Agents | 2026-07-27 | CAPABILITY_RESEARCH_2026-07-27.md |
