# Follow Ups – Agent Files CLI Tooling

- Consider adding `--regex` search and/or `--search-headings` for agent files.
- Consider supporting reference-style markdown links (`[x][ref]` + `[ref]: path`).
- Consider optional `--json` passthrough parsing for the md-edit proxy (capture stdout instead of inheriting).

- _Add actionable follow-ups here._
