# Follow-Ups: Screenshot Tooling Control Centre

1. Add an only-runs-with-comments toggle if screenshot artifact volume becomes hard to scan.
2. Add a tablet viewport preset if tablet operator review becomes a regular requirement.
3. Consider an Electron-specific capture wrapper only for workflows where the desktop shell changes behavior compared with Chromium/Puppeteer.