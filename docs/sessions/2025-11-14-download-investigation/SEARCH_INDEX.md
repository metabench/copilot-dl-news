# Search Index

| Keyword | Notes |
| --- | --- |
| `basicArticleDiscovery` | Crawl sequence invoked by `crawl.js` when no args specified. |
| `queue-exhausted` | Exit reason reported by CLI summary in this run. |
| `total-prioritisation-filter` | Queue drop reason observed repeatedly in logs. |
| `config/priority-config.json` | Flag `features.totalPrioritisation` keeps the queue in country-only mode. |
| `countryHubTargetCount` | Planner limit (~150 hubs) that exhausts work once fulfilled; see 2025-11-16 intelligent-crawl diagnostics. |
