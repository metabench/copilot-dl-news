# Follow Ups – md-scan encoded emoji search

- Optional: add `--find-emojis` output translations for `--lang zh`/bilingual modes.
- Optional: document a tiny helper snippet for converting emoji to `b16:`/`b64:` using Unicode escapes (no literal emoji required).
