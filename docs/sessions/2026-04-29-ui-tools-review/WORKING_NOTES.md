# Working Notes: UI Tools Review

- 2026-04-29: Session opened for a jsgui3-first review of UI tools. Active long-term link: LT-001 Advanced Crawler + Advanced UI.
- 2026-04-29: Initial `md-scan` searches for `jsgui3 UI tools controls unified app` returned no direct hits, so the review will rely on the indexed UI guides and targeted code discovery.
- 2026-04-29: Read UI knowledge sources, SSR/isomorphic controls guide, UI architecture guide, docs index, and LT-001 plan/working notes. Key constraints: call/guard `compose()`, pass context to children, prefer shared controls, add local checks for HTML-emitting controls, keep controls extracted and documented.
- 2026-04-29: Inventory found 200+ UI control classes across shared controls, app-specific server controls, and `src/ui/server/shared/isomorphic`. The highest-impact integration failure reproduced immediately: `node src/ui/server/unifiedApp/checks/shell.check.js` failed because unified-shell modules `panelHelpers`, `activators`, and `SearchExplorerControl` were missing.
- 2026-04-29: Restored missing unified-shell integration as organized modules: new shared jsgui3 `SearchExplorerControl`, a local check script, `subApps/panelHelpers.js`, and centralized `unifiedApp/activators.js`. Wired `search_explorer` into the shared control index/manifest and catalog.
- 2026-04-29: Unified server check then exposed missing compatibility imports for Design Studio (`../utils/fileTree`, `../utils/svgRenderer`). Added thin re-export modules under `src/ui/server/utils/` that point at the canonical shared utilities.
- 2026-04-29: Added `src/ui/server/shared/utils/jsgui3Helpers.js` as a compatibility re-export for crawl observer and article viewer controls that already depend on the shared utility path.
- 2026-04-29: Unified startup then exposed missing crawl-status companion modules. Restored `crawl-status-client.js` and `crawl-status-styles.js` as extracted modules so `CrawlStatusPage` can render and wire the RemoteObservable/script markers expected by its check.
- 2026-04-29: Unified startup then exposed missing Domain Registry modules. Added a minimal `DomainRegistryStore` that reads a `domain_registry` table when present and falls back to distinct `urls.host` values, plus a small `domainRegistry` router for unified-shell embedding/API use.
- 2026-04-29: Restored missing Search Explorer support modules under `unifiedApp/lib`: date preset resolution and search freshness scoring. These back the API contract asserted by `unified.server.check.js`.
- 2026-04-29: `unified.server.check.js` then proved check mode had drifted away from its contract: the server booted but `/` returned 404. Added lightweight check-mode shell and `/api/apps/:appId/content` routes so the integrated server check exercises the same shell/content seam without opening production DB modules.