# Working Notes – Distributed crawling defaults

- 2026-01-07 — Session created via CLI. Add incremental notes here.
- Set distributed crawling to default on for place/topic hub guessing UI, API /place-hubs, and GuessPlaceHubsOperation (env override still supported).
- Tests not run.
