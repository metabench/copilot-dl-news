# Per-turn continuation prompt

Paste this (or just "go") at the start of each turn once the seed prompt has
been given in the session:

---

Continue the loop: read docs/sessions/2026-07-14-recursive-crawl-loop/LOOP_STATE.md,
verify the bridge, do the top "Now" item with evidence, update LOOP_STATE.md,
commit+push if a chunk completed, and end with status + "NEXT:".
