# Follow Ups – LT-001 Advanced Crawler + Advanced UI

- Restore a bootable remote crawler runtime path before UI implementation starts.
- Decide the first supported deployment shape for v5: single remote host first, fleet/federation later.
- Define the auth/access boundary before exposing a remote operator shell beyond private access.
- Specify the bundle job schema and archive formats before implementing export UI.
