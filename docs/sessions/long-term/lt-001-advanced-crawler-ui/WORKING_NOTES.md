## 2026-05-08 — Electron 10x1000 crawl operator run

- Tactical session `docs/sessions/2026-05-08-electron-crawl-10x1000/` opened to run 10 major news sites at 1000 pages each through the Electron-hosted unified app.
- Scope includes a jsgui3 Crawl Status batch launcher, Electron `--app crawl-status --allow-multi-jobs` startup ergonomics, a real batch start against the Electron server, and screenshot evidence under the session folder.

## 2026-04-29 — Eight-site 250-download crawl with UI evidence

- Completed a distributed crawl producing at least 250 new local 200-status downloads for eight major news sites: `bbc.com`, `aljazeera.com`, `theguardian.com`, `cbsnews.com`, `nbcnews.com`, `france24.com`, `euronews.com`, and `independent.co.uk`.
- Verified success against `data/news.db`, not only remote counters; final local counts ranged from 272 to 300 OK downloads per site.
- Captured jsgui3 unified UI evidence from `http://localhost:51012` with screenshot analysis `ok: true` under `screenshots/eight-site-250-download-crawl/`.
- Gap found: optional `remote-crawl-admin` router is missing from the unified app, so distributed crawl control still depends on CLI while the UI displays download/crawl evidence.

## 2026-04-29 — Electron 500-download crawl with persistent UI

- Completed a second distributed crawl with a higher local evidence bar: at least 500 new local 200-status downloads per site across the same eight successful major outlets.
- Verified final local counts ranged from 572 to 595 OK downloads per site from baseline `2026-04-29T21:02:54.481Z`.
- Used the Electron unified app in long-lived mode at `http://127.0.0.1:51014/?app=downloads`; Electron PID 22172 remained loaded after crawl completion and after screenshot capture.
- Captured Electron-backed UI evidence under `screenshots/electron-500-download-crawl/` with screenshot analysis `ok: true`.

## 2026-05-10 - Local crawl throughput slice

- Tactical session `docs/sessions/2026-05-10-local-crawl-throughput/` opened to make local crawl throughput visible again after improved internet connectivity: explicit docs/sec and MB/sec metrics, replayable metrics artifacts, and a compact `/crawl-status` strip before any larger UI redesign.

## 2026-05-12 - Twelve-site 100-page crawl

- Tactical session `docs/sessions/2026-05-12-twelve-site-100-page-crawl/` completed a remote crawler operation that locally verified at least 100 successful, non-empty pages for 14 news hosts, exceeding the requested 12-host target.
- Operational findings: older preconfigured remote domains can stop immediately with 0 new fetched due existing remote state; replacement/fresh domains and local `http_responses` evidence are more reliable for bounded data-collection goals.
- Tooling fix promoted during the run: `tools/crawl/crawl-remote.js` now imports `normalizeDomains` for start-result validation; focused crawl tests passed.
- Environment finding: Windows-hosted Node UI processes can hold `data/news.db-shm` and block WSL SQLite access; stop the specific process or coordinate UI/server ownership before sync verification.

## 2026-05-12 - Crawl CLI operator command

- Tactical session `docs/sessions/2026-05-12-crawl-cli-operator-command/` promoted the previous manual remote-crawl sequence into `node tools/crawl/crawl-remote.js collect`, covering preflight, start, sync, local verification, stop, and drain in one operator command.
- Human CLI output now uses colour and relevant emojis for setup, remote, DB, target, sync, prune, ledger, verification, and stop/drain states; `--json`, `--no-color`, and `--no-emoji` preserve automation/plain-output use cases.
- DB-boundary correction: local setup and successful-download host verification SQL moved into `news-crawler-db` `SqliteRemoteCrawlerAccess`; the crawler CLI calls exported DB APIs only.
- Security posture: host verification uses conservative host normalization, fixed prepared statements, escaped `LIKE` patterns, and parsed `since` markers to avoid wildcard broadening and accidental broad queries.

## 2026-04-29 — Download verification UI

- Added a unified-shell `Download Verify` screen that joins recent `http_responses` to `content_storage` and compression metadata so operators can inspect downloaded/saved/compressed proof in the UI.
- Added reusable jsgui3 `DownloadVerificationPanelControl` plus focused render/query checks.
- Verified the screen in browser and Electron: latest rows show successful DB persistence and current imported rows report `gzip` via `storage_type` while correctly marking compression level as unrecorded.

# Working Notes – LT-001 Advanced Crawler + Advanced UI

- 2026-03-08 — Long-term session materialized because the hub already referenced LT-001 as the active strategic outcome, but the directory did not exist in the worktree.
- 2026-03-08 — Tactical planning session `docs/sessions/2026-03-08-v5-remote-crawler-application-plan/` added a concrete v5 plan centered on: remote unified shell, crawl control, bundle jobs, and article library/reader.
- 2026-03-08 — Immediate strategy choice: v5 should productize existing assets first (`remote-crawler-v2`, unified shell, Data Explorer, Article Viewer), not begin with a fresh crawler-engine rewrite.
- 2026-03-08 — Added execution-grade plan directory `docs/plans/2026-03-v5-remote-crawler-application/` so implementation can be staged across workstreams instead of relying on a single concept document.
- 2026-03-09 — Strategic scope tightened again: always-on hosting, responsiveness under load, auth before exposure, restart-safe run/job state, bundle integrity/retry semantics, and host-protection guardrails are now documented as first-class v5 requirements.
- 2026-03-09 — Intelligent crawling via place/topic hub guessing is now part of LT-001's core outcome, using existing hub-analysis, pattern-learning, and UI-query assets as the planned base rather than treating hub guessing as separate tooling.
- 2026-03-10 — Implementation has started with tactical session `docs/sessions/2026-03-10-v5-runtime-bootstrap/`, focused on the first backend slice: explicit `src/v5/remote/` boundary plus a minimal bootable and testable API contract.
- 2026-03-10 — First tactical slice landed: `src/v5/remote/server.js`, `src/v5/remote/runtime.js`, and `tests/v5/remote-server.test.js`. This establishes the v5 backend namespace, minimal health/status/domain/crawl-control contract, and a placeholder hub-suggestions contract without forcing a broad `remote-crawler-v2` restoration in the same pass.
- 2026-04-29 — Tactical session `docs/sessions/2026-04-29-simple-distributed-crawl-readiness/` opened to verify the smallest easy distributed crawl path, Oracle Cloud readiness, config/profile boundaries, and terminology around "simple" meaning low-scope rather than local-only.
- 2026-04-29 — Tactical session `docs/sessions/2026-04-29-ui-tools-review/` opened to review and tighten jsgui3 UI tooling, shared controls, and operator-facing UI integration.
- 2026-04-29 — UI tools review completed a focused integration repair: added reusable `SearchExplorerControl`, restored unified-shell activators/helpers, restored Crawl Status and Domain Registry support modules, documented focused checks, and got the unified server integration check passing.
- 2026-04-29 — Tactical session `docs/sessions/2026-04-29-electron-jsgui3-crawl-display/` opened to prove the real crawl -> jsgui3 UI -> Electron/screenshot display loop and iterate on any quality gaps.
- 2026-04-29 — Electron+jsgui3 crawl-display validation completed: the simple distributed smoke crawl produced fresh BBC evidence, Downloads and Crawl Status routes render cleanly in the unified shell, Electron route smokes pass, and `npm run ui:crawl-display-screenshots` now captures repeatable visual artifacts plus quality metrics.
- 2026-04-29 — Churn control salvage completed: disposable widget/app/lab shells stayed removed, while reusable jsgui3 patterns were promoted into shared controls (`ActionButtonGroupControl`, `OptionPickerControl`, `ActivityLogControl`, `CrawlProgressPanelControl`) with focused render checks and catalog documentation.
- 2026-05-04 — Tactical session `docs/sessions/2026-05-04-five-site-cloud-crawl-ui/` completed a five-site cloud crawl UI slice: added compact `CloudCrawlPanelControl`, mounted `/?app=cloud-crawl`, forwarded `--max-concurrent` through the remote CLI, ran five parallel cloud target crawls to 25/25 pages, synced 25 responses into local `data/news.db`, and captured screenshot evidence with final `analysis.json ok=true`.
- 2026-05-08 — Tactical session `docs/sessions/2026-05-08-electron-crawl-10x1000/` promoted Cloud Crawl to the 10-site x 1000-page operator path: in-process Electron crawling was rejected after UI starvation, remote 10x1000 was relaunched, Electron displayed live progress, and a 5-second metadata sync path was validated with sub-second remote export/ingest rounds.
- 2026-05-26 — Tactical session `docs/sessions/2026-05-25-news-ecosystem-status/` completed the 16-step GraphAccess and website graph-analysis cycle. This gives LT-001 a typed DB-owned graph read model and an analysis-service layer for future crawler feedback, hub discovery, and operator-facing graph intelligence without adding derived graph storage.
- 2026-05-26 — Tactical session `docs/sessions/2026-05-26-crawler-graph-feedback-loop/` opened the next track: consume `WebsiteGraphAnalysisService` outputs in crawler planning through bounded, low-storage recommendations before wiring any live crawl behavior.
- 2026-05-26 — Crawler graph feedback is now usable as a read-only operator dry run for populated hosts via `graph-feedback --fast`, while preserving the one-way dependency direction (`copilot-dl-news` consumes `news-db-analysis`/`news-crawler-db`). The remaining full-mode work is upstream query optimization for site summary and orphan/dead-end analyses.
- 2026-05-26 — Full-mode graph feedback now completes on a large populated no-edge host (`www.bbc.com`) under a 30s guard after upstream GraphAccess no-edge fast paths and bounded crawl-priority feature reads. This keeps crawler intelligence read-only while making the full service summary available to future planner integration.
- 2026-05-26 — `graph-feedback --out <path>` now writes bounded JSON planning artifacts, giving future crawler planning a stable read-only input format without seeding or changing `collect`.
- 2026-05-26 — `graph-feedback --from-artifact <path>` now consumes saved graph-feedback artifacts as a file-only planning dry run, validating schema/hosts/limits and showing seed candidate consideration with all enqueue/remote-seed/collect-change actions explicitly false.
- 2026-05-26 — `run.js --explain --graph-feedback-artifact <path>` now attaches saved graph-feedback recommendations to the normal crawl plan as read-only explain data, validating planned hosts while still refusing any live enqueue, remote seed, or collect behavior change.
- 2026-05-26 — Non-JSON `run.js --explain` now adds a compact graph-feedback operator summary with host/candidate counts, top URLs/reasons, and the explicit no-action policy, while keeping JSON explain output machine-readable.
- 2026-05-26 — Unified launcher remote `--dry-run` now supports `--graph-feedback-artifact <path>` for file-only graph-feedback previews of planned remote invocations, extending crawler intelligence into remote planning without starting crawlers or changing `collect`.
- 2026-05-26 — Graph-feedback artifact previews now have stricter operator-safety validation across planning surfaces, including aggregate recommendation counts and URL-length bounds; future live seeding is documented as an explicit default-off design only.
- 2026-05-26 — `graph-feedback --from-artifact ... --recipe` now gives operators a file-only command recipe for generating, validating, and previewing graph-feedback artifacts across local and remote planning surfaces, including the exact-host matching caveat.
- 2026-05-26 — `graph-feedback --from-artifact ... --compare-hosts` now gives profile-based planning a file-only host spelling check before strict previews reject mismatched artifacts such as `bbc.com` vs `www.bbc.com`.
- 2026-05-26 — Graph-feedback artifact mode is now profile-aware: file-only profile host extraction, `--profile` validation/compare/recipe flows, and `--profile-summary` make named crawl profiles safer to preview without live seeding or `collect` changes.
- 2026-05-26 — Profile-aware graph feedback now has a file-only `--profile-preflight` report that combines profile hosts, optional artifact host matches, candidate counts, caveats, and next safe commands for operator planning.
- 2026-05-26 — Graph-feedback planning evidence can now be emitted as a bounded file-only operator report/Markdown cheat sheet across common or selected profiles, making profile host spelling, hostless/orchestrator/e2e caveats, and canonical dry-run commands durable without live seeding.
- 2026-05-26 — Graph-feedback artifact planning evidence now includes file-only size/freshness metadata and per-profile readiness labels, helping operators distinguish ready previews from missing-artifact, host-mismatch, stale, and hostless cases without changing live crawler behavior.
- 2026-05-27 — Graph-feedback operator reports now have opt-in compact output controls for profile count and command verbosity, keeping planning evidence usable for broader profile sets while preserving the read-only/no-live-seeding boundary.
- 2026-05-27 — Graph-feedback reports now expose aggregate readiness counts and an explicit text preflight summary, giving operators a clearer CLI path from artifact evidence to safe profile dry-runs while keeping live crawl behavior unchanged.
- 2026-05-27 — Graph-feedback profile workflow checklists and `docs/workflows/graph-feedback-artifact-planning.md` now turn the read-only artifact/report/preflight surfaces into a durable operator workflow, including stale-artifact warnings and exact-host dry-run guidance without live seeding.
- 2026-05-27 — Guarded live graph-feedback seeding is now explicitly queued as the next crawler-intelligence track, with the current read-only workflow closure preserving dry-run-first evidence, freshness-gate inputs, host/candidate caps, and rollback constraints for the implementation pass.
- 2026-05-27 — First guarded live graph-feedback seeding slice landed in the unified launcher behind explicit `--use-graph-feedback-seeds`, reusing the existing remote seed argument only after strict artifact freshness/host/candidate/body gates and mocked delegation tests. Remote health/recovery/deploy workflow documentation now frames the operational prerequisite for real seed use.
- 2026-05-27 — Guarded live graph-feedback seeding now has required live-mode dry-run preview fingerprints, compact redacted seed-attempt JSONL, direct `crawl-remote.js` flag rejection, and stronger remote deploy preflight coverage, moving crawler intelligence closer to an auditable small real-remote seed smoke.
- 2026-05-27 — The guarded graph-feedback seed path now has a dry-run-only real-remote approval checklist and post-seed verification evidence shape, so the next LT-001 crawler-intelligence proof can be a tightly capped human-approved smoke rather than an implicit live behavior change.
- 2026-05-27 — Post-seed proof is now a bounded URL-redacted evidence artifact, and deploy recovery has pure troubleshooting hints for failed health, busy servers, stale packages, and wrong remote targets. This keeps the future first real seed smoke auditable without broad live crawler changes.
- 2026-05-27 — The real-seed rehearsal path now has a file-only approval readiness object tying checklist, preview fingerprint, caps, no-action policy, and optional post-seed evidence shape together before any separate human approval request.
- 2026-05-27 — The unified launcher now writes the approval readiness artifact directly during dry-run packaging, and the approval checklist includes pre-seed health/status-build/errors/content/deploy-preflight commands so the first real seed smoke can be gated by remote usability evidence.
- 2026-05-27 — Remote deploy preflight target and recovery overrides are now available from the unified launcher, making wrong-status-host, wrong-remote-dir, wrong-service, and skip-check decisions testable in dry-run before live crawler work.
- 2026-05-27 — The graph-feedback approval readiness artifact now carries blocker summaries, required remote-usability commands, post-seed proof commands, rollback commands, and seed-attempt log path in one bounded no-contact file, improving the audit trail before any human-approved tiny real seed.
- 2026-05-27 — The no-contact graph-feedback approval package now has an explicit post-seed checklist artifact, so preview evidence, approval checklist, readiness summary, and post-seed proof plan can be generated before any human-approved remote seed.
- 2026-05-27 — The first real graph-feedback seed smoke is now explicitly blocked on a separate approval line; without it, agents rehearse only local dry-run artifacts and local rejection paths, preserving LT-001 crawler intelligence safety while awaiting human approval.
- 2026-05-27 — The first approved real graph-feedback seed smoke reached pre-seed remote proof, but no seed was sent because several proof commands timed out under the 30s guard. LT-001 live crawler intelligence now needs a faster/cleaner remote proof path before retrying the tiny seed.
- 2026-05-27 — Remote proof hardening fixed the local `crawl-remote.js` JSON command exit lifecycle and added a fast deploy `--preflight-only --json` proof mode, reducing the operational blocker before the next approved tiny seed retry.
- 2026-05-27 — The next approved graph-feedback seed retry proved the read-only remote commands now exit cleanly and refreshed the local deploy package, but still sent no seed because remote build metadata was missing and SSH public-key auth failed before deployment. LT-001 live crawler intelligence is now blocked on deploy authentication/current remote build evidence, not on graph-feedback seed validation.
- 2026-05-27 — A follow-up approved retry stopped at the non-destructive SSH readiness gate: no deploy key/agent was available and BatchMode SSH still failed. The tiny live seed remains blocked until deployment authentication is configured or another documented path makes the remote build current.
- 2026-05-27 — Another approved retry reconfirmed the same SSH readiness blocker before any deploy or seed commands were run. The remaining LT-001 blocker is operational access to update the remote build metadata, not graph-feedback planner logic.
- 2026-05-27 — Latest approved retry again stopped before deploy/seed at SSH readiness: no deploy SSH env vars, no agent, no default runtime private key, and BatchMode SSH to `ubuntu@141.144.193.218` failed with public-key denial. LT-001 remains blocked on operational SSH/deploy access before a first tiny live seed can be attempted.
- 2026-05-27 — Follow-up approved retry found the likely Windows SSH config/key path for the Oracle worker, but OpenSSH refused the candidate keys because their file permissions are too open. LT-001 still needs accepted SSH key access or another deploy route before remote build metadata can be updated and the tiny live seed can proceed.
- 2026-05-27 — Another approved retry confirmed the SSH blocker is unchanged: candidate Windows key paths remain visible but are ignored by OpenSSH because of permissive file modes. No deploy proof or tiny live seed was run.
- 2026-05-28 — Approved retry again stopped at SSH readiness. Candidate Windows key paths are still present but ignored by OpenSSH due permissive file modes; active constraints forbade local chmod/copy key handling, so no deploy proof or tiny live seed was run.
- 2026-05-28 — After explicit key-handling approval, the first tiny guarded graph-feedback live seed completed through the unified launcher: accepted WSL key path, current remote build `20260527035514`, one host `bbc.com`, 3 graph-feedback URLs, matching preview evidence, compact seed-attempt log, healthy post-seed status/errors/content proof, one sync round, and local recent-download confirmation. LT-001 now has an end-to-end live crawler-intelligence proof, but broader rollout is blocked by residual remote state: `bbc.com` retained 1273 pending discovered URLs and deploy preflight reports `blocked-busy`.
- 2026-05-28 — Live-seed closeout hardened the deployment path and kept the residual queue safe: bounded read-only evidence reconfirmed `bbc.com` stopped with 0 recent errors, 3 stored content records, and 1273 pending URLs; queue retention is the default until explicit maintenance approval. Deploy tooling now expands the default home-relative remote dir safely and warns/hints when Node 24 arm64 needs native build tooling for `better-sqlite3`.
- 2026-05-28 — Remote queue readiness is now a first-class read-only proof surface: `crawl-remote.js queue-summary` and `queue-checklist` classify retained pending queues, produce bounded evidence/checklists, and separate queue-maintenance / force-deploy approvals from graph-feedback live seed approval. This gives LT-001 a safer bridge from crawler-intelligence seeding to operator queue/deploy decisions.
- 2026-05-28 — Remote crawler operations now have a tested safety taxonomy for future LT-001 workflow/UI work: read-only proof, safe stop/stabilize, sync/local proof, destructive maintenance, deploy action, and live crawl behavior. This prevents operator surfaces from treating sync/drain/prune/deploy decisions as equivalent to harmless status checks.
- 2026-05-28 — Combined remote crawler readiness is now a file-only report surface for LT-001 operators: graph artifact freshness, retained queue state, deploy proof, preview fingerprint, and post-seed proof plan can be reviewed together without candidate URL dumps or remote mutation.
- 2026-05-28 — Queue maintenance choices now have a file-only decision artifact for LT-001 operators: retain/sync/stop/prune/drain/clear/force-deploy plans can cite readiness blockers, approval tokens, pending counts, rollback commands, and data-loss caveats before any mutating path is implemented or approved.
- 2026-05-28 — Queue maintenance decisions now validate evidence drift before any future approved action: host mismatch, pending-count mismatch, stale queue/readiness proof, and deploy implication drift are surfaced as blockers. A new file-only `sync-proof-readiness` artifact gives LT-001 a safe operator bridge from retained queue state to a later bounded sync/local DB proof command.
- 2026-05-28 — Queue maintenance now has a file-only dry-run execution plan for LT-001 operators: saved decision, sync-proof readiness, queue/readiness/deploy evidence, no-prune proof, local DB confirmation, rollback command, and approval-token presence can be validated together while actual stop/sync/prune/drain/clear/force-deploy execution remains disabled.
- 2026-05-28 — Second graph-feedback seed readiness now has a file-only LT-001 gate: saved graph artifact, queue summary, deploy proof, preview evidence, post-seed checklist, combined readiness, and optional maintenance execution plan are checked together, with retained pending queues and non-current deploy proof blocking another seed by default.
- 2026-05-28 — LT-001 now has a monitored small-crawl development loop: `tools/crawl/monitored-small-crawl.js` captures local DB baselines, recent host/window download evidence, and post-crawl verification reports through DB-owned download evidence APIs, while the Cloud Crawl dashboard status payload exposes `monitoredSmallCrawl` and recent download samples for operator-visible crawl volume.
- 2026-05-28 — The monitored small-crawl loop now has a verified tiny local execution path: `monitored-small-crawl local-smoke --execute` starts one capped local UI crawl, auto-stops, and verifies DB persistence. The first successful run added one `www.bbc.com` DB row and proved the CLI can crawl and persist under a bounded local harness without remote contact or queue maintenance.
- 2026-05-28 — Monitored small-crawl evidence is now fast and visible enough for repeat crawler-development loops: the DB-owned recent cloud-crawl evidence query was optimized for mixed timestamp formats, reports carry bounded query timings, and Cloud Crawl's Operator Health card shows recent-crawl readiness/count evidence alongside recent download samples.
- 2026-05-28 — Monitored small-crawl reports are now comparable across runs: `monitored-small-crawl compare` produces bounded stable pass/fail evidence from saved reports, including DB deltas, no-new-data regressions, command/profile identity, and DB proof timing. Cloud Crawl status also exposes `monitoredSmallCrawlSummary` so dashboard/operator views can show crawl-volume health and proof timing compactly.
- 2026-05-28 — The monitored loop found a real local-smoke reliability issue on the next run: the launcher started a local job but DB proof saw only a URL row, not response/content rows. The harness now exits nonzero when DB verification is blocked, preserving the evidence artifact for LT-001 diagnostics before broadening crawler runs.
- 2026-05-28 — Local watch proof semantics are now stricter for LT-001: `run.js --watch` returns a failing exit code on timeout/poll-error/missing-target outcomes, and comparison artifacts explicitly flag partial persistence such as URL-only deltas, started-without-fetch evidence, stale latest fetch times, and missing samples.
- 2026-05-28 — A post-fix local-smoke validation confirmed the stricter semantics: the same local persistence issue now exits nonzero and produces a comparison artifact with stable pass failure, while preserving bounded evidence for the next diagnosis pass.
- 2026-05-28 — The local-smoke blocker is now resolved at the proof layer: local watch uses DB-owned recent download evidence with a bounded upper timestamp and stops on `--watch-min-fetches` response/content proof. A capped BBC local smoke verified new DB persistence (`responses:+3`, `content:+1`) and exited cleanly without changing collector behavior or touching remote state.
- 2026-05-29 — Tactical session `docs/sessions/2026-05-29-crawler-reliability-recursive-plan/` opened to turn the current crawler evidence surfaces into a reliable small/medium crawl improvement loop. The session defines the crawl packet contract, scorecard direction, shared failure taxonomy, owner routing, and first recursive continuation prompt, while keeping live remote work and queue maintenance gated by explicit approval.
- 2026-05-29 — The crawler reliability loop now has a first no-contact packet/scorecard CLI for tiny, small, and medium local proofs plus bounded small/medium local profiles. A fresh tiny local crawl passed with DB proof; the first two-host small live proof exposed a partial-launch orchestration gap where Reuters launch timeout prevented watch/DB proof for an accepted BBC job. Next LT-001 crawler work should fix or explicitly package partial local launch evidence before live medium broadening.
