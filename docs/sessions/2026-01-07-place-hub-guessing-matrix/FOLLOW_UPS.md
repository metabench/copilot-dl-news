# Follow Ups – Place hub guessing matrix + distributed downloader

- Consider exposing a UI toggle for distributed mode + worker URL in the matrix chrome filter form.
- Add a small check script to exercise `/api/guess` with distributed settings once the worker is available.
