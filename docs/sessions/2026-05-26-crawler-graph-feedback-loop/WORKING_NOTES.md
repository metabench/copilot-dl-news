# Working Notes

- 2026-05-26: Started separate crawler feedback-loop track following the completed GraphAccess and `WebsiteGraphAnalysisService` work from `docs/sessions/2026-05-25-news-ecosystem-status/`.
- Read the requested source docs plus `docs/INDEX.md` and the active LT-001 notes. The relevant boundary is `copilot-dl-news` crawler planning -> `news-db-analysis` `WebsiteGraphAnalysisService` -> `news-crawler-db` `db.graph` `GraphAccess`.
- Existing `tools/crawl/crawl-remote.js` already has `graph-seeds` and collect-time graph seeding, but it uses an older DB helper (`listUnvisitedDiscoveredLinksForDomain`) rather than the new website graph analysis service. This track should not rewrite that path immediately.
- First implementation step selected: add a pure graph-feedback planner in `tools/crawl/lib/` that accepts `WebsiteGraphAnalysisService`-shaped outputs and returns bounded seed recommendations. It will not open databases, import ESM analysis code, call network endpoints, or alter live crawler behavior.
- Implemented `tools/crawl/lib/graph-feedback-planner.js` with `buildGraphFeedbackPlan()`, bounded per-host/sample limits, graph posture labels, seed recommendation merging/deduplication, diagnostic orphan/dead-end samples, and per-domain error capture.
- Added `tests/tools/crawl/graph-feedback-planner.test.js` covering domain normalization, posture classification, recommendation merging, bounded service calls, limit clamping, and missing-service-shape errors.
- Verification passed: `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js` with 1 suite / 6 tests passing. Expected environment noise: Jest reported the existing `@copilot-dl-news/remote-crawler` haste naming collision under `wip/legacy-distributed/remote-crawler-v1`, but the focused test run passed.
- Boundary check passed for new planner/test files: no `SELECT`, `db.query`, `db.execute`, `better-sqlite3`, `new Database`, or prepared-statement usage found. Focused diff whitespace check passed for tracked touched docs; new files were checked for trailing whitespace manually.
- Next narrow step should add a small live loader/dry-run surface that dynamically imports `news-db-analysis`, opens `news-crawler-db` read-only, instantiates `WebsiteGraphAnalysisService`, and prints the planner output as JSON without seeding remote crawlers.
- User clarified dependency direction: `copilot-dl-news` is allowed to bring sibling repos together and depend on them; sibling repos should not depend back on `copilot-dl-news`.
- Implemented the read-only dry-run surface: `tools/crawl/lib/graph-feedback-loader.js` dynamically imports `news-db-analysis` from the installed package or sibling `../news-db-analysis/dist/index.js`, opens `news-crawler-db` read-only via `src/db/openNewsCrawlerDb.js`, instantiates `WebsiteGraphAnalysisService`, calls the pure planner, and closes the adapter in `finally`.
- Added `tools/crawl/graph-feedback.js` as a JSON-only dry-run CLI and registered it as `graph-feedback` in `tools/crawl/index.js`; this does not seed remote crawlers and does not alter `collect` behavior.
- Added `tests/tools/crawl/graph-feedback-loader.test.js` for fallback import, read-only DB open options, close-on-success/error, CLI arg parsing, JSON output, and missing-domain validation.
- Updated `tools/crawl/AGENT.md` to list the new dry-run CLI and loader/planner library modules.
- Verification passed: `node --check tools/crawl/lib/graph-feedback-loader.js`, `node --check tools/crawl/graph-feedback.js`, `node --check tests/tools/crawl/graph-feedback-loader.test.js`.
- Verification passed: `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 14 tests passing. Expected environment noise remains the `NO_COLOR`/`FORCE_COLOR` warning from the test runner.
- Verification passed: `node tools/crawl/index.js list --json | rg 'graph-feedback'`, `node tools/crawl/graph-feedback.js --help`, and `timeout 30s node tools/crawl/index.js run graph-feedback --domains no-such-host.invalid --limit 1 --sample-limit 1 --generated-at 2026-05-26T00:00:00.000Z --json`.
- Boundary check passed: no `SELECT`, `db.query`, `db.execute`, `better-sqlite3`, `new Database`, or `prepare(` usage in the graph-feedback loader/planner/CLI/tests. `git diff --check` passed for touched tracked files.
- Operational finding: a populated-host smoke with `www.bbc.com` and `--limit 1 --sample-limit 1` did not complete within a 30s guard. The dry-run surface is wired, but the current all-method per-host analysis bundle is too heavy for a fast operator smoke on the large local DB. Next step should add a lightweight mode or targeted upstream GraphAccess/service bounds before using populated-host graph feedback operationally.
- Upstream edge case observed during the empty-host smoke: `WebsiteGraphAnalysisService`/GraphAccess returned non-zero candidate/orphan/dead-end dataset metadata for `no-such-host.invalid` while the site summary reported `pageCount: 0`. The local planner now filters URL-less diagnostic samples, but count semantics should be fixed in `news-db-analysis`/`news-crawler-db`, not patched with SQL here.
- Investigated the populated-host timeout at the sibling boundary. The first owning-repo bug was in `../news-crawler-db/src/db/sqlite/index.ts`: the generic SQLite `query()` path treated only `SELECT` as read SQL, while GraphAccess uses CTE reads starting with `WITH`. Added `isReadQuery()` so `WITH ... SELECT` goes through row-returning execution, plus a regression test in `../news-crawler-db/src/db/__tests__/graphAccess.test.ts`.
- Upstream verification passed: `cd ../news-crawler-db && npm run build`; `npx vitest run src/db/__tests__/graphAccess.test.ts` passed 16 tests, including the new CTE-read regression. Focused `git diff --check` passed for the touched upstream files.
- Re-ran one-method populated-host timings after the upstream CTE fix. On `www.bbc.com`, `summarizeSiteGraph`, `findOrphanPages`, and `findDeadEndPages` each exceeded the 30s guard; `findHubCandidates({limit:1})` completed in about 21.8s; `buildCrawlPriorityDataset({limit:1})` completed in about 21.6s. The CTE fix was necessary for correctness, but full mode still needs upstream query optimization.
- Implemented `graph-feedback --fast` in `copilot-dl-news`. Fast mode remains read-only and still goes through `WebsiteGraphAnalysisService`, but only calls `buildCrawlPriorityDataset()` and records skipped `site-summary`, `hub-candidates`, `orphan-pages`, and `dead-end-pages` analyses in JSON diagnostics. It does not seed remote crawlers and does not alter `collect`.
- Added focused tests for fast mode: the planner verifies only the crawl-priority method is called and CLI tests verify `--fast` / `--mode fast` are passed to the read-only builder. Focused verification passed: syntax checks for graph-feedback planner/CLI/tests; `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 17 tests passing.
- Populated-host smoke passed: `timeout 30s node tools/crawl/graph-feedback.js --domains www.bbc.com --fast --limit 1 --sample-limit 1 --generated-at 2026-05-26T00:00:00.000Z --json` completed in about 20.1s and returned one real `www.bbc.com` crawl-priority recommendation. Boundary scan still found no raw SQL or driver usage in the copilot graph-feedback files.
- Next step: optimize the owning upstream full-mode query path, starting with `news-crawler-db` GraphAccess `getSiteGraphSummary()` and orphan/dead-end page queries for populated hosts, then re-run `graph-feedback` full mode with the same 30s populated-host guard.
- Implemented the upstream full-mode optimization in `../news-crawler-db/src/db/sqlite/access/graph.ts`. `SqliteGraphAccess` now performs a cheap included-edge presence check and short-circuits no-edge hosts for site summary, orphan/dead-end discovery, and hub candidates. It also adds early page-scope limiting to crawl-priority features when `includeFetched: true`, which is the graph-feedback full-mode default.
- Added a no-edge populated-host regression fixture in `../news-crawler-db/src/db/__tests__/graphAccess.test.ts`: URL rows exist, one page is fetched, no link rows exist, and summary/discovery/hub/priority outputs stay bounded and semantically correct.
- Upstream verification passed: `cd ../news-crawler-db && npm run build`; `npx vitest run src/db/__tests__/graphAccess.test.ts` passed 17 tests. Focused upstream `git diff --check` passed for the touched files.
- Full-mode populated-host smoke passed: `timeout 30s node tools/crawl/graph-feedback.js --domains www.bbc.com --limit 1 --sample-limit 1 --generated-at 2026-05-26T00:00:00.000Z --json` completed in about 17.9s. Output included a real full summary (`pageCount: 139594`, `fetchedPageCount: 32375`, zero graph edges), one crawl-priority recommendation, and bounded orphan/dead-end samples.
- Copilot verification passed after the upstream build: `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 17 tests passing. Boundary scan still found no raw SQL or driver usage in the copilot graph-feedback files.
- Repository note: `news-crawler-db` currently reports `src/db/sqlite/access/graph.ts` and `src/db/__tests__/graphAccess.test.ts` as untracked even though they are part of this GraphAccess workstream. I edited those files in place and did not revert or restructure unrelated work.
- Next step: expose graph-feedback recommendations to crawler planning as a read-only planning/dry-run input or bounded explicit artifact, still without seeding remote crawlers or altering `collect`.
- Implemented the bounded artifact surface in `tools/crawl/graph-feedback.js`: `--out <path>` now writes the same JSON dry-run plan that stdout prints. This makes graph-feedback recommendations reusable by future crawler planning while keeping the current command read-only and bounded by `--limit` / `--sample-limit`.
- Added focused tests in `tests/tools/crawl/graph-feedback-loader.test.js` for `--out` parsing, artifact writing without suppressing stdout, parent-directory creation, UTF-8 writes, and blank path rejection.
- Updated `tools/crawl/AGENT.md` to document `--out tmp/graph-feedback-plan.json` as the safe planning-artifact path.
- Verification passed: `node --check tools/crawl/graph-feedback.js`; `node --check tests/tools/crawl/graph-feedback-loader.test.js`; `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 20 tests passing. Boundary scan still found no raw SQL or driver usage in the copilot graph-feedback files.
- Full-mode artifact smoke passed: `timeout 30s node tools/crawl/graph-feedback.js --domains www.bbc.com --limit 1 --sample-limit 1 --generated-at 2026-05-26T00:00:00.000Z --out tmp/graph-feedback-bbc-full-smoke.json --json` completed in about 19.0s. The artifact exists, is about 2044 bytes, has `mode: "full"`, `domainCount: 1`, `recommendationCount: 1`, and includes bounded recommendations for `www.bbc.com`.
- Next step: add a read-only artifact consumer to an existing planning/dry-run surface so operators can see how saved graph-feedback seed candidates would be considered, without enqueueing or seeding remote crawlers.
- Implemented the read-only artifact consumer in `tools/crawl/graph-feedback.js`: `--from-artifact <path>` reads a saved graph-feedback artifact, validates `schemaVersion`, artifact/requested hosts, limits, recommendation counts, and diagnostic sample bounds, then prints planning-only candidate consideration JSON.
- Artifact-consumer mode is file-only. It runs before the live graph builder, so it does not open `data/news.db`, import `news-db-analysis`, instantiate `WebsiteGraphAnalysisService`, call remote crawlers, enqueue URLs, or alter `collect`.
- Added focused tests in `tests/tools/crawl/graph-feedback-loader.test.js` for artifact parsing, no-live-builder execution, schema validation, requested-host validation, per-host limit validation, host filtering, and explicit non-action flags on seed candidates.
- Verification passed: `node --check tools/crawl/graph-feedback.js`; `node --check tests/tools/crawl/graph-feedback-loader.test.js`; `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 25 tests passing. Boundary scan still found no raw SQL or driver usage in the graph-feedback files/tests.
- Artifact-consumer smoke passed: `timeout 30s node tools/crawl/graph-feedback.js --from-artifact tmp/graph-feedback-bbc-full-smoke.json --domains www.bbc.com --json` returned one `www.bbc.com` candidate with `wouldEnqueue:false`, `wouldSeedRemote:false`, and `wouldChangeCollect:false`.
- Next step: add a read-only `--graph-feedback-artifact <path>` explanation option to an existing crawl planning command such as `tools/crawl/run.js --explain`, reusing the artifact validator/consumer without live seeding.
- Implemented `tools/crawl/run.js --explain --graph-feedback-artifact <path>`. It reads the saved artifact as a file-only input, validates the artifact against the hosts already planned by `run.js`, and adds a `graphFeedback` block to the resolved plan. The flag is rejected outside `--explain`.
- The `run.js` integration reuses `buildArtifactPlanningDryRunFromPlan()` from `tools/crawl/graph-feedback.js`, so schema/host/limit validation and non-action candidate output stay consistent with the standalone artifact consumer.
- Added focused tests in `tests/tools/crawl/run.test.js` for flag parsing, explain output attachment, planned-host validation failure, and rejecting the artifact flag before live launch.
- Verification passed: `node --check tools/crawl/run.js`; `node --check tests/tools/crawl/run.test.js`; `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/run.test.js` with 3 suites / 82 tests passing. Boundary scan still found no raw SQL or driver usage in the touched graph-feedback/run files.
- Dispatcher verification also passed: `npm run test:by-path -- tests/tools/crawl/run.test.js tests/tools/crawl/crawl-batch.test.js tests/tools/crawl/crawl-backend.test.js` with 3 suites / 78 tests passing.
- Explain smoke passed: `timeout 30s node tools/crawl/run.js --explain --json --graph-feedback-artifact tmp/graph-feedback-bbc-full-smoke.json www.bbc.com` returned a normal local batch plan plus one `graphFeedback` candidate with `wouldEnqueue:false`, `wouldSeedRemote:false`, and `wouldChangeCollect:false`.
- Next step: either add compact non-JSON operator summary text for the graph-feedback explain block, or add the same read-only artifact attachment to a more specific remote dry-run surface. Do not wire candidates into live launch yet.
- Implemented the compact non-JSON operator summary in `tools/crawl/run.js --explain --graph-feedback-artifact <path>`. Human output now appends planned hosts, total/host candidate counts, top candidate URLs/reasons, and the explicit no-enqueue/no-remote-seed/no-collect-change policy after the resolved plan JSON.
- JSON output stays machine-readable and unchanged except for the existing `graphFeedback` field; `--json` does not print the human summary text.
- Added focused tests in `tests/tools/crawl/run.test.js` for non-JSON summary content and JSON-output stability.
- Verification passed: `node --check tools/crawl/run.js`; `node --check tests/tools/crawl/run.test.js`; `npm run test:by-path -- tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/run.test.js` with 3 suites / 84 tests passing. Boundary scan still found no raw SQL or driver usage in the touched graph-feedback/run files.
- Dispatcher verification also passed: `npm run test:by-path -- tests/tools/crawl/run.test.js tests/tools/crawl/crawl-batch.test.js tests/tools/crawl/crawl-backend.test.js` with 3 suites / 80 tests passing.
- Non-JSON explain smoke passed: `timeout 30s node tools/crawl/run.js --explain --graph-feedback-artifact tmp/graph-feedback-bbc-full-smoke.json www.bbc.com` printed the compact graph feedback summary with one `www.bbc.com` candidate and the explicit non-action policy.
- Next step: add the same read-only artifact attachment to a more specific remote dry-run/planning surface, or write the design note for a future explicit live seeding flag without implementing live seeding yet.
- Inspected `tools/crawl/crawl-remote.js` and `tools/crawl/index.js` for safe remote planning surfaces. `crawl-remote.js` does not have a global dry-run parser that can validate a graph-feedback artifact without touching remote endpoints, but `tools/crawl/index.js --dry-run` resolves remote tool/profile invocations and prints the command without running it.
- Implemented the safe remote planning attachment in `tools/crawl/index.js --dry-run --graph-feedback-artifact <path>` for remote invocations. It extracts `--domain`/`--domains` hosts from the planned remote args, validates them against the saved artifact, and prints the same compact read-only summary used by `run.js --explain`.
- Added shared file-only helper `tools/crawl/lib/graph-feedback-artifact-explain.js` so `run.js` and `index.js` reuse the same artifact validation and summary renderer. Artifact-consumer mode still does not open the DB, import `news-db-analysis`/`news-crawler-db`, call remote crawlers, enqueue URLs, or change `collect`.
- Added focused tests in `tests/tools/crawl-index.test.js` for host extraction, remote dry-run graph-feedback explanation, human summary output, rejecting the artifact flag outside `--dry-run`, and rejecting non-remote dry-run targets.
- Verification passed: `node --check tools/crawl/index.js`; `node --check tools/crawl/run.js`; `node --check tools/crawl/lib/graph-feedback-artifact-explain.js`; `node --check tests/tools/crawl-index.test.js`; `node --check tests/tools/crawl/run.test.js`.
- Focused graph-feedback/run verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js` with 4 suites / 106 tests passing. Existing expected output in the test run included the silent child timeout fixture log and the `NO_COLOR`/`FORCE_COLOR` warning.
- Boundary scan passed for touched graph-feedback/planning files: no `SELECT`, `db.query`, `db.execute`, `better-sqlite3`, `new Database`, or `prepare(` usage.
- Unified launcher dry-run smoke passed: `timeout 30s node tools/crawl/index.js remote bounded --domains www.bbc.com --dry-run --graph-feedback-artifact tmp/graph-feedback-bbc-full-smoke.json` printed the planned remote command, one `www.bbc.com` candidate, and the explicit no-enqueue/no-remote-seed/no-collect-change policy.
- Next continuation pass should be larger but still bounded: audit remaining dry-run/profile surfaces, add host-mismatch and schema/limit negative examples, document a future explicit live seed flag without implementing it, refresh docs/examples, and return another recursive continuation prompt.
- Audited the remaining planning/dry-run surfaces requested for this pass. Direct unified-launcher `remote ... --dry-run`, remote profile `--dry-run`, `run.js --remote --explain`, and `graph-feedback --from-artifact` all use the same saved-artifact validation path or graph-feedback artifact consumer; `crawl-remote.js` still has no safe global dry-run hook and remains untouched.
- Hardened artifact validation in `tools/crawl/graph-feedback.js`: it now rejects aggregate `recommendationCount` mismatches and recommendation URLs longer than 4096 characters, in addition to the existing `schemaVersion`, host list, per-host limit, hard limit, and diagnostic sample bound checks.
- Hardened flag parsing in `tools/crawl/index.js` and `tools/crawl/run.js`: `--graph-feedback-artifact` now requires a non-empty path and also supports `--graph-feedback-artifact=<path>`.
- Moved unified-launcher dry-run artifact validation ahead of command printing. Invalid artifacts now fail without partial dry-run output.
- Added focused negative-path and surface-coverage tests: remote profile dry-run attachment, `run.js --remote --explain` attachment, host mismatch, missing remote hosts, malformed schema, excessive hard limits, aggregate recommendation mismatch, sample bound overflow, URL-length overflow, non-remote dry-run rejection, and live-path flag rejection.
- Added design-only note `FUTURE_LIVE_SEEDING_DESIGN.md` for a possible future `--use-graph-feedback-seeds` path. It documents default-off behavior, dry-run-first safety gates, host/candidate/body caps, rollback/disable paths, and verification commands. No live seeding was implemented.
- Verification passed: syntax checks for `tools/crawl/index.js`, `tools/crawl/run.js`, `tools/crawl/graph-feedback.js`, `tools/crawl/lib/graph-feedback-artifact-explain.js`, `tests/tools/crawl-index.test.js`, `tests/tools/crawl/run.test.js`, `tests/tools/crawl/graph-feedback-loader.test.js`, and `tests/tools/crawl/graph-feedback-planner.test.js`.
- Focused verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js` with 4 suites / 118 tests passing. Expected existing output included the silent-child timeout fixture and the `NO_COLOR`/`FORCE_COLOR` warning.
- Boundary scan passed again for touched graph-feedback/planning files: no raw SQL or SQLite driver usage outside the owning DB repo.
- Artifact smokes passed: `graph-feedback --from-artifact tmp/graph-feedback-bbc-full-smoke.json --domains www.bbc.com --json`, `index.js remote bounded --domains www.bbc.com --dry-run --graph-feedback-artifact tmp/graph-feedback-bbc-full-smoke.json`, and `run.js --remote --explain --json --graph-feedback-artifact tmp/graph-feedback-bbc-full-smoke.json www.bbc.com`. A profile dry-run against `simple-distributed-smoke` correctly rejected the same artifact because the profile plans `bbc.com` while the artifact host is `www.bbc.com`.
- Final whitespace/diff checks passed for touched tracked files; explicit trailing-whitespace scan passed for the untracked session and graph-feedback files in this workstream.
- Next continuation pass should improve operator ergonomics around the safe artifact workflow and possibly factor repeated test fixtures, while still avoiding any live seeding or `collect` changes.
- Added `graph-feedback --from-artifact <path> --domains <host> --recipe` as a file-only operator recipe mode. It validates the saved artifact and prints JSON commands for the safe workflow: generate bounded artifact, validate artifact, preview `run.js --explain`, and preview unified-launcher remote `--dry-run`.
- Recipe output includes the exact host matching caveat: `www.bbc.com` artifacts do not validate `bbc.com` plans, and vice versa. This is intentionally strict because the planner validates the concrete hosts it will use.
- Recipe mode stays read-only: it does not open `data/news.db`, import sibling repos, enqueue URLs, seed remote crawlers, or change `collect`.
- Added `tests/tools/crawl/helpers/graph-feedback-fixtures.js` so graph-feedback artifact test data is shared across `tests/tools/crawl-index.test.js`, `tests/tools/crawl/run.test.js`, and `tests/tools/crawl/graph-feedback-loader.test.js`.
- Added focused tests for recipe parsing/output, multi-host command rendering with quoted artifact paths, rejecting `--recipe` with `--out`, and verifying host-mismatch dry-run failures do not print a planned command first.
- Verification passed before docs update: syntax checks for `tools/crawl/graph-feedback.js`, `tools/crawl/index.js`, `tools/crawl/run.js`, `tests/tools/crawl/helpers/graph-feedback-fixtures.js`, `tests/tools/crawl-index.test.js`, `tests/tools/crawl/run.test.js`, and `tests/tools/crawl/graph-feedback-loader.test.js`.
- Focused verification passed before docs update: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js` with 4 suites / 121 tests passing. Expected existing output included the silent-child timeout fixture and the `NO_COLOR`/`FORCE_COLOR` warning.
- Final verification passed after docs update: syntax checks for touched JS/test files; raw SQL/driver boundary scan over touched graph-feedback/planning files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js` with 4 suites / 121 tests passing.
- Bounded recipe smoke passed: `timeout 30s node tools/crawl/graph-feedback.js --from-artifact tmp/graph-feedback-bbc-full-smoke.json --domains www.bbc.com --recipe --pretty` printed the four-command safe workflow with no enqueue/remote-seed/collect-change actions.
- Final `git diff --check` and trailing-whitespace scans passed for touched tracked and untracked files.
- Next continuation pass can add a file-only planned-host/artifact-host comparison helper or profile-aware recipe preview if it remains strictly read-only.
- Added `graph-feedback --from-artifact <path> --domains <hosts> --compare-hosts` as a file-only exact host comparison mode for profile planning.
- The compare mode validates the artifact itself, then reports artifact hosts, requested/planned hosts, matched hosts, missing hosts, extra artifact hosts, per-host recommendation counts, and the exact-host caveat without opening the DB or requiring requested hosts to exist in the artifact.
- Updated `graph-feedback --recipe` to include a `compare-hosts` command before strict validation. This gives operators a non-failing way to see `bbc.com` / `www.bbc.com` mismatches before `run.js --explain` or `index.js --dry-run` rejects the artifact.
- Added focused graph-feedback tests for compare-mode parsing/output, malformed artifact rejection, non-action policy, recipe command ordering, and ambiguous flag combinations. Added a profile dry-run mismatch regression proving `simple-distributed-smoke` fails before printing a planned command when a `www.bbc.com` artifact is supplied for its `bbc.com` plan.
- Verification passed: syntax checks for touched JS/test files; raw SQL/driver boundary scan over graph-feedback/planning files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js` with 4 suites / 125 tests passing.
- Bounded artifact smokes passed with `tmp/graph-feedback-bbc-full-smoke.json`: `--compare-hosts` showed `www.bbc.com` matched and `bbc.com` missing without failing, and `--recipe --pretty` printed the five-step workflow including `compare-hosts`.
- Updated `tools/crawl/AGENT.md` and this session plan with profile-host comparison guidance. `simple-distributed-smoke` currently plans `bbc.com`, so a `www.bbc.com` artifact is expected to fail strict profile validation unless regenerated with `bbc.com`.
- Added `tools/crawl/lib/profile-hosts.js` as a file-only profile host extraction helper. It reads profile JSON directly, extracts safe static host fields (`options.domains`, `options.domain`, root host/domain fields, and static `args` host flags), and reports a caveat for profiles with no static hosts.
- Added `graph-feedback --from-artifact <path> --profile <name>` support for strict validation, `--compare-hosts`, and `--recipe`. The implementation validates profile hosts and explicit `--domains` agree before reading artifacts, so mismatched operators get a bounded error without partial output.
- Added `graph-feedback --profile-summary` for compact file-only compatibility summaries of common remote profiles or explicitly supplied profile names. Artifact/profile consumer modes still do not open DBs, import sibling repos, call crawlers, seed remote crawlers, enqueue URLs, or change `collect`.
- Added focused tests in `tests/tools/crawl/profile-hosts.test.js` and expanded `tests/tools/crawl/graph-feedback-loader.test.js` for profile extraction, malformed/missing/hostless profiles, profile compare, profile recipe commands, profile/domain mismatch handling, and profile summary output.
- Initial focused verification passed: `npm run test:by-path -- tests/tools/crawl/profile-hosts.test.js tests/tools/crawl/graph-feedback-loader.test.js` with 2 suites / 41 tests passing.
- Final focused verification passed: syntax checks for `tools/crawl/graph-feedback.js`, `tools/crawl/lib/profile-hosts.js`, and touched crawl tests; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 139 tests passing.
- Boundary scan still found no raw SQL or SQLite driver usage in graph-feedback/planning/profile files.
- Profile/artifact smokes passed: existing `tmp/graph-feedback-bbc-full-smoke.json` compared against `simple-distributed-smoke` and correctly reported `www.bbc.com` as an extra artifact host with `bbc.com` missing; a bounded profile recipe smoke with a temporary `bbc.com` artifact printed the five-step profile workflow and all non-action policy flags.
- Added `graph-feedback --profile-preflight --profile <name> [--from-artifact <path>]` as a file-only operator report mode. It summarizes profile hosts, artifact hosts, host matches/misses, candidate counts, caveats, and recommended next safe commands.
- `--profile-preflight --out <path>` writes compact JSON reports only; it does not include candidate dumps.
- Expanded default profile compatibility coverage to include common remote profiles, `news-10x1000` orchestrator, `news-10x1000-15m-e2e`, sync/status profiles, and `local-news-10x1000`. Hostless profiles report caveats; orchestrator/e2e/static profiles report exact hosts with runtime-path caveats.
- Added focused tests for matching artifact/profile preflight, `www.bbc.com` vs `bbc.com` mismatch, hostless profile caveats, orchestrator/e2e/local fallback caveats, bounded report writing, and no artifact-read on profile/domain mismatch.
- Initial focused verification passed: syntax checks for touched graph-feedback/profile files; `npm run test:by-path -- tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/profile-hosts.test.js` with 2 suites / 49 tests passing.
- Final verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 147 tests passing.
- Boundary scan still found no raw SQL or SQLite driver usage in graph-feedback/planning/profile files. Profile compare and profile preflight smokes both passed with the existing `tmp/graph-feedback-bbc-full-smoke.json`, and a bounded matching preflight/report smoke wrote a 3048-byte JSON report.
- Final `git diff --check` and trailing-whitespace scans passed for touched tracked and untracked files in this workstream.
- Added `graph-feedback --operator-report` as a file-only operator cheat-sheet/report mode. It can target repeated `--profile <name>` values or the default common profile set with `--all-common-profiles`.
- Operator reports reuse the profile-summary/preflight data path and can include `--from-artifact <path>` for exact host-match and candidate-count evidence. JSON is the default output; `--format markdown --out <path>` writes a compact Markdown cheat sheet.
- Report mode remains artifact/profile file-only: it reads profile JSON and an optional artifact only, does not open DBs, does not import sibling repos, does not invoke crawlers, does not enqueue URLs, does not seed remote crawlers, and does not change `collect`.
- Documented that `tools/crawl/index.js <profile> --dry-run --graph-feedback-artifact <path>` is the canonical profile preview surface. `run.js --explain` remains the direct URL/host dispatcher preview and should not grow named-profile host extraction unless a later need is proven.
- Added focused tests for common-profile operator reports, bounded output shape, hostless/orchestrator/e2e caveats, Markdown report writing, no live graph builder calls, missing profile/output/artifact option errors, invalid report format, and report text writing.
- Verification passed: syntax checks for touched JS/test files; raw SQL/driver boundary scan over graph-feedback/planning/profile files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 152 tests passing.
- Bounded generated-report smoke passed: `node tools/crawl/graph-feedback.js --operator-report --all-common-profiles --generated-at 2026-05-26T00:00:00.000Z --pretty` wrote a 19,966-byte JSON report with 11 profiles and all action-policy flags false.
- Bounded profile preview smoke passed with a matching temporary `bbc.com` artifact: `node tools/crawl/graph-feedback.js --operator-report --profile simple-distributed-smoke --from-artifact <tmp> --format markdown --out <tmp>` wrote a 1,800-byte Markdown report, and `timeout 30s node tools/crawl/index.js simple-distributed-smoke --dry-run --graph-feedback-artifact <tmp>` printed the read-only graph-feedback summary without starting crawlers.
- Final `git diff --check` for tracked touched files and trailing-whitespace scan for tracked/untracked touched files passed.
- Added file-only artifact evidence for graph-feedback artifact readers: byte size, 256 KB cap, `generatedAt` parse validity, reference timestamp, age seconds, stale threshold, and warnings for missing/invalid/future/stale timestamps.
- Oversized artifacts are now rejected before JSON parsing in both graph-feedback artifact modes and the shared run/index dry-run artifact reader. This is a hard safety bound only; stale artifacts remain warnings.
- Threaded artifact evidence into `graph-feedback --from-artifact ... --compare-hosts`, `--profile-preflight`, and `--operator-report`. `run.js`/`index.js` JSON output remains unchanged except for rejecting clearly oversized artifacts.
- Added per-profile readiness labels in operator reports and profile preflights: `ready-for-preview`, `needs-artifact`, `host-mismatch`, and `hostless-caveat`.
- Added focused tests for valid age evidence, missing/invalid/stale timestamp warnings, oversized artifact rejection, readiness labels for matching/missing/mismatch/hostless profiles, and continued proof that artifact/report modes do not call the live graph builder.
- Verification passed: syntax checks for touched JS/test files; raw SQL/driver boundary scan over graph-feedback/planning/profile files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 155 tests passing.
- Bounded operator-report smoke passed with a matching temp `bbc.com` artifact: report size 4,566 bytes, readiness `ready-for-preview`, artifact byte evidence 517 bytes, and age evidence 43,200 seconds.
- Bounded profile dry-run preview smoke passed with the same temp artifact: `timeout 30s node tools/crawl/index.js simple-distributed-smoke --dry-run --graph-feedback-artifact <tmp>` printed the read-only graph-feedback summary and did not start crawlers.
- Final `git diff --check` for tracked touched files and trailing-whitespace scan for tracked/untracked touched files passed.
- Added opt-in operator report compaction controls: `--report-max-profiles <n>` and `--report-commands full|minimal|none`. Defaults remain unchanged; compact modes only affect `--operator-report`.
- `--report-commands minimal` keeps one readiness-specific next command per profile (`preview-profile-planning`, `generate-bounded-artifact`, `compare-profile-hosts`, or `profile-summary`); `none` omits commands entirely and Markdown says they were omitted by report settings.
- Markdown operator reports now show profile-selection truncation evidence, command-detail mode, and a readiness-label legend so stale/host mismatch/hostless evidence is easier to scan without adding candidate URL dumps.
- Added focused tests for compaction controls, invalid compaction flags, bounded Markdown shape, command omission, and continued no-live-builder behavior. Initial focused verification passed: `npm run test:by-path -- tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/profile-hosts.test.js` with 2 suites / 59 tests passing.
- Updated `FUTURE_LIVE_SEEDING_DESIGN.md` to point at existing `generatedAt` age evidence and stale warnings as the future freshness gate input. No live seeding was implemented.
- Final focused verification passed: syntax checks for touched JS/test files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 157 tests passing.
- Raw SQL/driver boundary scan over graph-feedback/planning/profile files returned no matches. Bounded compact operator-report smoke wrote a 2,125-byte Markdown report from a 523-byte matching `bbc.com` artifact.
- Bounded profile dry-run preview smoke passed: `timeout 30s node tools/crawl/index.js simple-distributed-smoke --dry-run --graph-feedback-artifact /tmp/graph-feedback-bbc-profile-match-smoke.json` printed the read-only summary and did not start crawlers.
- Final `git diff --check` for tracked touched files and trailing-whitespace scan for tracked/untracked touched files passed.
- Added `Recursive Backlog Status` to `PLAN.md` with Done/Next/Later queues so future recursive prompts can pull from an explicit backlog instead of inferred next-agent suggestions.
- Added aggregate readiness evidence to `graph-feedback --operator-report`: readiness counts by label, requested/reported/omitted profile counts, artifact supplied/missing counts, static/hostless counts, exact host matches, matched candidate totals, and caveat totals. Markdown reports now include a `Readiness Summary` section.
- Added explicit `--preflight-format text` for `graph-feedback --profile-preflight`. The default remains JSON; text mode shows readiness, planned hosts, artifact size/age/warnings, host match state, candidate count, next safest command, and the no-enqueue/no-remote-seed/no-collect-change policy.
- Added focused tests for readiness aggregates, text preflight output/writes, JSON preflight stability, invalid preflight/report option combinations, and continued file-only/no-live-builder behavior. Initial focused verification passed: `npm run test:by-path -- tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/profile-hosts.test.js` with 2 suites / 62 tests passing.
- Final focused verification passed: syntax checks for touched JS/test files; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 160 tests passing.
- Raw SQL/driver boundary scan over graph-feedback/planning/profile files returned no matches. Bounded operator-report smoke wrote a 2,396-byte Markdown report with readiness summary, and bounded `--profile-preflight --preflight-format text` smoke printed readiness/artifact-age/host-match/next-command evidence.
- Bounded profile dry-run preview smoke passed with the matching temp `bbc.com` artifact: `timeout 30s node tools/crawl/index.js simple-distributed-smoke --dry-run --graph-feedback-artifact /tmp/graph-feedback-bbc-profile-match-smoke.json` printed the read-only summary and did not start crawlers.
- Final `git diff --check` for tracked touched files and trailing-whitespace scan for tracked/untracked touched files passed.
- Added `graph-feedback --profile-workflow --profile <name> [--from-artifact <path>]` as a file-only profile workflow checklist. It packages exact profile host summary, bounded artifact generation, host comparison, strict validation, text preflight, compact operator report, and canonical `tools/crawl/index.js <profile> --dry-run --graph-feedback-artifact <path>` preview into one bounded JSON or Markdown output.
- The workflow checklist reuses existing profile preflight evidence, so readiness labels, artifact byte size, generatedAt validity, artifact age, stale warnings, host mismatch caveats, candidate counts, references, and no-action policy stay aligned with the rest of the graph-feedback artifact surfaces.
- Added durable workflow documentation at `docs/workflows/graph-feedback-artifact-planning.md`, linked it from `docs/INDEX.md` and `tools/crawl/AGENT.md`, and documented the stale-artifact warning-only behavior with copy/paste examples.
- Added focused tests for profile workflow Markdown output, explicit workflow report writes, hostless workflow shape, stale-artifact preflight/report warning behavior, invalid workflow flag combinations, and proof that the live graph builder is not called in workflow/report/preflight consumer modes.
- Initial focused verification passed: `node --check tools/crawl/graph-feedback.js`, `node --check tests/tools/crawl/graph-feedback-loader.test.js`, and `npm run test:by-path -- tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/profile-hosts.test.js` with 2 suites / 67 tests passing.
- Final focused verification passed: syntax checks for touched JS/test files; raw SQL/driver boundary scan returned no matches; `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js` with 5 suites / 165 tests passing.
- Bounded workflow/checklist/report smoke passed with `/tmp/graph-feedback-bbc-profile-match-workflow.json`: `--profile-workflow --workflow-format markdown --out /tmp/graph-feedback-profile-workflow-checklist.md` printed the seven-step safe checklist, and `--operator-report --report-commands minimal --format markdown --out /tmp/graph-feedback-profile-workflow-report.md` printed readiness summary without candidate URL dumps.
- Bounded stale-artifact preflight/report smoke passed with `/tmp/graph-feedback-bbc-profile-stale-workflow.json`: both `--profile-preflight --preflight-format text` and `--operator-report --report-commands minimal --format markdown` warned that the artifact was older than 7 days and did not reject the read-only preview.
- Bounded profile dry-run preview smoke passed: `timeout 30s node tools/crawl/index.js simple-distributed-smoke --dry-run --graph-feedback-artifact /tmp/graph-feedback-bbc-profile-match-workflow.json` printed the read-only graph-feedback summary and did not start crawlers.
- Final `git diff --check` for tracked touched files, no-index check for the new workflow doc, and trailing-whitespace scan for touched files produced no whitespace errors.
- Read-only closure consistency pass completed: CLI help now describes `--recipe` as compact artifact-derived preview commands and `--profile-workflow` as the complete profile workflow checklist; docs now keep those surfaces distinct.
- Added `docs/workflows/graph-feedback-profile-workflow-sample.md` as a compact sample output shape without candidate URL dumps, and linked it from `docs/INDEX.md`, `tools/crawl/AGENT.md`, and the graph-feedback workflow doc.
- Guarded live seeding is now explicitly queued as the next track because the operator requested it. `FUTURE_LIVE_SEEDING_DESIGN.md` now states that the next implementation pass should add `--use-graph-feedback-seeds` behind dry-run-first proof, freshness gates, caps, mocked remote tests, rollback/disable notes, and no raw SQL outside `news-crawler-db`.
- Added focused help-alignment test coverage; final focused Jest run passed with 5 suites / 166 tests.
- Final read-only smoke matrix passed with temporary artifacts: matching profile workflow, `www.bbc.com` vs `bbc.com` host mismatch compare and strict dry-run rejection, stale-artifact preflight warning, hostless `remote-status` preflight, compact operator report, canonical profile dry-run preview, and compact artifact recipe.
- Started guarded live seeding behind explicit unified-launcher `--use-graph-feedback-seeds`. Without the flag, `--graph-feedback-artifact` remains dry-run/read-only. With the flag, only remote `start`, `launch`, `bounded`, and `run` commands with exact planned hosts are eligible.
- Added `tools/crawl/lib/graph-feedback-live-seeds.js` to validate saved artifacts for live use and translate candidates into the existing `crawl-remote.js --seed-urls-by-domain` surface. It enforces fresh `generatedAt`, max 5 hosts, max 10 candidates/host, max 25 URLs total, max URL length 4096, artifact byte cap, max 128 KB seed request body, exact URL host matching, and delimiter rejection.
- The first live slice rejects `collect`, status/sync/drain/hostless/non-remote paths, stale/malformed/mismatched/oversized artifacts, excessive candidates, unsafe URLs, invocations that already include seed flags, and the `run.js` surface where the flag could imply live behavior. No raw SQL, new endpoint, DB write, or collect change was added.
- Added `tests/tools/crawl/graph-feedback-live-seeds.test.js` and expanded `tests/tools/crawl-index.test.js` for explicit flag parsing, mocked live delegation, no seed args without the flag, dry-run live preview, stale/mismatch/oversized/bad schema failures, host/candidate/URL/body caps, and collect rejection.
- Added `docs/workflows/remote-crawler-health-recovery-deploy.md` and linked it from crawler docs/plans so future live seeding includes fast remote health checks, stop/stabilize steps, PM2/install inspection, deploy/update commands, deploy CLI/doc/test hardening, and post-recovery proof.
- Verification passed: syntax checks for touched JS/test files; focused Jest `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` with 8 suites / 197 tests passing.
- Boundary/smoke verification passed: raw SQL/driver scan over touched graph-feedback/planning/profile files returned no matches; read-only dry-run and `--use-graph-feedback-seeds` dry-run previews used `/tmp/graph-feedback-bbc-live-smoke.json` without contacting the fleet; `run.js` rejected the live seed flag with exit 3; targeted `git diff --check` and trailing-whitespace scan over touched files passed. Whole-repo `git diff --check`/stat timed out due unrelated repo-wide CRLF warnings, so targeted checks were used for touched files.
- Added dry-run-first preview evidence for guarded live seeding. `--graph-feedback-preview-evidence <path>` writes a bounded fingerprint JSON file in dry-run, and the operator live mode requires then verifies the fingerprint before deploy preflight or remote delegation. The evidence file records artifact path, artifact `generatedAt`, byte size, planned hosts, candidate counts, request body size, and caps without candidate URL dumps.
- Added bounded live seed-attempt logging. `--seed-attempt-log <path>` appends compact JSONL with artifact path, host/count/body/freshness evidence, preview fingerprint, and a redacted delegated command. The log is live-only; dry-run proof uses `--graph-feedback-preview-evidence`.
- Hardened live seed edge cases: future-dated artifacts now have explicit coverage, `www.` remote domain keys are rejected in this first slice because direct remote seed-domain parsing canonicalizes domains, and live commands that supply pre-existing seed flags continue to fail before delegation.
- Kept direct `crawl-remote.js` graph-feedback usage rejected. The lower-level CLI now exits before remote contact when graph-feedback artifact/live-seed flags are supplied, pointing operators back to the unified launcher where all gates live.
- Expanded remote deploy preflight coverage for `start` and `run` start-like commands while keeping `status`, `health`, and `sync` non-deploying.
- Initial focused verification passed: syntax checks for touched JS/test files; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl-index.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/remote-deploy-preflight.test.js --runInBand` with 4 suites / 60 tests passing. Jest reported the existing haste-map package-name collision warning under `wip/legacy-distributed/remote-crawler-v1`.
- Final focused verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` with 9 suites / 212 tests passing. Existing expected output included the silent-child timeout fixture and the `NO_COLOR`/`FORCE_COLOR` warning.
- Boundary and smoke verification passed: raw SQL/driver scan over graph-feedback/planning/profile/live-seed helper files returned no matches; a generated `/tmp/graph-feedback-bbc-live-evidence-smoke.json` artifact produced a read-only dry-run preview and a `--use-graph-feedback-seeds --graph-feedback-preview-evidence` dry-run preview without contacting the fleet; preview evidence was 822 bytes with one candidate and no URL dump; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact.
- Targeted `git diff --check` and trailing-whitespace scans over touched files passed. `crawl-remote.js` had pre-existing trailing whitespace in the touched region; only those whitespace characters were cleaned.
- Added the real-remote approval package for the guarded live-seeding track. `tools/crawl/index.js --dry-run --use-graph-feedback-seeds --graph-feedback-preview-evidence <path> --graph-feedback-approval-checklist <path>` now writes a bounded checklist with max 1 host, max 3 URLs, 30s guard, preview evidence path, seed-attempt log path, health/status commands, rollback stop command, post-seed verification shape, and the explicit approval line `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`. The checklist remains dry-run-only and never authorizes a real seed.
- Added post-seed verification design in code via `buildPostSeedVerificationChecklist()`: health, status, errors, content, one-round sync/pull, local DB confirmation, and rollback stop/status commands. This is evidence shape only; no remote contact is made by the helper.
- Hardened remote recovery/deploy proof paths with tests for deploy recovery flags, missing remote build metadata, busy status evidence in dry-run summaries, and `always` deploy preflight args with remote dir/service and skip overrides.
- Updated `docs/workflows/graph-feedback-artifact-planning.md`, `docs/workflows/remote-crawler-health-recovery-deploy.md`, `tools/crawl/AGENT.md`, `PLAN.md`, and `FUTURE_LIVE_SEEDING_DESIGN.md` so real remote seeding is queued only behind a separate explicit approval line and a ready checklist.
- Focused verification passed: `node --check` for touched live-seed, launcher, deploy/preflight, and test files; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl-index.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js --runInBand` with 4 suites / 79 tests passing.
- Broader focused verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` with 9 suites / 220 tests passing. Existing expected output included the silent-child timeout fixture and the `NO_COLOR`/`FORCE_COLOR` warning.
- Boundary and smoke verification passed: raw SQL/driver scan over graph-feedback/planning/profile/live-seed files returned no matches; a generated `/tmp/graph-feedback-bbc-approval-smoke.json` artifact produced a dry-run preview and approval checklist without contacting the fleet; checklist was 5,809 bytes, ready for human approval, `realSeedAuthorized:false`, and contained no candidate URL dump; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact.
- No real seed approval line was supplied in this turn, so no real remote seed was run.
- Added compact post-seed verification evidence handling in `tools/crawl/lib/graph-feedback-live-seeds.js`: `buildPostSeedVerificationEvidence()` / `writePostSeedVerificationEvidenceSync()` record preview fingerprint, seed-attempt log path, hosts, candidate/request body counts, check booleans, short URL-redacted summaries, rollback status, and evidence-only/no-enqueue/no-collect-change policy. Evidence is capped at 16 KB and supports at most 12 checks.
- Added pure deploy troubleshooting hints in `tools/crawl/deploy-remote-server.js` for failed post-deploy health, busy-server refusal, stale local package metadata, missing/stale remote build metadata, custom remote dir/service targets, and `--force` decision points. This is helper/test coverage only and does not contact the fleet.
- Updated `tools/crawl/AGENT.md`, both workflow docs, `PLAN.md`, and `FUTURE_LIVE_SEEDING_DESIGN.md` to point at the compact post-seed evidence shape and keep real remote seeding behind the separate approval token.
- Initial focused verification passed: `node --check` for touched live-seed/deploy files and tests; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/remote-crawler-deploy.test.js --runInBand` with 2 suites / 33 tests passing.
- Broader focused verification passed: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` with 9 suites / 227 tests passing. Existing expected output included the silent-child timeout fixture and the `NO_COLOR`/`FORCE_COLOR` warning.
- Boundary and smoke verification passed: raw SQL/driver scan over graph-feedback/planning/profile/live-seed files returned no matches; a generated `/tmp/graph-feedback-bbc-approval-refresh.json` artifact produced a dry-run preview, `/tmp/graph-feedback-bbc-approval-refresh-checklist.json` approval checklist, and `/tmp/graph-feedback-bbc-post-seed-evidence-shape.json` post-seed evidence shape without contacting the fleet. Checklist was 6,014 bytes; post-seed evidence was 1,034 bytes and redacted the URL; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact.
- Added file-only dry-run approval readiness validation in `tools/crawl/lib/graph-feedback-live-seeds.js`. `buildLiveSeedApprovalReadiness()` ties together the approval checklist, preview evidence fingerprint, max 1 host / max 3 URL / 30s smoke caps, seed-attempt log path, post-seed evidence shape, and dry-run-only/no-enqueue/no-collect policy. It reports `realSeedAuthorized:false` and does not contact the fleet.
- Added focused tests for approval readiness built from temporary checklist/preview/post-seed evidence files, plus preview fingerprint mismatch behavior. The helper reads bounded JSON evidence files and keeps URL dumps out of the readiness object.
- Verification passed: `node --check` for the touched live-seed helper/test; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js --runInBand` passed 18 tests; the broader focused crawler/deploy suite passed 9 suites / 229 tests.
- Boundary and smoke verification passed: raw SQL/driver scan over graph-feedback/planning/profile/live-seed files returned no matches; `/tmp/graph-feedback-bbc-approval-rehearsal.json` produced a dry-run preview, preview evidence, approval checklist, and approval-readiness object without contacting the fleet; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact; profile dry-run preview remained read-only. Targeted `git diff --check` and trailing-whitespace scans for touched files passed.
- Added `--graph-feedback-approval-readiness <path>` to the unified launcher dry-run package. It writes the same bounded approval-readiness object as the library helper after producing preview evidence and the approval checklist, still with `realSeedAuthorized:false` and no candidate URL dump.
- Enriched the real-seed approval checklist with a pre-seed remote usability proof section: health, status/build metadata, recent errors, content probe, and deploy preflight commands plus decision points for unhealthy status, stale build metadata, and `--force` usage. This is command evidence only; the dry-run package does not contact the fleet.
- Added focused tests for the launcher readiness artifact, readiness flag validation, bounded readiness writes, and pre-seed usability proof commands.
- Verification passed: `node --check` for touched launcher/live-seed files and tests; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl-index.test.js --runInBand` passed 70 tests; broader focused verification passed 9 suites / 233 tests.
- Boundary and smoke verification passed: raw SQL/driver scan over graph-feedback/planning/profile/live-seed files returned no matches; `/tmp/graph-feedback-bbc-readiness-package.json` produced dry-run preview evidence, approval checklist, and launcher-written approval readiness without contacting the fleet; readiness reported `readyForApproval:true`, `realSeedAuthorized:false`, 16 checks, pre-seed usability commands, and no URL dumps; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact; profile dry-run preview remained read-only.
- Added remote deploy preflight passthrough flags to the unified launcher: `--remote-deploy-status-host`, `--remote-deploy-status-port`, `--remote-deploy-remote-dir`, `--remote-deploy-service`, `--remote-deploy-skip-busy-check`, `--remote-deploy-skip-health-check`, and `--remote-deploy-skip-db-build`. This lets operators rehearse wrong-status-endpoint, wrong-install-dir, wrong-service, and recovery override decisions through dry-run launcher commands before any live seed or deploy.
- Added focused tests for launcher parsing of those deploy target/recovery flags and for `remote-deploy-preflight` argument generation with explicit status host/port, remote dir, service, and skip overrides.
- Added deploy preflight command rendering for remote launcher dry-runs. Start-like remote dry-runs now print the exact `deploy-remote-server.js` preflight command next to the delegated crawl command, including target host/port, remote dir/service, and skip overrides, without running the preflight.
- No real seed approval line was supplied in the current turn, so no remote health/status/deploy/seed command was run.
- Strengthened the dry-run approval readiness artifact in `tools/crawl/lib/graph-feedback-live-seeds.js`: it now includes a compact summary, blockers, seed-attempt log path, pre-seed usability command names, post-seed proof command names, rollback command names, and required command-name lists while keeping `realSeedAuthorized:false` and URL dumps out.
- Added readiness checks for missing remote-usability proof, missing post-seed proof commands, missing rollback commands, and checklist-only/no-action remote-usability policy.
- Updated `tools/crawl/AGENT.md`, both workflow docs, `PLAN.md`, `FUTURE_LIVE_SEEDING_DESIGN.md`, and LT-001 notes so future approval requests inspect the launcher-written readiness artifact as the single bounded no-contact proof package.
- Verification passed: syntax checks for touched live-seed, launcher, and test files; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl-index.test.js --runInBand` passed 73 tests; the broader focused crawler/deploy suite passed 9 suites / 238 tests.
- Boundary and no-contact smoke verification passed: raw SQL/driver scan over graph-feedback/planning/live-seed files returned no matches; `/tmp/graph-feedback-bbc-dryrun-package.json` produced preview evidence, approval checklist, and readiness artifacts without contacting the fleet; readiness reported `readyForApproval:true`, `blockerCount:0`, required remote-usability/post-seed/rollback command names, seed-attempt log path, and no URL dumps; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact; profile dry-run preview and deploy preflight rendering remained no-contact.
- No separate real-seed approval line was supplied in the current turn, so the work stayed no-contact again.
- Added `--graph-feedback-post-seed-checklist <path>` to the unified launcher dry-run package. It writes `buildPostSeedVerificationChecklist()` output as a bounded dry-run-only JSON artifact containing post-seed proof commands, rollback commands, and the evidence shape without executing health/status/sync checks.
- Added `writePostSeedVerificationChecklistSync()` plus focused tests for the helper, launcher parsing, launcher writing, and dry-run/live validation. This closes the dry-run approval package gap: preview evidence, approval checklist, readiness summary, and post-seed proof plan can now all be produced from the same no-contact command.
- Verification passed: syntax checks for touched live-seed, launcher, and test files; `npx jest tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl-index.test.js --runInBand` passed 75 tests; the broader focused crawler/deploy suite passed 9 suites / 240 tests.
- Boundary and no-contact smoke verification passed: raw SQL/driver scan over graph-feedback/planning/live-seed files returned no matches; `/tmp/graph-feedback-bbc-dryrun-package.json` produced preview evidence, approval checklist, readiness, and post-seed checklist artifacts without contacting the fleet; the post-seed checklist listed health/status/errors/content/one-round-sync/local-db and stop/status rollback commands with no URL dumps; direct `crawl-remote.js status --use-graph-feedback-seeds` rejected before remote contact; profile dry-run preview and deploy preflight rendering remained no-contact.
- No separate real-seed approval line was supplied in the current turn, so the live smoke remains blocked on approval. Per the active prompt, no real remote health/status/deploy/seed command was run.
- Reconfirmed the dry-run approval package with `/tmp/graph-feedback-bbc-no-approval-package.json`: the unified launcher wrote preview evidence, approval checklist, approval readiness, and post-seed checklist artifacts. Readiness reported `readyForApproval:true`, `realSeedAuthorized:false`, one planned host, one candidate, required pre-seed usability commands, and no URL dumps in the saved evidence files.
- Direct remote rejection was reconfirmed locally: `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` exits before remote contact and points operators back to `tools/crawl/index.js`.
- Documentation refresh: `PLAN.md` now marks the real smoke as explicitly blocked until a separate `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE` line is present, and the remote recovery workflow clarifies that approval absence means no real remote health/status/deploy checks.
- No separate approval token was supplied in the current turn. I ran no real remote health/status/deploy/seed commands.
- No-contact drift check passed with `/tmp/graph-feedback-bbc-blocked-rehearsal.json`: launcher dry-run wrote preview evidence, approval checklist, approval readiness, and post-seed checklist artifacts. Readiness reported `readyForApproval:true`, `realSeedAuthorized:false`, one planned host, one candidate, required pre-seed usability commands, post-seed proof commands, rollback commands, and no URL dumps.
- Local-only smokes passed: direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before remote contact; `simple-distributed-smoke --dry-run --graph-feedback-artifact ...` remained read-only; remote deploy preflight dry-run rendered the delegated preflight command without executing it.
- No implementation drift required code changes. `PLAN.md` now records this as an approval-blocked drift check; `Next` remains blocked on the separate explicit approval line.
- 2026-05-27: User stated "I approve it all" and asked that approval context be noted. The active continuation prompt still did not contain the exact standalone approval token required by `FUTURE_LIVE_SEEDING_DESIGN.md`, so this note records intent/context only; it is not treated as executable approval for real remote health/status/deploy/seed commands.
- 2026-05-27: Active prompt included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`, so the approved path was entered. Generated `/tmp/graph-feedback-bbc-real-smoke.json` (953 bytes, `bbc.com`, 1 recommendation, fresh `generatedAt`) plus dry-run proof files: `/tmp/graph-feedback-bbc-real-preview.json`, `/tmp/graph-feedback-bbc-real-checklist.json`, `/tmp/graph-feedback-bbc-real-readiness.json`, and `/tmp/graph-feedback-bbc-real-post-seed-checklist.json`.
- 2026-05-27: Pre-seed remote proof was attempted with 30s guards and raw outputs captured under `/tmp/graph-feedback-real-smoke-proof/`. `health` exited 0 and reported the multi-domain server healthy. `status --json`, `errors --limit 10 --json`, and `content --domain bbc.com --json` produced parseable JSON but exited 124 due the guard. `deploy-remote-server.js --if-needed --json` also exited 124 while running the local `news-crawler-db` TypeScript build.
- 2026-05-27: No live seed was sent because the pre-seed proof did not exit cleanly under guard. No `/tmp/graph-feedback-bbc-real-seed-attempts.jsonl` was created. Wrote `/tmp/graph-feedback-bbc-real-post-seed-evidence.json` as a bounded abort evidence artifact with `allChecksOk:false`, five pre-seed check summaries, and rollback `attempted:false` / `ok:true`.
- 2026-05-27: Diagnosed the read-only remote proof hang as a client-side process lifecycle bug: `requestWithTimeout()` left a timeout race alive after successful HTTP responses, and HTTP connections were not explicitly one-shot. `crawl-remote.js` now delegates timeout handling to the request, uses `Connection: close` / `agent:false`, and local HTTP-server tests prove `status --json`, `errors --json`, and `content --json` exit after output.
- 2026-05-27: Added `deploy-remote-server.js --preflight-only --json` as the fast deploy proof path for live-seed readiness. It reports local build state, remote build comparison, decision, and proof-only action policy without running the `news-crawler-db` build, uploading, stopping PM2, or contacting SSH. The graph-feedback approval checklist now points at this command instead of `--if-needed --json`.
- 2026-05-27: No standalone approval token was present in this follow-up prompt, so no real remote health/status/deploy/seed command was run and no live retry was attempted. Local-only dry-run package and proof-mode smokes were used instead.
- 2026-05-27: Local-only dry-run package smoke wrote `/tmp/graph-feedback-bbc-proof-hardened-preview.json`, checklist, readiness, and post-seed checklist. Readiness remained `readyForApproval:true` / `realSeedAuthorized:false`, the checklist deploy proof command is `timeout 30s node tools/crawl/deploy-remote-server.js --preflight-only --json`, and saved evidence had no candidate URL dumps.
- 2026-05-27: Local deploy proof smoke `timeout 30s node tools/crawl/deploy-remote-server.js --preflight-only --json --skip-busy-check` exited cleanly and wrote `/tmp/remote-deploy-preflight-proof-smoke.json` (1804 bytes). It reported `decision:"needs-local-build"`, `readyForLiveSeedProof:false`, `localBuild.current:false`, and proof-only action flags (`buildsLocalPackage:false`, `deploysRemote:false`).
- 2026-05-27: Verification passed for proof-path hardening: syntax checks for touched crawl/deploy/live-seed JS and tests; focused Jest `npx jest tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/remote-crawler-deploy.test.js --runInBand`; broader `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/graph-feedback-loader.test.js tests/tools/crawl/graph-feedback-planner.test.js tests/tools/crawl/profile-hosts.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` with 9 suites / 246 tests passing. Raw SQL/direct-driver scan returned no matches; targeted `git diff --check` and trailing-whitespace scans passed.
- 2026-05-27: Follow-up prompt lacked the standalone approval token, so no real remote health/status/deploy/seed command was run. Local no-contact drift checks passed with `/tmp/graph-feedback-bbc-no-approval-drift.json`: dry-run package wrote preview/checklist/readiness/post-seed checklist, readiness was `readyForApproval:true` and `realSeedAuthorized:false`, the checklist deploy proof command stayed `timeout 30s node tools/crawl/deploy-remote-server.js --preflight-only --json`, and no seed-attempt log was created.
- 2026-05-27: Additional local drift checks passed: direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before contact; `simple-distributed-smoke --dry-run --graph-feedback-artifact ...` remained read-only; local deploy proof `timeout 30s node tools/crawl/deploy-remote-server.js --preflight-only --json --skip-busy-check` exited cleanly with `decision:"needs-local-build"`, `readyForLiveSeedProof:false`, and no build/deploy/stop action flags.
- 2026-05-27: Verification for the no-approval drift check passed: syntax checks for touched crawl/deploy/live-seed JS and tests; raw SQL/direct-driver scan returned no matches; targeted `git diff --check` and trailing-whitespace scans passed; broader focused Jest passed with 9 suites / 246 tests. No remote fleet contact was made.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`. Generated `/tmp/graph-feedback-bbc-real-retry.json` (1353 bytes, `bbc.com`, one fast-mode recommendation, fresh `generatedAt`) plus dry-run proof files `/tmp/graph-feedback-bbc-real-retry-preview.json`, checklist, readiness, and post-seed checklist. Readiness remained `readyForApproval:true` and `realSeedAuthorized:false`.
- 2026-05-27: Local deploy readiness was refreshed before remote proof. `/tmp/remote-deploy-preflight-real-retry-local-before.json` reported `decision:"needs-local-build"` from stale `../news-crawler-db` source evidence. `timeout 180s node tools/crawl/deploy-remote-server.js --build-only --json --skip-busy-check` completed and produced build `20260527035514` at `/mnt/c/Users/james/Documents/repos/copilot-dl-news/tmp/remote-crawler-v2-deploy/remote-crawler-v2-deploy.tar.gz` (565335 bytes). The follow-up local proof showed `localBuild.current:true`.
- 2026-05-27: Approved pre-seed remote proof commands all exited within the 30s guard. `health` exited 0 with `mode:"multi-domain"`, 10 domains, and 0 running. `status --json` exited 0 with orchestrator idle and 0 fetched/stored/errors/pending. `errors --limit 10 --json` exited 0 with count 0. `content --domain bbc.com --json` exited 0 with 17 total stored content rows and no `bbc.com` row in the content summary.
- 2026-05-27: Deploy preflight exited 0 but blocked seeding: `/tmp/remote-deploy-preflight-real-retry-remote.json` reported `decision:"deploy-needed"`, `readyForLiveSeedProof:false`, local build current, remote build metadata missing, and remote status not busy. Per the approval gate, no live seed was sent from this proof.
- 2026-05-27: Followed the deploy workflow far enough to attempt current deployment without force because the remote status was idle. `timeout 180s node tools/crawl/deploy-remote-server.js --if-needed --apply --json` exited 1 before upload/service changes: `SSH access check failed for ubuntu@141.144.193.218: Permission denied (publickey)`. This is now the concrete blocker before another seed retry: configure SSH auth or provide an alternate documented deploy path, then rerun deploy preflight.
- 2026-05-27: Wrote `/tmp/graph-feedback-bbc-real-retry-post-seed-evidence.json` as bounded abort evidence. `allChecksOk:false`; health/status/errors/content checks were ok, deploy preflight and deploy apply were not ok, and `live-seed` was false because no seed request was sent. No `/tmp/graph-feedback-bbc-real-retry-seed-attempts.jsonl` was created.
- 2026-05-27: Verification for the approved retry pass passed: syntax checks for touched crawl/deploy/live-seed JS and tests; raw SQL/direct-driver scan returned no matches; focused Jest passed with 9 suites / 246 tests; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` still rejects before contact; profile dry-run preview remains read-only; targeted `git diff --check` and trailing-whitespace scans passed.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`, but the pass stopped at SSH readiness before deploy or seed. Non-destructive readiness checks found no `REMOTE_CRAWLER_SSH_KEY`, no `SSH_AUTH_SOCK`, no reachable ssh-agent (`ssh-add -l` exit 2), and `timeout 20s ssh -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new -p 22 ubuntu@141.144.193.218 true` exited 255 with `Permission denied (publickey)`.
- 2026-05-27: Because SSH auth was unavailable, no deploy, no deploy preflight retry, no graph-feedback live seed, no remote stop, and no remote service change was attempted in this pass. The current blocker remains: configure SSH auth or provide another documented deploy path so remote build metadata can be made current before the next approved seed attempt.
- 2026-05-27: Verification for the SSH-blocked pass passed: syntax checks for touched crawl/deploy/live-seed JS and tests; raw SQL/direct-driver scan returned no matches; focused Jest passed with 9 suites / 246 tests; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` still rejects before contact; targeted `git diff --check` and trailing-whitespace scans passed.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`; SSH readiness was checked before any deploy or seed. Environment still had no `REMOTE_CRAWLER_SSH_KEY`, no `SSH_AUTH_SOCK`, and `ssh-add -l` could not connect to an agent. BatchMode SSH to `ubuntu@141.144.193.218` exited 255 with `Permission denied (publickey)`.
- 2026-05-27: This pass did not run deploy, deploy preflight retry, remote health/status/errors/content, remote stop, or graph-feedback live seed because the SSH readiness gate failed. The operational blocker remains unchanged: configure SSH auth or provide another documented deploy route, then rerun deploy readiness before any seed.
- 2026-05-27: Verification for the repeated SSH-blocked pass passed: syntax checks covered crawl/deploy/live-seed files and focused tests; raw SQL/direct-driver scan over graph-feedback/deploy planning files found no matches; direct `crawl-remote.js` graph-feedback flag rejection exited before contact; focused Jest passed 9 suites / 246 tests; targeted diff and trailing-whitespace checks passed.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`; the latest retry stopped at the same SSH readiness gate. Environment checks found `REMOTE_CRAWLER_SSH_KEY`, `SSH_AUTH_SOCK`, `FLEET_HOST`, and deploy SSH env vars unset, no default private key under `/root/.ssh`, `ssh-add -l` unable to contact an agent, and BatchMode SSH to `ubuntu@141.144.193.218` failing with `Permission denied (publickey)`.
- 2026-05-27: Because SSH auth was unavailable, this pass ran no deploy, deploy preflight retry, remote health/status/errors/content proof, remote stop, or graph-feedback live seed. The next useful approved pass must start after SSH auth is configured or an alternate documented deploy path is available.
- 2026-05-27: Verification for the latest SSH-blocked pass passed: syntax checks for crawl/deploy/live-seed files and tests; raw SQL/direct-driver scan returned no matches; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before contact; focused Jest passed 9 suites / 246 tests; targeted diff and trailing-whitespace checks passed.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`. Non-destructive SSH discovery found `/mnt/c/Users/james/.ssh/config` with `oracle-worker` pointing at `ubuntu@141.144.193.218` and `ssh-key-2025-11-11.key`. Explicit BatchMode probes with both discovered candidate keys failed before authentication because OpenSSH ignored the key files as too open: `ssh-key-2025-11-10.key` mode `0444`, `ssh-key-2025-11-11.key` mode `0777`.
- 2026-05-27: Because SSH auth still failed, this pass did not run deploy, deploy preflight retry, remote health/status/errors/content proof, remote stop, or graph-feedback live seed. The next useful approved pass should either run with `REMOTE_CRAWLER_SSH_KEY` pointing at a private key path OpenSSH accepts with `0600`-style permissions, or use another documented deploy path that makes remote build metadata current.
- 2026-05-27: Verification for the candidate-key SSH-blocked pass passed: syntax checks for crawl/deploy/live-seed files and focused tests; raw SQL/direct-driver scan returned no matches; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before contact; focused Jest passed 9 suites / 246 tests; targeted diff and trailing-whitespace checks passed.
- 2026-05-27: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`. SSH readiness is unchanged: no deploy SSH env vars, no `SSH_AUTH_SOCK`, no reachable ssh-agent, no default private key under `/root/.ssh`, Windows SSH config still points at `ssh-key-2025-11-11.key`, and explicit BatchMode probes with `ssh-key-2025-11-10.key` / `ssh-key-2025-11-11.key` were ignored because OpenSSH rejected their `0444` / `0777` permissions as too open.
- 2026-05-27: Because SSH auth still failed, this pass again ran no deploy, deploy preflight retry, remote health/status/errors/content proof, remote stop, or graph-feedback live seed. No remote build metadata changed.
- 2026-05-27: Verification for the unchanged SSH-blocked pass passed: syntax checks for crawl/deploy/live-seed files and focused tests; raw SQL/direct-driver scan returned no matches; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before contact; focused Jest passed 9 suites / 246 tests; targeted diff and trailing-whitespace checks passed.
- 2026-05-28: Active prompt again included standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE`. SSH readiness remains unchanged after the date rollover: no `REMOTE_CRAWLER_SSH_KEY`, no deploy SSH env vars, no `SSH_AUTH_SOCK`, no reachable ssh-agent, no default private key under `/root/.ssh`, and `/mnt/c/Users/james/.ssh/config` still maps `oracle-worker` to `ubuntu@141.144.193.218` via `ssh-key-2025-11-11.key`.
- 2026-05-28: Explicit BatchMode probes with `/mnt/c/Users/james/.ssh/ssh-key-2025-11-10.key` and `/mnt/c/Users/james/.ssh/ssh-key-2025-11-11.key` were again ignored by OpenSSH before authentication because their permissions are too open (`0444` and `0777`). Per the active prompt constraint, no chmod/copy key handling was attempted, and no deploy, deploy preflight retry, remote proof, remote stop, or graph-feedback live seed was run.
- 2026-05-28: Verification for the SSH-blocked pass passed: syntax checks for crawl/deploy/live-seed files and focused tests; raw SQL/direct-driver scan returned no matches; direct `crawl-remote.js status --use-graph-feedback-seeds --graph-feedback-artifact ...` rejected before contact; focused Jest passed 9 suites / 246 tests; targeted diff and trailing-whitespace checks passed.
- 2026-05-28: After the user explicitly asked for local key permissions to be made less open, copied the Windows key to `/root/.ssh/oracle-worker-2025-11-11.key` with `0600` permissions. Fingerprint `SHA256:zw6wNgk4KSPPMvA2BDDzazjnuP7KSlhdJV70ltzlQw0`; BatchMode SSH to `ubuntu@141.144.193.218` succeeded using that key.
- 2026-05-28: Approved live-smoke prompt included the standalone `APPROVE_GRAPH_FEEDBACK_REAL_SEED_SMOKE` token. Initial deploy preflight with the accepted SSH key reported `decision:"deploy-needed"`, `readyForLiveSeedProof:false`, local build current `20260527035514`, remote build metadata missing, and remote not busy.
- 2026-05-28: First deploy apply attempt with the default remote dir failed after stopping/removing `crawl-server-v4`: `tar: ~/apps/remote-crawler-v2: Cannot open: No such file or directory`. This exposed a deploy CLI/doc issue: the default `~/apps/...` remote dir is unsafe when stored in a quoted shell variable because tilde expansion does not occur.
- 2026-05-28: Retried deploy with `--remote-dir /home/ubuntu/apps/remote-crawler-v2`. Extraction succeeded, but remote `npm install` failed because the VM runs Node `v24.13.1`, `better-sqlite3` had no linux arm64/node 24 prebuild, and `make`/compiler tooling was absent. Installed `build-essential python3 make g++` via sudo, then reran the same documented apply path successfully. PM2 started `crawl-server-v4` with build `20260527035514`.
- 2026-05-28: Pre-seed proof after deploy passed under 30s guards. `health` reported `10 domains, 0 running, 0 stored`; `status --json` reported build `20260527035514` and no active work; `errors --limit 10 --json` reported count 0; `content --domain bbc.com --json` reported no stored BBC content before seed; deploy preflight reported `decision:"current"` and `readyForLiveSeedProof:true`.
- 2026-05-28: Generated fresh tiny artifact `/tmp/graph-feedback-bbc-real-20260528.json` with `schemaVersion:1`, one host `bbc.com`, 3 candidates, and 2348 bytes. Dry-run approval package wrote preview/checklist/readiness/post-seed checklist files; readiness was `readyForApproval:true`, planned hosts `[bbc.com]`, candidate count 3, preview fingerprint `b2c0c7646392587648ca99e83bccae6d89556e62d68a73955467128ad2ebd608`, request body 249 bytes.
- 2026-05-28: Ran exactly one live seed through the unified launcher: `remote bounded --domains bbc.com --max-pages 3 --timeout-min 1 --poll 5 --graph-feedback-artifact /tmp/graph-feedback-bbc-real-20260528.json --use-graph-feedback-seeds --graph-feedback-preview-evidence /tmp/graph-feedback-bbc-real-20260528-preview.json --seed-attempt-log /tmp/graph-feedback-bbc-real-20260528-seed-attempts.jsonl --remote-deploy never`. Exit 0; completed `bbc.com(3)` in 6.6s. Seed-attempt log is 730 bytes, URL-redacted, and records 3 candidates / 249 request-body bytes.
- 2026-05-28: Post-seed proof passed for crawl health/content/sync: post health healthy with 10 domains, 0 running, 3 stored; post status shows `bbc.com` stopped with fetched 3, done 3, errors 0, pending 1273; post errors count 0; post content shows 3 BBC content records; one sync round pulled 5 URLs and 3 content records; `npm run db:downloads:recent` shows local DB IDs 301980-301982 for the 3 BBC seed downloads at `2026-05-28 04:44:15`. `npm run db:downloads:stats` timed out under a 30s guard and should not be treated as completed proof.
- 2026-05-28: Post-seed deploy preflight now reports `decision:"blocked-busy"` / `readyForLiveSeedProof:false` because `bbc.com` has 1273 pending discovered URLs even though no domain is running. Rollback stop was run for `bbc.com`; final status shows 0 running and `bbc.com` stopped, but the pending queue is retained. No further live seed or deploy should be attempted until the residual queue is analyzed.
- 2026-05-28: Wrote bounded post-seed evidence `/tmp/graph-feedback-bbc-real-20260528-post-seed-evidence.json` (2363 bytes) with preview fingerprint, seed-attempt log path, 10 short check summaries, rollback status, and no candidate URL dumps. `allChecksOk:false` because local stats timed out and post-deploy preflight is blocked-busy, despite the seed, health, errors, content, sync, and local recent-download proof succeeding.
- 2026-05-28: Residual queue closeout used only bounded read-only remote evidence: `status --json`, `errors --limit 10 --json`, and `content --domain bbc.com --json`. The remote still reports totals fetched 3, stored 3, errors 0, pending 1273; orchestrator not running; `bbc.com` state stopped / `isRunning:false`; recent error count 0; BBC content summary 3 stored records, 952481 uncompressed bytes, 177707 compressed bytes. No stop/prune/deploy/seed maintenance action was run in this pass.
- 2026-05-28: Queue decision: retain the pending queue for now. Stop-only was already done after the smoke, sync already pulled bounded proof, and prune/drain would be remote maintenance rather than inspection. The next safe action is a documented queue-maintenance decision with explicit human approval and bounded local/remote proof, not an implicit second seed or deploy.
- 2026-05-28: Fixed the deploy default remote-dir bug in `tools/crawl/deploy-remote-server.js`. The generated remote install script now assigns home-relative paths as `REMOTE_DIR="$HOME/apps/remote-crawler-v2"` instead of `REMOTE_DIR='~/apps/remote-crawler-v2'`, so quoted shell variables no longer preserve a literal `~` during `mkdir`, `cd`, and `tar -C`.
- 2026-05-28: Added remote build-tool evidence to deploy hardening. The remote install script now warns before `npm install` if `make` or `g++` is absent, and troubleshooting hints detect `better-sqlite3`/`prebuild-install`/`node-gyp`/`make` failures and suggest `build-essential`, `make`, `g++`, and `python3`. Added focused tests for default path expansion, pending-queue `blocked-busy` proof with zero running domains, and native dependency build-tool hints.
- 2026-05-28: Added `NEXT_FEW_DAYS_PLAN.md` as the larger recursive prompt roadmap. It groups the next work into queue readiness, explicit queue maintenance gates, deploy reliability, second tiny seed readiness, and operator packaging, and instructs future prompts to bundle 8-12 related work items rather than shrinking back to narrow turns.
- 2026-05-28: Added `tools/crawl/lib/remote-queue-summary.js` plus `crawl-remote.js queue-summary` and `queue-checklist`. The summary reads only `/api/status`, `/api/errors`, and `/api/content/stats`; it reports bounded per-domain running/fetched/done/error/pending/content evidence, deploy-preflight implications, missing hosts, recent error counts, and next safest action without stopping, syncing, pruning, draining, clearing, deploying, seeding, or changing `collect`.
- 2026-05-28: Added dry-run queue maintenance checklist output with required current summary, recent-error/content proof, sync/local DB proof plan, rollback stop command, deploy preflight command, data-loss caveats, and separate `APPROVE_REMOTE_QUEUE_MAINTENANCE` / `APPROVE_REMOTE_FORCE_DEPLOY` tokens. Focused tests cover stopped-with-pending, running-with-pending, no-pending, host-not-found, malformed status, invalid bounds, no destructive action policy, and CLI clean exits.
- 2026-05-28: Added a tested remote operation taxonomy with `classifyRemoteOperation()`. It classifies read-only proof (`health`, `status`, `errors`, `content`, queue surfaces), safe stop/stabilize (`stop`), sync/local proof (`pull`, `sync`, `graph-seeds`), destructive maintenance (`remove`, prune/drain/clear), deploy action, and live crawl behavior. `pull`/`sync` are intentionally not pure read-only because prune flags or pending prune ledger state can mutate remote export state.
- 2026-05-28: Added file-only combined readiness reporting through `crawl-remote.js readiness-report` and `buildCombinedReadinessReport()`. The report reads saved graph artifact, queue summary, deploy proof, preview evidence, and post-seed checklist JSON files; emits readiness labels, stale/missing evidence warnings, pending/deploy/fingerprint counts; omits candidate URLs/full remote payloads; and has a no-action policy for remote state, deploy, seeds, sync, and collect.
- 2026-05-28: Focused coverage now includes the remote operation taxonomy, stale/missing/busy combined-readiness evidence, URL redaction in report output, direct `crawl-remote.js` graph-feedback rejection, and clean JSON exits for read-only proof commands. No second seed, prune, drain, clear, stop, sync, deploy, or force-deploy action was run.
- 2026-05-28: Added file-only queue maintenance decision artifacts through `crawl-remote.js maintenance-decision` and `buildQueueMaintenanceDecisionArtifact()`. The artifact consumes saved readiness/queue evidence, records intended action and approval-token state, cites affected hosts/pending counts, sync/local proof and rollback commands, readiness blockers, and data-loss caveats while keeping every execution flag false.
- 2026-05-28: Maintenance decision coverage now includes missing queue-maintenance approval, wrong graph-feedback seed approval, force-deploy approval recording without execution, sync/local proof caveats for prune flags/pending prune ledger state, malformed evidence rejection, and no candidate URL/full payload output. No remote maintenance, force deploy, second live seed, sync, or stop was run.
- 2026-05-28: Hardened maintenance-decision evidence validation. Decision artifacts now compare saved readiness-report and full queue-summary evidence for host match, pending-count match, freshness, and deploy-preflight implication drift before any future approved maintenance execution path. Stale or mismatched evidence blocks non-retain actions even when an approval token is recorded.
- 2026-05-28: Added `crawl-remote.js sync-proof-readiness` and `buildSyncLocalProofReadinessArtifact()` as a file-only no-contact sync/local DB proof plan. It emits the bounded `sync --rounds 1 --limit 25 --include-content true --include-links true --no-prune-after-ingest` command, local DB confirmation command, rollback stop command, stale/mismatch blockers, and prune-ledger caveats while keeping `syncsLocalDb:false` and all remote execution flags false.
- 2026-05-28: Focused verification for this pass passed for syntax and Jest (`remote-queue-summary.test.js`, `crawl-remote-graph-feedback-flags.test.js`, 28 tests). The broad raw SQL/driver scan over `crawl-remote.js` still reports the existing native-binding troubleshooting string containing `better-sqlite3`; the new queue summary helper/test files add no SQL or driver calls.
- 2026-05-28: Added `crawl-remote.js maintenance-execution-plan` and `buildQueueMaintenanceExecutionPlanArtifact()` as the file-only dry-run execution skeleton for queue maintenance. It consumes saved maintenance-decision, sync-proof-readiness, readiness-report, queue-summary, and deploy-proof JSON; validates freshness, host match, pending-count match, deploy proof state, no-prune sync proof, local DB confirmation, rollback command, and approval-token presence; and emits the future command skeleton while keeping `executesRemoteAction:false`.
- 2026-05-28: `deploy-remote-server.js --preflight-only --json` proof now includes `generatedAt`, so readiness reports and maintenance execution plans can distinguish fresh deploy proof from missing/stale proof. Focused tests cover the new timestamp and the maintenance execution plan's missing approvals, stale evidence, host/pending mismatch, force-deploy gate, malformed evidence, and accepted-but-non-executing approval tokens.
- 2026-05-28: Added `crawl-remote.js second-seed-readiness` and `buildSecondSeedReadinessArtifact()` as a file-only gate before any future second graph-feedback live seed. It consumes saved queue summary, combined readiness report, deploy proof, graph artifact, preview evidence, post-seed checklist, and optional maintenance execution plan; validates freshness, exact host agreement, preview fingerprint, deploy current/readiness, tiny caps (default 1 host / 3 total candidates / 3 per host), and retained/running queue blockers; and keeps all seed/sync/stop/prune/drain/clear/deploy execution flags false.
- 2026-05-28: Deploy preflight proof now includes a compact `operatorMessage` for `current`, `blocked-busy`, `needs-local-build`, and `deploy-needed` so operator reports can distinguish retained queues, stale/missing local builds, and deploy requirements without scraping logs.
- 2026-05-28: Focused verification for this pass passed for syntax and Jest: `remote-queue-summary.test.js` and `crawl-remote-graph-feedback-flags.test.js` passed 39 tests; `remote-crawler-deploy.test.js` and `remote-deploy-preflight.test.js` passed 30 tests. This pass did not run sync/pull/stop/prune/drain/clear/force-deploy or another live seed.
- 2026-05-28: File-only second-seed readiness smoke with existing `/tmp` evidence returned `readinessLabel:"blocked-retained-queue"` and kept every action flag false. A bounded read-only queue-summary smoke against `bbc.com` confirmed the current remote state is still stopped, 0 running, 0 recent errors, 3 stored, and 1273 pending; the next safest action remains `retain-queue-or-run-maintenance-checklist`.
- 2026-05-28: Broader focused verification passed after docs refresh: `npm run test:by-path -- tests/tools/crawl-index.test.js tests/tools/crawl/run.test.js tests/tools/crawl/graph-feedback-live-seeds.test.js tests/tools/crawl/crawl-remote-graph-feedback-flags.test.js tests/tools/crawl/remote-queue-summary.test.js tests/tools/remote-crawler-deploy.test.js tests/tools/remote-deploy-preflight.test.js` passed 7 suites / 207 tests. Raw SQL query/driver scan returned no matches; the broader driver-string scan only reports existing `better-sqlite3` troubleshooting text. Targeted diff and trailing-whitespace checks passed.
- 2026-05-28: Added monitored small-crawl evidence tooling. `tools/crawl/monitored-small-crawl.js` now supports read-only `policy`, `baseline`, `recent`, and `verify` modes backed by DB-owned `downloadEvidence` APIs through `tools/crawl/lib/monitored-small-crawl.js`; the unified launcher exposes it as `monitored-small-crawl` / `monitored` and package script `npm run crawl:monitored-small` points to it. The tool records baseline/recent/post-run DB evidence, host/window counts, samples, no-new-data warnings, and expected-count blockers without starting crawlers, contacting remote hosts, writing DB rows, pruning, force-deploying, or changing `collect`.
- 2026-05-28: Wired monitored small-crawl evidence into the existing Cloud Crawl dashboard/API path. `/api/cloud-crawl/status` now returns `monitoredSmallCrawl` and uses its bounded recent samples as the fallback visible Recent Downloads list, so operators can monitor recent crawl volume by host/window from the dashboard without new crawl behavior.
- 2026-05-28: Bounded DB-read-only smoke `timeout 30s node tools/crawl/monitored-small-crawl.js recent --hosts bbc.com --window-min 1440 --limit 5 --json` passed and reported the three 2026-05-28 BBC seed downloads, 952481 bytes, status 200, and no blockers. No actual new crawl, sync/pull, prune/drain/clear/force-deploy, or second graph-feedback seed was run.
- 2026-05-28: Tightened monitored sample handling after the first verify smoke showed counts but no samples. SQLite-style `YYYY-MM-DD HH:MM:SS` timestamps are now interpreted as UTC for sample-window filtering, and the baseline+verify smoke now reports the same bounded BBC sample rows as the recent overview.
- 2026-05-28: Did not run a new monitored crawl in this pass. The documented smallest `simple-distributed-smoke` profile is remote-backed and the retained `bbc.com` pending queue still blocks broader remote work; existing local profiles are either large fallback crawls or unrelated place-hub workflows. The next safe implementation step is to identify or add a truly tiny local monitored-crawl profile/harness, then run baseline -> crawl -> verify.
- 2026-05-28: Verification after the timestamp fix passed: syntax checks for monitored-small-crawl/helper/dashboard files; focused Jest `tests/tools/crawl/monitored-small-crawl.test.js`, `tests/tools/crawl/remote-queue-summary.test.js`, and `tests/tools/crawl-index.test.js` passed 84 tests; raw SQL/driver scan over monitored/planning files found no new SQL/driver usage; policy, profile dry-run, recent DB, baseline+verify, and direct graph-feedback rejection smokes passed; `git diff --check` and trailing-whitespace scan passed.
- 2026-05-28: Dashboard check-mode smoke initially failed because the unified server can take about 50s to start on this Windows-mounted workspace while the check waited 15s. `unified.server.check.js` now uses configurable `UNIFIED_APP_CHECK_STARTUP_TIMEOUT_MS` with a 60s default, the monitored helper is required lazily inside the Cloud Crawl status path, and `timeout 90s node src/ui/server/unifiedApp/checks/unified.server.check.js` passed.
- 2026-05-28: Added a tiny local monitored-crawl harness. `tools/crawl/monitored-small-crawl.js local-smoke` now emits a no-action plan by default and, with `--execute`, captures baseline DB evidence, runs one local UI-backed BBC URL with page/depth caps and auto-stop, then verifies new DB rows through the existing monitored report path. Added `tools/crawl/profiles/local-tiny-monitored-smoke.json` for launcher dry-run visibility.
- 2026-05-28: The first local-smoke execution exposed two CLI reliability gaps instead of producing verified data: local watch opened SQLite through a direct `better-sqlite3` require that is not available in this repo, and the local watch window started after the launch command returned, allowing fast jobs to be missed. Fixed watch DB opening through `src/db/openNewsCrawlerDb.js`, added `CRAWL_RUN_SERVER_READY_TIMEOUT_MS`, moved the local watch baseline to before launch, and added `--watch-min-fetches` so tiny proof runs cannot stop as "stable" with zero rows.
- 2026-05-28: The final bounded local-smoke execution passed with `readinessLabel:"verified-new-data"`. It wrote `tmp/monitored-local-smoke-report.json`, started one local job on `127.0.0.1:3171`, did not contact remote hosts, and verified DB delta `urls:+1 responses:+1 successResponses:+1 content:+1` with one recent `www.bbc.com` sample (`robots.txt`, HTTP 200). No prune/drain/clear/force-deploy, remote crawl, or second graph-feedback seed was run.
- 2026-05-28: Verification for the tiny local monitored-crawl pass passed: syntax checks for touched JS/test files; focused Jest `tests/tools/crawl/monitored-small-crawl.test.js`, `tests/tools/crawl/run.test.js`, `tests/tools/crawl/crawl-backend.test.js`, `tests/tools/crawl-index.test.js`, and `tests/tools/crawl/remote-queue-summary.test.js` passed 172 tests; local-smoke plan and profile dry-run smokes passed; direct graph-feedback remote flag rejection still exits before contact; unified server check passed. A 30s `recent --hosts www.bbc.com --window-min 60` smoke timed out on the large local DB, but the saved local-smoke report contains the bounded DB persistence proof. Raw SQL/driver scan over the new monitored tool files returned no matches; broader scan only reported the existing `LocalBackend.status()` SQL path and optional old better-sqlite fixture test. `git diff --check` and trailing-whitespace scan passed.
- 2026-05-28: Diagnosed the `monitored-small-crawl recent --hosts www.bbc.com --window-min 60` timeout to the DB-owned `getCloudCrawlRecentEvidence` path, not the local monitor. A timing probe on `data/news.db` showed snapshot around 3s, recent evidence around 8.9s, window samples around 2ms, and recent samples around 7ms before the upstream query fix.
- 2026-05-28: Optimized the upstream `news-crawler-db` cloud-crawl recent evidence query path for mixed SQLite/ISO `fetched_at` rows by using indexed direct timestamp ranges for both storage formats instead of `datetime(replace(r.fetched_at, 'T', ' '))` on the column. Rebuilt `news-crawler-db`; the same `www.bbc.com` 60-minute recent-evidence step dropped to about 5-40ms in local probes while preserving mixed timestamp coverage in `legacyDownloadTooling.test.ts`.
- 2026-05-28: Added bounded DB evidence timings to `monitored-small-crawl` reports and human output. Slow DB evidence steps now produce warnings, so future small-crawl evidence can distinguish crawl failure/no-new-data from a slow proof path. The 30s guarded `recent --hosts www.bbc.com --window-min 60 --limit 5 --json` smoke now exits cleanly and reports `verified-new-data`.
- 2026-05-28: Improved operator scanability in Cloud Crawl without adding a new crawl path. The dashboard health card now includes a compact recent-crawl readiness/count cell driven by `monitoredSmallCrawl`, while the API keeps bounded recent samples as the visible Recent Downloads fallback. Check-mode status now includes the `evidence.queryTimings` contract.
- 2026-05-28: Made `tools/crawl/profiles/local-tiny-monitored-smoke.json` visible to git by adding a narrow `.gitignore` exception for that single profile. The broad local/dynamic profile ignore remains in place, but the tiny monitored smoke profile can now be tracked as part of the repeatable crawler-development loop.
- 2026-05-28: Verification for the recent-evidence/dashboard pass passed: syntax checks for touched monitored/UI files; `news-crawler-db` build; upstream Vitest `legacyDownloadTooling.test.ts` passed 6 tests; focused Jest `monitored-small-crawl.test.js` passed 13 tests and broader crawl Jest passed 5 suites / 173 tests. Smokes passed for `monitored-small-crawl recent --hosts www.bbc.com --window-min 60`, policy/local-smoke plan, local tiny profile dry-run, direct graph-feedback remote rejection, and unified server check. Raw boundary scan found only the existing unified server `better-sqlite3` helper comment in touched UI code; the monitored helper adds no SQL/driver calls. Targeted `git diff --check` and trailing-whitespace scans passed for copilot and `news-crawler-db`.
- 2026-05-28: Added monitored report comparison for the local small-crawl cadence. `tools/crawl/monitored-small-crawl.js compare` reads saved monitored/local-smoke reports only, writes a bounded `monitored-small-crawl-comparison` artifact, and summarizes DB deltas, command/profile identity, no-new-data evidence, DB proof timing regressions, and stable pass/fail evidence without rerunning crawls or contacting remote hosts. A smoke against `tmp/monitored-local-smoke-report.json` wrote `tmp/monitored-local-smoke-comparison.json` and reported `stablePassEvidence.passed:true` with DB delta `successResponses:+1 content:+1`.
- 2026-05-28: Cloud Crawl dashboard/API scanability now includes a compact `monitoredSmallCrawlSummary` with readiness, counts, sample count, max query timing, slow-step count, and blocker/warning counts. The Operator Health recent-crawl cell includes proof timing when available, so slow DB evidence is visible without changing crawl behavior.
- 2026-05-28: The second bounded local-smoke execution on UI port 3172 produced useful failure evidence instead of verified content: the launcher exited 0 after starting one local job, but post-run DB verification saw only `urls:+1` and no response/content rows, so the report is `verification-blocked` with `expected-download-count-not-met` and `db-success-delta-below-expected`. This confirms the comparison/no-new-data path is needed before treating local-smoke runs as a cadence.
- 2026-05-28: Hardened monitored local-smoke CLI exit semantics. `local-smoke --execute` now returns exit code 2 when its bounded verification report has blockers, while still printing/writing the evidence artifact. This keeps blocked DB persistence proof visible to automation without changing the crawl collector or remote behavior.
- 2026-05-28: Verification for the comparison/dashboard pass passed: syntax checks for touched monitored/UI files; focused Jest `tests/tools/crawl/monitored-small-crawl.test.js` passed 16 tests and the broader focused crawl set (`monitored-small-crawl`, `remote-queue-summary`, `crawl-index`) passed 94 tests before the final exit-semantics tweak; direct graph-feedback remote flag rejection still exits before contact; `recent --hosts www.bbc.com --window-min 120` exited cleanly with one recent success and bounded query timings; unified server check passed with `monitoredSmallCrawlSummary`; comparison smokes wrote bounded JSON/text artifacts. Raw SQL/driver scan found only existing `better-sqlite3` comments/troubleshooting strings outside the monitored helper. Targeted `git diff --check` and trailing-whitespace scans passed.
- 2026-05-28: Diagnosed the latest local-smoke failure as a false clean follow-mode result: `run.js` launch returned 0 after starting the operation job, while the local watch loop timed out with zero DB fetches and returned the launch code. `run.js --watch` now exits 2 on timeout, poll-error, and missing-target outcomes, and JSON watch final evidence includes `minFetches` / `minFetchesMet`.
- 2026-05-28: Refined monitored comparison diagnostics for partial persistence. Comparison artifacts now flag URL-only DB deltas, started jobs with no fetch evidence, stale `latestFetchedAt` before the crawl window, missing recent samples, watch-loop timeout, and unmet `--watch-min-fetches`. Local-smoke reports now retain bounded stdout/stderr tails and parsed `watchFinal` evidence so the final watch state is not lost behind the head-only previews. Cloud Crawl `monitoredSmallCrawlSummary` now includes `dataCompletenessLabel`, with `partial-url-only` available for reports that include URL-only deltas.
- 2026-05-28: Post-fix bounded local-smoke validation on UI port 3173 wrote `tmp/monitored-local-smoke-report-watch-fix-20260528.json` and exited `LOCAL_SMOKE_EXIT=2`, proving the watch timeout / no-fetch case is now machine-failing. The report still shows the underlying local persistence issue: `urls:+1`, `responses:+0`, `content:+0`, no recent downloads, and stale latest fetch time. Comparison against the earlier passing report wrote `tmp/monitored-local-smoke-comparison-watch-fix-20260528.json` with `stablePassEvidence.passed:false`, `crawl-command-failed`, URL-only delta, started-without-fetch, no samples, stale latest fetch, and readiness regression diagnostics.
- 2026-05-28: Final verification for the local-watch stabilization pass passed: syntax checks for touched monitored/run/UI files; focused Jest `monitored-small-crawl`, `run`, `crawl-backend`, `crawl-index`, and `remote-queue-summary` passed 5 suites / 178 tests; local-smoke plan, recent DB proof, comparison, direct graph-feedback remote rejection, and unified server check smokes passed. A raw SQL/driver scan only reported the existing `LocalBackend.status()` SELECT and the existing unified-server `better-sqlite3` comment; no new raw SQL/driver path was added. `git diff --check` passed for touched files.
- 2026-05-28: Diagnosed the remaining local-smoke partial-persistence blocker as a proof-path bug, not broad collector failure. The UI crawl log showed `Pages downloaded: 1` and `Articles saved: 1`, and post-run verification showed `responses:+3` / `content:+1`, but `run.js --watch` still timed out because `LocalBackend.status()` queried recent evidence without a `finishedAt` upper bound and therefore saw zero rows.
- 2026-05-28: Fixed local watch proof narrowly. `LocalBackend.status()` now uses `src/data/db/queries/downloadEvidence.getCloudCrawlRecentEvidence()` with bounded `startedAt` and `finishedAt` timestamps; `run.js --watch` skips stale job-registry polling once `--watch-min-fetches` DB proof is met and exits with `stoppedReason:"min-fetches-met"`; local-smoke comparison no longer treats stale running job evidence as a diagnostic when DB proof is satisfied.
- 2026-05-28: Bounded local-smoke validation on UI port 3177 passed. `tmp/monitored-local-smoke-report-db-proof-fixed-20260528.json` reports `readinessLabel:"verified-new-data"`, `LOCAL_SMOKE_EXIT=0`, `watchFinal.minFetchesMet:true`, DB delta `responses:+3`, `successResponses:+3`, `content:+1`, recent downloads 3, and latest `www.bbc.com` sample at `2026-05-28T13:57:37.426Z`. No remote crawl, second seed, prune/drain/clear, force deploy, or collect change was run.
- 2026-05-28: Focused verification for the local-smoke proof-path fix passed: syntax checks for touched JS/tests; focused Jest `monitored-small-crawl`, `run`, and `crawl-backend` passed 3 suites / 104 tests; raw SQL/driver scan over touched monitored/watch files found no raw SQL/direct driver path; local-smoke plan and compare smokes passed; direct graph-feedback remote rejection and dashboard check-mode smokes were run during final verification.
