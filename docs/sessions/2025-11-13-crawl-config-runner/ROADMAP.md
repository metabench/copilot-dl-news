# Roadmap — Crawl Config Runner

## Objectives
1. Baseline `crawl.js` inputs + identify target config format.
2. Implement config loader + CLI glue so base runs only need a config reference.
3. Document workflow and add regression coverage where practical.

## Task Board
- [x] Review `crawl.js`, `config/` directory, and related docs.
- [x] Define config format + precedence rules.
- [x] Update `crawl.js` (and helpers) to load config + merge overrides.
- [ ] Document instructions (README/session docs) and add tests if feasible.
