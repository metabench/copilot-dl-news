# Search Index — Crawl Config Runner

| Keyword | Context | Link |
| --- | --- | --- |
| `crawl-runner.json` | Default manifest that `crawl.js` now loads before falling back to `config.json`. Supports JSON or YAML plus relative paths for sequence configs. | [Working Notes](./WORKING_NOTES.md) |
| `--config` / `CRAWL_CONFIG_PATH` | Flag/env pair that selects a specific runner manifest; CLI overrides layer on top of file-defined defaults. | [Working Notes](./WORKING_NOTES.md) |
