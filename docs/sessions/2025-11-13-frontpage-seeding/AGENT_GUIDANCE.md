# Agent Guidance — Front Page Seeding

- Use `node tools/dev/js-scan.js --search "NewsCrawler" --json` to map entrypoints before editing.
- Prefer modifying planner/adapters over service code; keep queue mutations centralized.
- Record telemetry additions in `WORKING_NOTES.md` and update `SEARCH_INDEX.md` keywords.
