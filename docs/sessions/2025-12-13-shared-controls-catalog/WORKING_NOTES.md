# Working Notes – Update Shared Controls Catalog

## Consolidation

- Added catalog entries for:
	- `SearchFormControl` (`src/ui/controls/SearchFormControl.js`)
	- `MetricCardControl` (`src/ui/controls/MetricCardControl.js`)

## Notes

- Both controls are exported from `src/ui/controls/index.js`.
- Examples avoid depending on literal emoji input when possible.
