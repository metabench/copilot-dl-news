# Follow Ups: Churn Control Salvage

- Add CSS theme coverage for the four promoted controls when they are first integrated into a live UI surface.
- If a host app needs client-side filtering/dropdowns, add activation-focused checks on top of the current SSR render checks.
- Consider replacing inline crawl-control markup in unified panels with `ActionButtonGroupControl`, `OptionPickerControl`, and `CrawlProgressPanelControl` in a later UI consolidation pass.
