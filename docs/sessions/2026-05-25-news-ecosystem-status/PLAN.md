# Plan: news ecosystem status

Objective: Estimate completion percentages for the local news and crawling ecosystem, excluding jsgui3 platform work, and turn the DB-query/analysis focus into an actionable next plan.

Done when:
- Current relevant repos and recent crawler docs are inspected.
- Major subsystem percentages are estimated with confidence notes.
- Gaps and next priorities are called out plainly.
- DB query boundary and graph-analysis priorities are captured in a durable session artifact.

Change set: session notes only.

Risks/assumptions: Percentages are engineering estimates from local files, tests, docs, and recent session state; they are not product-management commitments.

Tests: None; this is a discovery/status task.

Docs to update: `docs/sessions/SESSIONS_HUB.md`, this session folder.
