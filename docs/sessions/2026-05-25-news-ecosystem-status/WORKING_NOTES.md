# Working Notes

- 2026-05-25: Started cross-repo status estimate for news/crawling ecosystem, excluding jsgui3 platform work.
- Read `tools/crawl/AGENT.md`; current crawl tooling has working local/remote dispatcher, remote status/watch, deploy preflight, sync/prune/ledger, and 73 focused crawl CLI/backend tests documented there.
- Reviewed `docs/sessions/2026-05-10-news-crawler-db-boundary/STATUS.md`; active DB ownership migration is marked complete for active runtime/UI/check/tool paths, with 125 broad residual SQL/driver-pattern matches classified as generated/static/docs/dev-tool/non-DB categories.
- Reviewed `docs/sessions/2026-05-12-twelve-site-100-page-crawl/WORKING_NOTES.md`; latest substantial crawl exceeded target with 14 hosts at >=100 successful local pages, 6007 URL rows and 1720 content records synced before drain.
- Remote status check succeeded: multi-domain crawl server v4, schema version 4, 10 configured domains, 0 running, idle/healthy shield, 0 pending/fetched/stored in current window.
- Local `npm run db:downloads:*` commands failed in this WSL shell because `../news-crawler-db/node_modules/better-sqlite3/build/Release/better_sqlite3.node` has an invalid ELF header. Treat local live DB stats as unavailable until native deps are rebuilt.
- Package scan: backend-core has 18 test files / 96 tests from Phase 8 verification; news-crawler-db has 223 SQLite access files and 124 db tests; intelligence modules now have real source/tests despite older checklist saying URL/document were empty.
- Added `NEWS_ECOSYSTEM_COMPLETION_ESTIMATE.md` with subsystem percentages and `DB_QUERY_AND_ANALYSIS_FOCUS.md` with the recommended DB-boundary/graph-analysis implementation path.
- Rebuilt `better-sqlite3` in `../news-crawler-db` with `npm rebuild better-sqlite3`; recent-download and host breakdown commands now work from this repo.
- `npm run db:downloads:recent` showed the latest 25 downloads on 2026-05-13, mostly Guardian and BBC rows.
- `npm run db:downloads:hosts` showed substantial corpus volume, including `www.theguardian.com` with 92,215 downloads and `www.bbc.com` with 48,856 downloads.
- Stopped the broad `npm run db:downloads:stats` scan after it continued running against the large DB; use bounded/indexed stats paths for normal agent fast answers.
- Added `GRAPH_ACCESS_AND_ANALYSIS_IMPLEMENTATION_PLAN.md` and `GRAPH_ACCESS_RECURSIVE_PROMPT_BOOTSTRAP.md`.
- Reviewed and improved the GraphAccess plan: clarified local camelCase conventions, optional `graph` adapter rollout, SQLite-first implementation, legacy raw SQL boundary in `news-db-analysis`, exact Vitest/build commands, no initial schema migration, and a safer 16-step candidate split.
- Created `GRAPH_ACCESS_16_STEP_RECURSIVE_PLAN.md` as the authoritative 16-step recursive execution plan. Step 1 is a readiness audit only; implementation starts in later steps.
- Completed Step 1 readiness audit and saved `GRAPH_ACCESS_STEP_01_READINESS_AUDIT.md`. Findings: no sibling `AGENT.md` files found; `news-crawler-db` and `news-db-analysis` baseline builds pass; `news-db-pure-analysis` build fails before compile due missing Rollup optional native package `@rollup/rollup-linux-x64-gnu`; Step 2 should add DTO/option types only in `news-crawler-db/src/db/types.ts` and avoid `src/db/sqlite/access/index.ts` because it is UTF-16LE.
- Completed Step 2 by adding graph DTO and option interfaces to `../news-crawler-db/src/db/types.ts`: `GraphPageEdge`, `GraphPageEdgeOptions`, `GraphHostEdgeCount`, `HostEdgeCountOptions`, `GraphPageRankRow`, `PageRankOptions`, `SiteGraphSummary`, `SiteGraphOptions`, `PageDiscoveryOptions`, `HubCandidateOptions`, `CrawlPriorityFeatureRow`, and `CrawlPriorityFeatureOptions`. No `GraphAccess` interface, adapter wiring, schema changes, or SQLite implementation were added. Verification passed with `npm run build` in `news-crawler-db`.
- Completed Step 3 by adding the read-only `GraphAccess` interface to `../news-crawler-db/src/db/types.ts` and adding optional `graph?: GraphAccess` to `DbAdapter`. No SQLite fixtures, adapter instantiation, implementation files, Postgres parity work, or schema changes were added. Verification passed with `npm run build` in `news-crawler-db`.
- Completed Step 4 by adding `../news-crawler-db/src/db/__tests__/graphAccess.test.ts`. The fixture seeds `example.com` home, section, article, dead-end article, orphan, external URL, internal links, one external link, and one nullable destination edge. Active tests verify the fixture graph shape and confirm `db.graph` remains unwired; the planned `GraphAccess` methods are recorded as TODO tests for Steps 5-10. No `SqliteGraphAccess` behavior, adapter wiring, schema changes, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 2 passing tests and 10 TODO tests.
- Completed Step 5 by adding `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.iteratePageEdges(options?)` only. The method is read-only, projects explicit link/source-url/target-url columns, preserves nullable targets and URL/host fields, supports source/target host filters, `urlId`, internal/external/missing-target flags, `afterLinkId`, `limit`, and `batchSize`, and uses keyset pagination. Updated `graphAccess.test.ts` to activate only the two `iteratePageEdges` contract tests; later graph methods remain TODO. No schema changes, production adapter wiring, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 4 passing tests and 8 TODO tests.
- Completed Step 6 by extending `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.getSiteGraphSummary(host, options?)` only. Summary semantics are now explicit: page scope is URL rows for the host, optionally restricted to URLs with at least one `http_responses` row when `fetchedOnly` is true; `fetchedPageCount` is the count of host URLs with at least one `http_responses` row; edge counts are outgoing included internal/external edges from the scoped host pages and exclude null/unresolved targets; orphan, dead-end, and candidate hub counts use the same page scope and edge policy; default hub thresholds are 2 internal outbound edges and 2 total outbound edges. Updated `graphAccess.test.ts` to activate only `getSiteGraphSummary` coverage; later methods remain TODO. No schema changes, production adapter wiring, Postgres work, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 6 passing tests and 7 TODO tests.
- Completed Step 7 by extending `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.getHostEdgeCounts(options?)` only. The query groups existing link rows by resolved source host and nullable target host, returns edge count, distinct source URL count, distinct target URL count, and `internal`, preserves null/unresolved target groups with `uniqueTargetPageCount: 0`, supports source/target host filters, internal/external/missing-target flags, and `limit`, and uses explicit projected columns. Updated `graphAccess.test.ts` to activate only `getHostEdgeCounts` coverage; later page ranking and discovery methods remain TODO. No schema changes, production adapter wiring, Postgres work, Step 8 ranking, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 8 passing tests and 6 TODO tests.
- Completed Step 8 by extending `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.rankPagesByInboundLinks(host, options?)` and `rankPagesByOutboundLinks(host, options?)` only. Ranking rows use the existing `GraphPageRankRow` shape with URL metadata and internal/external inbound/outbound counts. Internal/external options zero out excluded buckets; `minInboundCount` and `minOutboundCount` apply to the included totals; inbound ranking orders by included inbound total, included outbound total, then URL id; outbound ranking orders by included outbound total, included inbound total, then URL id. Added focused ranking tests for default ordering, limit/offset, internal/external filtering, and min-count filters. No schema changes, production adapter wiring, Postgres work, Step 9 discovery methods, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 10 passing tests and 4 TODO tests.
- Completed Step 9 by extending `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.findOrphanPages(host, options?)` and `findDeadEndPages(host, options?)` only. Both methods return `GraphPageRankRow` with URL metadata and internal/external inbound/outbound counts. Root/home pages are included by default because the generic URL graph has no durable seed-page marker; callers can use `excludeRootPath` to remove root-like URLs. Orphans are pages with zero included inbound links; dead ends are pages with zero included outbound links; missing/null target links do not count as resolved outbound connectivity. Options cover `limit`, `offset`, `fetchedOnly`, `excludeRootPath`, `includeInternal`, and `includeExternal`, with deterministic ordering by opposite connectivity total and URL id. No schema changes, production adapter wiring, Postgres work, Step 10 hub/crawl-priority methods, or UTF-16LE index edits were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 12 passing tests and 2 TODO tests.
- Completed Step 10 by extending `../news-crawler-db/src/db/sqlite/access/graph.ts` with `SqliteGraphAccess.findHubCandidates(host, options?)` and `iterateCrawlPriorityFeatures(host, options?)` only. Hub candidates use transparent included-link thresholds, defaulting to at least 2 same-host outbound links and 2 included outbound links total, and sort deterministically by a simple graph score then URL id. Crawl-priority feature iteration is read-only, keyset-paginated by URL id, and returns bounded rows with URL metadata, latest response metadata, content presence, stale/missing-content flags, graph counts, transparent `priorityScore`, and signal labels. `CrawlPriorityFeatureOptions` now also accepts `staleFetchedBefore`, `includeInternal`, and `includeExternal` while preserving `staleBefore`. No derived results are persisted; no writes, schema changes, production adapter wiring, Postgres work, or UTF-16LE index edits were added. The graph access SQL still avoids `SELECT *`; `git diff --check` passed for touched DB files. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 14 passing tests.
- Completed Step 11 by wiring `SqliteGraphAccess` onto the real `createSqliteAdapter()` result and the in-memory `createTestDb()` helper as `db.graph`. Exported `SqliteGraphAccess` through `src/db/sqlite/index.ts` and `src/db/index.ts`; did not touch the UTF-16LE `src/db/sqlite/access/index.ts`. Updated `docs/ACCESS_API.md`, `docs/DATABASE_SCHEMA.md`, and `docs/README.md`; added `docs/decisions/2026-05-25-graph-access-sqlite-first.md` documenting the SQLite-first decision and leaving Postgres without `graph` because `DbAdapter.graph` is optional. No schema changes, writes, Postgres implementation, or graph-query behavior changes were added. Verification passed with `npm run build` and `npx vitest run src/db/__tests__/graphAccess.test.ts` in `news-crawler-db` with 15 passing tests. The broader saved-plan command `npx vitest run src/db/__tests__` was also run: graph tests passed, but the overall run failed with one independent `src/db/__tests__/unit/sqlite/taskEvents.test.ts` assertion (`previewPruneTaskEventsOlderThan(1)` expected dry-run count 0/tasks 0 but returned count 2/tasks 1). Running that single task-events file reproduced the same failure, so it is documented as outside Step 11 scope.
- Completed Step 12 in `../news-db-analysis` by adding `src/interfaces/GraphAccess.ts` with local GraphAccess-compatible DTOs/access contract and `requireGraphAccess(db)`. Added optional `graph?: GraphAccess` to `NewsDbAdapter`, exported the guard and graph types from `src/index.ts`, and added `tests/WebsiteGraphAnalysisService.test.ts` covering successful graph resolution plus a clear missing-capability error that does not call `query` or `execute`. No `WebsiteGraphAnalysisService` behavior, raw SQL, service methods, or Step 13 implementation were added. `news-db-analysis` had many pre-existing modified files; Step 12 touched only `src/interfaces/GraphAccess.ts`, `src/interfaces/NewsDbAdapter.ts`, `src/index.ts`, and `tests/WebsiteGraphAnalysisService.test.ts`. Verification passed with `npm run build` and `npx vitest run tests/WebsiteGraphAnalysisService.test.ts` in `news-db-analysis` with 2 passing tests. The first Vitest attempt failed before test collection because Rollup's optional native package `@rollup/rollup-linux-x64-gnu` was missing from `node_modules`; `npm install --no-save @rollup/rollup-linux-x64-gnu@4.55.1` repaired the local install, after which the focused test passed. `git diff --check` passed for the Step 12 source/test files.
- Completed Step 13 in `../news-db-analysis` by adding `src/services/WebsiteGraphAnalysisService.ts` with constructor graph-capability guarding plus `summarizeSiteGraph(host, options?)`, `getPageAdjacency(host, options?)`, and `getCrossDomainLinking(host, options?)`. The service uses only typed `GraphAccess` methods, derives analysis-shaped summary ratios, groups streamed page edges into source-page adjacency, and summarizes resolved cross-domain host targets plus unresolved target counts without `db.query` or `db.execute`. Exported the service and result/option types from `src/index.ts`, and expanded `tests/WebsiteGraphAnalysisService.test.ts` with mocked graph-access coverage for summary ratios, adjacency grouping, cross-domain shares, and the existing missing-capability guard. Step 14 ranking/hub/orphan/dead-end/priority methods were not implemented. Verification passed with `npm run build` and `npx vitest run tests/WebsiteGraphAnalysisService.test.ts` in `news-db-analysis` with 5 passing tests; `rg` found no raw query/execute calls in the new service or focused test, and `git diff --check` passed for Step 13 source/test files.
- Completed Step 14 in `../news-db-analysis` by extending `WebsiteGraphAnalysisService` with `rankPagesByGraphImportance(host, options?)`, `findHubCandidates(host, options?)`, `findOrphanPages(host, options?)`, `findDeadEndPages(host, options?)`, and `buildCrawlPriorityDataset(host, options?)`. Ranking combines typed inbound/outbound graph accessor results with explicit weights and deterministic tie-breaks; hub, orphan, and dead-end methods wrap typed graph results with analysis metadata; crawl-priority datasets materialize bounded feature rows with streamable manifest metadata. No `news-db-pure-analysis` dependency was added, and the service still avoids `db.query` and `db.execute`. Exported the new option/result types from `src/index.ts` and expanded `tests/WebsiteGraphAnalysisService.test.ts` to 9 mocked-graph tests. Verification passed in `news-db-analysis`: `npm run build`, `npx vitest run tests/WebsiteGraphAnalysisService.test.ts` with 9 passing tests, and `npm run test:run` with 19 files / 155 tests passing. `rg` found no raw query/execute calls in the graph service or focused test, and `git diff --check` passed for the Step 14 source/test files.
- Completed Step 15 as an explicit deferral in `../news-db-pure-analysis`. After reviewing Step 14, the graph scoring now in `WebsiteGraphAnalysisService` is a thin presentation layer over typed DB graph counts: weighted inbound/outbound importance, hub score/signal labels, orphan/dead-end wrappers, and crawl-priority dataset manifest metadata. Extracting those helpers into `news-db-pure-analysis` now would add package churn and cross-package API coordination before graph-analysis consumers stabilize, so no pure-analysis source, tests, exports, package files, DB access, IO, persistence, schema, or crawler integration changes were made. Verification followed the deferred path: the first `npm run build` failed because Rollup's optional native package `@rollup/rollup-linux-x64-gnu` was missing from local `node_modules`; `npm install --no-save @rollup/rollup-linux-x64-gnu@4.55.1` repaired the local install without package file changes, and the rerun of `npm run build` passed. `git status --short` in `news-db-pure-analysis` remained clean after the build.
- Completed Step 16 final cross-repo verification and documentation. Updated `../news-crawler-db/docs/ACCESS_API.md` and `docs/DATABASE_SCHEMA.md` to clarify GraphAccess as a read-only, no-derived-storage graph read model consumed by downstream analysis; updated `../news-db-analysis/docs/04-types-and-interfaces.md` and `docs/06-services.md` to document optional `graph?: GraphAccess`, `requireGraphAccess(db)`, graph DTOs, and `WebsiteGraphAnalysisService`; and appended the LT-001 long-term notes because the graph surface is now relevant to crawler feedback and operator-facing graph intelligence. Verification results: `news-crawler-db` `npm run build` passed; `npx vitest run src/db/__tests__/graphAccess.test.ts` passed with 15 tests; broad `npx vitest run src/db/__tests__` reproduced the known unrelated `src/db/__tests__/unit/sqlite/taskEvents.test.ts` dry-run assertion failure while 123 files / 720 tests passed and graph tests passed; `news-db-analysis` `npm run build` passed; `npm run test:run` passed with 19 files / 155 tests; `copilot-dl-news` `npm run db:downloads:hosts` and `npm run db:downloads:recent` passed, with current top hosts including `www.theguardian.com` at 92,215 downloads and `www.bbc.com` at 48,856 downloads, and latest rows on 2026-05-13. Confirmed no schema migration, graph cache table, or large derived DB storage was added; `rg` found no `db.query`/`db.execute` calls in `WebsiteGraphAnalysisService` or its focused tests. Known verification noise: repeated legacy news-source bootstrap warnings in the broad DB suite, plus the unrelated task-events dry-run count failure already documented in Step 11.
