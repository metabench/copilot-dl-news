# Follow Ups – Docs Memory MCP

- Add POST/PUT endpoints so brainstorm agent can push FOLLOW_UPS / LESSONS updates through MCP rather than editing files directly.
- Expand memory surface to include AGENTS.md excerpts + AGI session indices once consumers request them.
