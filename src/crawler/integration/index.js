'use strict';

/**
 * Integration module for new abstractions.
 *
 * Provides adapters for migrating to new crawler abstractions.
 *
 * @module crawler/integration
 */

const NewAbstractionsAdapter = require('./NewAbstractionsAdapter');

module.exports = {
  NewAbstractionsAdapter
};
