'use strict';

const {
  slugifyCountryName,
  normalizeHostVariants,
  buildInClausePlaceholders,
  safeParseJson
} = require('news-crawler-db');

module.exports = {
  slugifyCountryName,
  normalizeHostVariants,
  buildInClausePlaceholders,
  safeParseJson
};
