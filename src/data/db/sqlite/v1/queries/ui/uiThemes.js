'use strict';

const {
  resolveDbHandle,
  ensureUiThemesTable,
  ensureSystemThemes,
  listThemes,
  getThemeRow,
  getDefaultThemeRow,
  createTheme,
  updateTheme,
  setDefaultTheme,
  deleteTheme
} = require('news-crawler-db');

module.exports = {
  resolveDbHandle,
  ensureUiThemesTable,
  ensureSystemThemes,
  listThemes,
  getThemeRow,
  getDefaultThemeRow,
  createTheme,
  updateTheme,
  setDefaultTheme,
  deleteTheme
};
