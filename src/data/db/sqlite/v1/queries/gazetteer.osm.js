'use strict';

const {
  createOsmBoundaryStatements,
  listBoundaryCandidates,
  saveBoundaryData
} = require('news-crawler-db');

module.exports = {
  createOsmBoundaryStatements,
  listBoundaryCandidates,
  saveBoundaryData
};
