'use strict';

const {
  createAttributeStatements,
  recordAttribute,
  recordAttributes
} = require('news-crawler-db');

module.exports = {
  createAttributeStatements,
  recordAttribute,
  recordAttributes
};
