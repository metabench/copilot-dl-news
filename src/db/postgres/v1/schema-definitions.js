/**
 * AUTO-GENERATED by tools/schema-sync-postgres.js
 * Generated at 2026-01-05T00:19:17.416Z
 *
 * Postgres schema definitions translated from SQLite.
 */
'use strict';

const TABLE_STATEMENTS = [
  // analysis_run_events
  `CREATE TABLE IF NOT EXISTS analysis_run_events (
  id SERIAL PRIMARY KEY,
  run_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  stage TEXT ,
  message TEXT ,
  details TEXT ,
  FOREIGN KEY(run_id) REFERENCES analysis_runs(id) ON DELETE CASCADE
)`,

  // analysis_runs
  `CREATE TABLE IF NOT EXISTS analysis_runs (
  id TEXT PRIMARY KEY,
  started_at TEXT NOT NULL,
  ended_at TEXT ,
  status TEXT NOT NULL,
  stage TEXT ,
  analysis_version INTEGER ,
  page_limit INTEGER ,
  domain_limit INTEGER ,
  skip_pages INTEGER ,
  skip_domains INTEGER ,
  dry_run INTEGER ,
  verbose INTEGER ,
  summary TEXT ,
  last_progress TEXT ,
  error TEXT ,
  background_task_id INTEGER ,
  background_task_status TEXT 
)`,

  // api_keys
  `CREATE TABLE IF NOT EXISTS api_keys (
  id SERIAL PRIMARY KEY,
  -- TEXT identification (never store plaintext - this is the hash) key_hash TEXT NOT NULL UNIQUE,
  key_prefix TEXT NOT NULL,
  -- TEXT 8 chars of key for identification (e.g., "dlnews_a1") -- Access control tier TEXT NOT NULL DEFAULT 'free' CHECK(tier IN ('free', 'premium', 'unlimited')),
  -- TEXT name TEXT,
  -- TEXT friendly name for the key owner_email TEXT,
  -- TEXT owner email created_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_used_at TEXT ,
  -- TEXT limiting (daily counters, reset at midnight UTC) requests_today INTEGER NOT NULL DEFAULT 0,
  requests_reset_date TEXT ,
  -- TEXT when requests_today was last reset (YYYY-MM-DD) -- Status is_active INTEGER NOT NULL DEFAULT 1,
  revoked_at TEXT ,
  revoke_reason TEXT 
)`,

  // article_place_relations
  `CREATE TABLE IF NOT EXISTS article_place_relations (
  id SERIAL PRIMARY KEY,
  article_id INTEGER NOT NULL,
  place_id INTEGER NOT NULL,
  relation_type TEXT NOT NULL CHECK(relation_type IN ('primary', 'secondary', 'mentioned', 'affected', 'origin')),
  confidence DOUBLE PRECISION NOT NULL CHECK(confidence >= 0.0 AND confidence <= 1.0),
  matching_rule_level INTEGER NOT NULL DEFAULT 0,
  evidence TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY (article_id) REFERENCES http_responses(id) ON DELETE CASCADE,
  FOREIGN KEY (place_id) REFERENCES places(id) ON DELETE CASCADE,
  UNIQUE(article_id, place_id, matching_rule_level)
)`,

  // article_places
  `CREATE TABLE IF NOT EXISTS article_places (
  id SERIAL PRIMARY KEY,
  place TEXT NOT NULL,
  place_kind TEXT ,
  -- TEXT | region | city | other method TEXT,
  -- TEXT | heuristic | other source TEXT,
  -- TEXT | text | metadata offset_start INTEGER,
  offset_end INTEGER ,
  context TEXT ,
  first_seen_at TEXT ,
  article_url_id INTEGER NOT NULL REFERENCES urls(id),
  UNIQUE(article_url_id, place, source, offset_start, offset_end)
)`,

  // article_xpath_patterns
  `CREATE TABLE IF NOT EXISTS article_xpath_patterns (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  domain_id INTEGER REFERENCES domains(id),
  xpath TEXT NOT NULL,
  confidence DOUBLE PRECISION ,
  learned_from TEXT ,
  learned_at TEXT ,
  sample_text_length INTEGER ,
  paragraph_count INTEGER ,
  usage_count INTEGER NOT NULL DEFAULT 0,
  last_used_at TEXT ,
  metadata TEXT 
)`,

  // background_tasks
  `CREATE TABLE IF NOT EXISTS background_tasks (
  id SERIAL PRIMARY KEY,
  task_type TEXT NOT NULL,
  status TEXT NOT NULL CHECK(status IN ('pending', 'resuming', 'running', 'paused', 'completed', 'failed', 'cancelled', 'abandoned')),
  progress_current INTEGER DEFAULT 0,
  progress_total INTEGER DEFAULT 0,
  progress_message TEXT ,
  config TEXT ,
  metadata TEXT ,
  error_message TEXT ,
  created_at TEXT NOT NULL,
  started_at TEXT ,
  updated_at TEXT NOT NULL,
  completed_at TEXT ,
  resume_started_at TEXT ,
  cancellation_reason TEXT 
)`,

  // bucket_entries
  `CREATE TABLE IF NOT EXISTS bucket_entries (
  id SERIAL PRIMARY KEY,
  bucket_id INTEGER NOT NULL REFERENCES compression_buckets(id),
  entry_key TEXT NOT NULL,
  uncompressed_size INTEGER NOT NULL,
  compressed_size INTEGER NOT NULL,
  offset INTEGER NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(bucket_id, entry_key)
)`,

  // classification_types
  `CREATE TABLE IF NOT EXISTS classification_types (
  id SERIAL PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  display_name TEXT NOT NULL,
  emoji TEXT ,
  description TEXT ,
  category TEXT ,
  sort_order INTEGER DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // compression_buckets
  `CREATE TABLE IF NOT EXISTS compression_buckets (
  id SERIAL PRIMARY KEY,
  bucket_type TEXT NOT NULL,
  domain_pattern TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  finalized_at TEXT ,
  content_count INTEGER DEFAULT 0,
  uncompressed_size INTEGER DEFAULT 0,
  compressed_size INTEGER DEFAULT 0,
  compression_ratio DOUBLE PRECISION ,
  compression_type_id INTEGER REFERENCES compression_types(id),
  bucket_blob BYTEA ,
  index_json TEXT ,
  status TEXT 
)`,

  // compression_status
  `CREATE TABLE IF NOT EXISTS compression_status (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  last_analyzed_at TEXT ,
  analysis_version INTEGER ,
  analysed_pages INTEGER DEFAULT 0,
  pages_updated INTEGER DEFAULT 0,
  skipped_pages INTEGER DEFAULT 0,
  last_run_summary TEXT ,
  total_items INTEGER DEFAULT 0,
  uncompressed_items INTEGER DEFAULT 0,
  total_uncompressed_bytes INTEGER DEFAULT 0,
  total_compressed_bytes INTEGER DEFAULT 0,
  total_space_saved_bytes INTEGER DEFAULT 0,
  avg_compression_ratio DOUBLE PRECISION ,
  compression_types_json TEXT ,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // compression_types
  `CREATE TABLE IF NOT EXISTS compression_types (
  id SERIAL PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  algorithm TEXT NOT NULL,
  level INTEGER NOT NULL,
  mime_type TEXT ,
  extension TEXT ,
  memory_mb INTEGER DEFAULT 0,
  window_bits INTEGER ,
  block_bits INTEGER ,
  description TEXT 
)`,

  // content_analysis
  `CREATE TABLE IF NOT EXISTS content_analysis (
  id SERIAL PRIMARY KEY,
  content_id INTEGER NOT NULL REFERENCES content_storage(id),
  analysis_version INTEGER NOT NULL DEFAULT 1,
  classification TEXT ,
  title TEXT ,
  date TEXT ,
  section TEXT ,
  word_count INTEGER ,
  language TEXT ,
  article_xpath TEXT ,
  nav_links_count INTEGER ,
  article_links_count INTEGER ,
  analysis_json TEXT ,
  analyzed_at TEXT NOT NULL DEFAULT (datetime('now')),
  confidence_score DOUBLE PRECISION ,
  body_text TEXT ,
  byline TEXT ,
  authors TEXT 
)`,

  // content_storage
  `CREATE TABLE IF NOT EXISTS content_storage (
  id SERIAL PRIMARY KEY,
  storage_type TEXT NOT NULL,
  compression_type_id INTEGER REFERENCES compression_types(id),
  compression_bucket_id INTEGER REFERENCES compression_buckets(id),
  bucket_entry_key TEXT ,
  content_blob BYTEA ,
  content_sha256 TEXT ,
  uncompressed_size INTEGER ,
  compressed_size INTEGER ,
  compression_ratio DOUBLE PRECISION ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  http_response_id INTEGER REFERENCES http_responses(id),
  content_category TEXT ,
  content_subtype TEXT 
)`,

  // coverage_gaps
  `CREATE TABLE IF NOT EXISTS coverage_gaps (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  gap_type TEXT NOT NULL,
  gap_identifier TEXT NOT NULL,
  gap_description TEXT ,
  priority_score DOUBLE PRECISION DEFAULT 0,
  first_detected TEXT NOT NULL,
  last_updated TEXT NOT NULL,
  resolution_status TEXT DEFAULT 'open',
  resolution_method TEXT ,
  resolved_at TEXT ,
  attempts_count INTEGER DEFAULT 0,
  metadata TEXT ,
  UNIQUE(job_id, gap_type, gap_identifier)
)`,

  // coverage_snapshots
  `CREATE TABLE IF NOT EXISTS coverage_snapshots (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  snapshot_time TEXT NOT NULL,
  domain TEXT NOT NULL,
  total_hubs_expected INTEGER ,
  total_hubs_discovered INTEGER ,
  coverage_percentage DOUBLE PRECISION ,
  gap_count INTEGER ,
  active_problems INTEGER ,
  milestone_count INTEGER ,
  telemetry_data TEXT ,
  created_at TEXT DEFAULT (datetime('now'))
)`,

  // crawl_jobs
  `CREATE TABLE IF NOT EXISTS crawl_jobs (
  id TEXT PRIMARY KEY,
  args TEXT ,
  pid INTEGER ,
  started_at TEXT ,
  ended_at TEXT ,
  status TEXT ,
  crawl_type_id INTEGER ,
  url_id INTEGER ,
  paused_at TEXT ,
  pause_reason TEXT ,
  resume_at TEXT ,
  pause_count INTEGER DEFAULT 0,
  total_paused_ms INTEGER DEFAULT 0
)`,

  // crawl_milestones
  `CREATE TABLE IF NOT EXISTS crawl_milestones (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  kind TEXT NOT NULL,
  scope TEXT ,
  target TEXT ,
  message TEXT ,
  details TEXT 
)`,

  // crawl_pause_history
  `CREATE TABLE IF NOT EXISTS crawl_pause_history (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  action TEXT NOT NULL CHECK (action IN ('pause', 'resume')),
  reason TEXT ,
  triggered_by TEXT DEFAULT 'user',
  paused_at TEXT ,
  resumed_at TEXT ,
  duration_ms INTEGER ,
  queue_size_at_pause INTEGER ,
  visited_at_pause INTEGER ,
  created_at TEXT DEFAULT (datetime('now')),
  FOREIGN KEY(job_id) REFERENCES crawl_jobs(id) ON DELETE CASCADE
)`,

  // crawl_problems
  `CREATE TABLE IF NOT EXISTS crawl_problems (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  kind TEXT NOT NULL,
  scope TEXT ,
  target TEXT ,
  message TEXT ,
  details TEXT 
)`,

  // crawl_queue
  `CREATE TABLE IF NOT EXISTS crawl_queue (
  id SERIAL PRIMARY KEY,
  url TEXT NOT NULL UNIQUE,
  domain TEXT NOT NULL,
  priority INTEGER DEFAULT 0,
  depth INTEGER DEFAULT 0,
  status TEXT DEFAULT 'pending',
  parent_url TEXT ,
  retry_count INTEGER DEFAULT 0,
  worker_id TEXT ,
  error_message TEXT ,
  meta TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
)`,

  // crawl_skip_terms
  `CREATE TABLE IF NOT EXISTS crawl_skip_terms (
  id SERIAL PRIMARY KEY,
  lang TEXT NOT NULL,
  term TEXT NOT NULL,
  normalized TEXT NOT NULL,
  reason TEXT ,
  source TEXT ,
  metadata JSONB 
)`,

  // crawl_tasks
  `CREATE TABLE IF NOT EXISTS crawl_tasks (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  host TEXT ,
  kind TEXT ,
  status TEXT NOT NULL DEFAULT 'pending',
  url TEXT ,
  payload TEXT ,
  note TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(job_id) REFERENCES crawl_jobs(id) ON DELETE CASCADE
)`,

  // crawl_types
  `CREATE TABLE IF NOT EXISTS crawl_types (
  id SERIAL PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  description TEXT ,
  declaration TEXT NOT NULL -- JSON string describing flags/behavior
)`,

  // crawler_settings
  `CREATE TABLE IF NOT EXISTS crawler_settings (
  key TEXT PRIMARY KEY,
  value TEXT ,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // cross_crawl_knowledge
  `CREATE TABLE IF NOT EXISTS cross_crawl_knowledge (
  id SERIAL PRIMARY KEY,
  source_domain TEXT NOT NULL,
  knowledge_type TEXT NOT NULL,
  knowledge_key TEXT NOT NULL,
  knowledge_value TEXT NOT NULL,
  confidence_level DOUBLE PRECISION NOT NULL,
  usage_count INTEGER DEFAULT 0,
  last_used TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  UNIQUE(source_domain, knowledge_type, knowledge_key)
)`,

  // dashboard_metrics
  `CREATE TABLE IF NOT EXISTS dashboard_metrics (
  job_id TEXT NOT NULL,
  metric_name TEXT NOT NULL,
  metric_value DOUBLE PRECISION NOT NULL,
  metric_unit TEXT ,
  timestamp TEXT NOT NULL,
  aggregation_period TEXT DEFAULT 'instant',
  metadata TEXT ,
  PRIMARY KEY (job_id, metric_name, timestamp)
) `,

  // discovery_events
  `CREATE TABLE IF NOT EXISTS discovery_events (
  id SERIAL PRIMARY KEY,
  url_id INTEGER NOT NULL REFERENCES urls(id),
  discovered_at TEXT NOT NULL,
  referrer_url TEXT ,
  crawl_depth INTEGER ,
  discovery_method TEXT ,
  crawl_job_id TEXT REFERENCES crawl_jobs(id)
)`,

  // domain_categories
  `CREATE TABLE IF NOT EXISTS domain_categories (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  description TEXT 
)`,

  // domain_category_map
  `CREATE TABLE IF NOT EXISTS domain_category_map (
  domain_id INTEGER NOT NULL,
  category_id INTEGER NOT NULL,
  PRIMARY KEY (domain_id, category_id),
  FOREIGN KEY (domain_id) REFERENCES domains(id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES domain_categories(id) ON DELETE CASCADE
)`,

  // domain_classification_profiles
  `CREATE TABLE IF NOT EXISTS domain_classification_profiles (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL UNIQUE,
  article_pattern TEXT ,
  hub_pattern TEXT ,
  nav_pattern TEXT ,
  common_sections TEXT ,
  date_path_format TEXT ,
  slug_characteristics TEXT ,
  verified_article_count INTEGER DEFAULT 0,
  verified_hub_count INTEGER DEFAULT 0,
  verified_nav_count INTEGER DEFAULT 0,
  profile_confidence DOUBLE PRECISION DEFAULT 0.0,
  last_updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // domain_locales
  `CREATE TABLE IF NOT EXISTS domain_locales (
  host TEXT PRIMARY KEY,
  country_code TEXT ,
  primary_langs TEXT ,
  -- TEXT or JSON of language tags confidence REAL,
  source TEXT ,
  updated_at TEXT 
)`,

  // domain_rate_events
  `CREATE TABLE IF NOT EXISTS domain_rate_events (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  event_type TEXT NOT NULL,
  rpm_at_time INTEGER ,
  http_status INTEGER ,
  retry_after_ms INTEGER ,
  created_at TEXT DEFAULT (datetime('now'))
)`,

  // domain_rate_limits
  `CREATE TABLE IF NOT EXISTS domain_rate_limits (
  domain TEXT PRIMARY KEY,
  learned_rpm INTEGER DEFAULT 30,
  safe_rpm INTEGER ,
  max_observed_rpm INTEGER ,
  min_safe_rpm INTEGER ,
  last_429_at TEXT ,
  last_success_at TEXT ,
  total_requests INTEGER DEFAULT 0,
  total_429s INTEGER DEFAULT 0,
  success_streak INTEGER DEFAULT 0,
  err_429_streak INTEGER DEFAULT 0,
  backoff_until TEXT ,
  crawl_delay_seconds INTEGER ,
  source TEXT DEFAULT 'learned',
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
)`,

  // domains
  `CREATE TABLE IF NOT EXISTS domains (
  id SERIAL PRIMARY KEY,
  host TEXT NOT NULL UNIQUE,
  tld TEXT ,
  created_at TEXT ,
  last_seen_at TEXT ,
  analysis TEXT 
)`,

  // errors
  `CREATE TABLE IF NOT EXISTS errors (
  id INTEGER PRIMARY KEY,
  host TEXT ,
  kind TEXT ,
  code INTEGER ,
  message TEXT ,
  details TEXT ,
  at TEXT ,
  url_id INTEGER 
)`,

  // gap_predictions
  `CREATE TABLE IF NOT EXISTS gap_predictions (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  predicted_url TEXT NOT NULL,
  prediction_source TEXT NOT NULL,
  confidence_score DOUBLE PRECISION NOT NULL,
  gap_type TEXT ,
  expected_coverage_lift DOUBLE PRECISION ,
  validation_status TEXT DEFAULT 'pending',
  validation_result TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  validated_at TEXT ,
  UNIQUE(job_id, predicted_url)
)`,

  // gazetteer_crawl_state
  `CREATE TABLE IF NOT EXISTS gazetteer_crawl_state (
  id SERIAL PRIMARY KEY,
  stage TEXT NOT NULL,
  -- TEXT | adm1 | adm2 | cities | osm_boundaries status TEXT NOT NULL,
  -- TEXT | in_progress | completed | failed started_at INTEGER,
  completed_at INTEGER ,
  records_total INTEGER DEFAULT 0,
  records_processed INTEGER DEFAULT 0,
  records_upserted INTEGER DEFAULT 0,
  errors INTEGER DEFAULT 0,
  error_message TEXT ,
  metadata JSONB 
)`,

  // http_responses
  `CREATE TABLE IF NOT EXISTS http_responses (
  id SERIAL PRIMARY KEY,
  url_id INTEGER NOT NULL REFERENCES urls(id),
  request_started_at TEXT NOT NULL,
  fetched_at TEXT ,
  http_status INTEGER ,
  content_type TEXT ,
  content_encoding TEXT ,
  etag TEXT ,
  last_modified TEXT ,
  redirect_chain TEXT ,
  ttfb_ms INTEGER ,
  download_ms INTEGER ,
  total_ms INTEGER ,
  bytes_downloaded INTEGER ,
  transfer_kbps DOUBLE PRECISION ,
  cache_category TEXT ,
  cache_key TEXT ,
  cache_created_at TEXT ,
  cache_expires_at TEXT ,
  request_method TEXT DEFAULT 'GET'
)`,

  // hub_discoveries
  `CREATE TABLE IF NOT EXISTS hub_discoveries (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  discovered_at TEXT NOT NULL,
  hub_url TEXT NOT NULL,
  hub_type TEXT ,
  discovery_method TEXT NOT NULL,
  confidence_score DOUBLE PRECISION ,
  classification_reason TEXT ,
  gap_filled TEXT DEFAULT 0,
  coverage_impact DOUBLE PRECISION ,
  metadata TEXT ,
  hub_url_id INTEGER ,
  UNIQUE(job_id, hub_url)
)`,

  // hub_validations
  `CREATE TABLE IF NOT EXISTS hub_validations (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  hub_url TEXT NOT NULL,
  hub_type TEXT NOT NULL,
  validation_status TEXT NOT NULL,
  classification_confidence DOUBLE PRECISION ,
  last_fetch_status INTEGER ,
  content_indicators TEXT ,
  validation_method TEXT ,
  validated_at TEXT NOT NULL,
  expires_at TEXT ,
  revalidation_priority INTEGER DEFAULT 0,
  metadata TEXT ,
  hub_url_id INTEGER ,
  UNIQUE(domain, hub_url)
)`,

  // ingestion_runs
  `CREATE TABLE IF NOT EXISTS ingestion_runs (
  id SERIAL PRIMARY KEY,
  source TEXT NOT NULL,
  -- TEXT name (restcountries, wikidata, osm, etc.) source_version TEXT,
  -- TEXT version (v3.1, latest, etc.) started_at INTEGER NOT NULL,
  -- TEXT timestamp completed_at INTEGER,
  -- TEXT timestamp status TEXT DEFAULT 'running',
  -- TEXT | completed | failed countries_processed INTEGER,
  places_created INTEGER ,
  places_updated INTEGER ,
  names_added INTEGER ,
  error_message TEXT ,
  metadata JSONB -- Additional run metadata
)`,

  // integrations
  `CREATE TABLE IF NOT EXISTS integrations (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL,
  type TEXT NOT NULL,
  name TEXT ,
  config TEXT NOT NULL,
  enabled INTEGER DEFAULT 1,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP
)`,

  // knowledge_reuse_events
  `CREATE TABLE IF NOT EXISTS knowledge_reuse_events (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  reuse_type TEXT NOT NULL,
  source_pattern_id INTEGER ,
  source_hub_id INTEGER ,
  reused_url TEXT ,
  success_outcome TEXT ,
  time_saved_ms INTEGER ,
  confidence_at_reuse DOUBLE PRECISION ,
  outcome_details TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  FOREIGN KEY (source_pattern_id) REFERENCES planner_patterns(id),
  FOREIGN KEY (source_hub_id) REFERENCES hub_validations(id)
)`,

  // layout_masks
  `CREATE TABLE IF NOT EXISTS layout_masks (
  signature_hash TEXT PRIMARY KEY,
  mask_json TEXT NOT NULL,
  sample_count INTEGER DEFAULT 0,
  dynamic_nodes_count INTEGER DEFAULT 0,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (signature_hash) REFERENCES layout_signatures(signature_hash) ON DELETE CASCADE
)`,

  // layout_signatures
  `CREATE TABLE IF NOT EXISTS layout_signatures (
  signature_hash TEXT PRIMARY KEY,
  level INTEGER NOT NULL,
  signature TEXT NOT NULL,
  first_seen_url TEXT ,
  seen_count INTEGER DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  last_seen_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)`,

  // layout_templates
  `CREATE TABLE IF NOT EXISTS layout_templates (
  id SERIAL PRIMARY KEY,
  signature_hash TEXT NOT NULL,
  producer TEXT NOT NULL DEFAULT 'static-skeletonhash-v1',
  host TEXT ,
  label TEXT ,
  notes TEXT ,
  example_url TEXT ,
  extraction_config_json TEXT ,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (signature_hash) REFERENCES layout_signatures(signature_hash) ON DELETE CASCADE,
  UNIQUE (producer, signature_hash)
)`,

  // links
  `CREATE TABLE IF NOT EXISTS links (
  id INTEGER PRIMARY KEY,
  anchor TEXT ,
  rel TEXT ,
  type TEXT ,
  depth INTEGER ,
  on_domain INTEGER ,
  discovered_at TEXT NOT NULL,
  src_url_id INTEGER ,
  dst_url_id INTEGER 
)`,

  // milestone_achievements
  `CREATE TABLE IF NOT EXISTS milestone_achievements (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  milestone_type TEXT NOT NULL,
  achieved_at TEXT NOT NULL,
  threshold_value DOUBLE PRECISION ,
  actual_value DOUBLE PRECISION ,
  improvement_percentage DOUBLE PRECISION ,
  context_data TEXT ,
  celebration_level TEXT DEFAULT 'normal'
)`,

  // news_websites
  `CREATE TABLE IF NOT EXISTS news_websites (
  id SERIAL PRIMARY KEY,
  url TEXT NOT NULL UNIQUE,
  label TEXT ,
  parent_domain TEXT ,
  url_pattern TEXT NOT NULL,
  website_type TEXT NOT NULL,
  added_at TEXT NOT NULL,
  added_by TEXT ,
  enabled INTEGER DEFAULT 1,
  metadata TEXT ,
  favicon_data TEXT ,
  favicon_content_type TEXT ,
  favicon_updated_at TEXT ,
  favicon_fetch_error TEXT 
)`,

  // news_websites_stats_cache
  `CREATE TABLE IF NOT EXISTS news_websites_stats_cache (
  website_id INTEGER PRIMARY KEY,
  -- TEXT statistics article_count INTEGER DEFAULT 0,
  article_latest_date TEXT ,
  article_latest_crawled_at TEXT ,
  article_first_seen_at TEXT ,
  -- TEXT statistics fetch_count INTEGER DEFAULT 0,
  fetch_ok_count INTEGER DEFAULT 0,
  fetch_error_count INTEGER DEFAULT 0,
  fetch_last_at TEXT ,
  fetch_first_at TEXT ,
  -- TEXT status distribution (top 5) status_200_count INTEGER DEFAULT 0,
  status_404_count INTEGER DEFAULT 0,
  status_403_count INTEGER DEFAULT 0,
  status_500_count INTEGER DEFAULT 0,
  status_503_count INTEGER DEFAULT 0,
  -- TEXT statistics avg_article_size_bytes INTEGER DEFAULT 0,
  total_content_bytes INTEGER DEFAULT 0,
  -- TEXT performance avg_fetch_time_ms INTEGER DEFAULT 0,
  successful_crawls INTEGER DEFAULT 0,
  failed_crawls INTEGER DEFAULT 0,
  -- TEXT statistics last_updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
  cache_version INTEGER DEFAULT 1,
  FOREIGN KEY (website_id) REFERENCES news_websites(id) ON DELETE CASCADE
)`,

  // non_geo_topic_slugs
  `CREATE TABLE IF NOT EXISTS non_geo_topic_slugs (
  id SERIAL PRIMARY KEY,
  slug TEXT NOT NULL,
  label TEXT ,
  lang TEXT NOT NULL DEFAULT 'und',
  source TEXT ,
  notes TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(slug, lang)
)`,

  // page_categories
  `CREATE TABLE IF NOT EXISTS page_categories (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  slug TEXT NOT NULL UNIQUE,
  description TEXT ,
  detection_heuristics TEXT ,
  -- JSONB describing how to detect this category created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // page_category_map
  `CREATE TABLE IF NOT EXISTS page_category_map (
  content_id INTEGER NOT NULL,
  category_id INTEGER NOT NULL,
  confidence DOUBLE PRECISION DEFAULT 1.0,
  detected_at TEXT NOT NULL DEFAULT (datetime('now')),
  detection_method TEXT ,
  PRIMARY KEY (content_id, category_id),
  FOREIGN KEY (content_id) REFERENCES content_analysis(id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES page_categories(id) ON DELETE CASCADE
)`,

  // place_attribute_values
  `CREATE TABLE IF NOT EXISTS place_attribute_values (
  place_id INTEGER NOT NULL,
  attr TEXT NOT NULL,
  source TEXT NOT NULL,
  value_json TEXT ,
  confidence DOUBLE PRECISION ,
  fetched_at INTEGER ,
  metadata JSONB ,
  PRIMARY KEY (place_id, attr, source),
  FOREIGN KEY (place_id) REFERENCES places(id) ON DELETE CASCADE
)`,

  // place_attributes
  `CREATE TABLE IF NOT EXISTS place_attributes (
  id SERIAL PRIMARY KEY,
  place_id INTEGER NOT NULL REFERENCES places(id),
  attribute_kind TEXT NOT NULL,
  value TEXT NOT NULL,
  source TEXT NOT NULL,
  fetched_at INTEGER ,
  confidence DOUBLE PRECISION ,
  metadata TEXT ,
  UNIQUE(place_id, attribute_kind, source)
)`,

  // place_external_ids
  `CREATE TABLE IF NOT EXISTS place_external_ids (
  source TEXT NOT NULL,
  ext_id TEXT NOT NULL,
  place_id INTEGER NOT NULL,
  PRIMARY KEY (source, ext_id),
  FOREIGN KEY (place_id) REFERENCES places(id) ON DELETE CASCADE
)`,

  // place_hierarchy
  `CREATE TABLE IF NOT EXISTS place_hierarchy (
  parent_id INTEGER NOT NULL,
  child_id INTEGER NOT NULL,
  relation TEXT ,
  -- TEXT | contains | member_of depth INTEGER,
  PRIMARY KEY (parent_id, child_id),
  FOREIGN KEY (parent_id) REFERENCES places(id) ON DELETE CASCADE,
  FOREIGN KEY (child_id) REFERENCES places(id) ON DELETE CASCADE
)`,

  // place_hub_audit
  `CREATE TABLE IF NOT EXISTS place_hub_audit (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  url TEXT NOT NULL,
  place_kind TEXT ,
  place_name TEXT ,
  decision TEXT NOT NULL,
  validation_metrics_json TEXT ,
  attempt_id TEXT ,
  run_id TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // place_hub_candidates
  `CREATE TABLE IF NOT EXISTS place_hub_candidates (
  id INTEGER PRIMARY KEY,
  domain TEXT NOT NULL,
  place_kind TEXT ,
  place_name TEXT ,
  place_code TEXT ,
  place_id INTEGER ,
  analyzer TEXT ,
  strategy TEXT ,
  score DOUBLE PRECISION ,
  confidence DOUBLE PRECISION ,
  pattern TEXT ,
  signals_json TEXT ,
  attempt_id TEXT ,
  attempt_started_at TEXT ,
  status TEXT DEFAULT 'pending',
  validation_status TEXT ,
  source TEXT DEFAULT 'guess-place-hubs',
  last_seen_at TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  candidate_url_id INTEGER ,
  normalized_url_id INTEGER 
)`,

  // place_hub_determinations
  `CREATE TABLE IF NOT EXISTS place_hub_determinations (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  determination TEXT NOT NULL,
  reason TEXT NOT NULL,
  details_json TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // place_hub_guess_runs
  `CREATE TABLE IF NOT EXISTS place_hub_guess_runs (
  id TEXT PRIMARY KEY,
  started_at TEXT NOT NULL,
  ended_at TEXT ,
  status TEXT NOT NULL,
  stage TEXT ,
  -- TEXT guessing specific parameters domain_count INTEGER,
  total_domains INTEGER ,
  kinds TEXT ,
  -- JSONB array of hub kinds limit_per_domain INTEGER,
  apply_changes INTEGER ,
  -- TEXT for boolean emit_report INTEGER,
  -- TEXT for boolean report_path TEXT,
  readiness_timeout_seconds INTEGER ,
  enable_topic_discovery INTEGER ,
  -- TEXT for boolean -- Results summary domains_processed INTEGER DEFAULT 0,
  hubs_generated INTEGER DEFAULT 0,
  hubs_validated INTEGER DEFAULT 0,
  hubs_persisted INTEGER DEFAULT 0,
  errors_count INTEGER DEFAULT 0,
  -- TEXT duration_ms INTEGER,
  -- TEXT task linkage (like analysis_runs) background_task_id INTEGER,
  background_task_status TEXT ,
  -- TEXT metadata summary TEXT,
  last_progress TEXT ,
  error TEXT ,
  FOREIGN KEY (background_task_id) REFERENCES background_tasks(id)
)`,

  // place_hub_unknown_terms
  `CREATE TABLE IF NOT EXISTS place_hub_unknown_terms (
  id INTEGER PRIMARY KEY,
  host TEXT NOT NULL,
  term_slug TEXT NOT NULL,
  term_label TEXT ,
  source TEXT ,
  reason TEXT ,
  confidence TEXT ,
  evidence TEXT ,
  occurrences INTEGER NOT NULL DEFAULT 1,
  first_seen_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_seen_at TEXT NOT NULL DEFAULT (datetime('now')),
  url_id INTEGER ,
  canonical_url_id INTEGER 
)`,

  // place_hubs
  `CREATE TABLE IF NOT EXISTS place_hubs (
  id INTEGER PRIMARY KEY,
  host TEXT NOT NULL,
  place_slug TEXT ,
  title TEXT ,
  first_seen_at TEXT ,
  last_seen_at TEXT ,
  nav_links_count INTEGER ,
  article_links_count INTEGER ,
  evidence TEXT ,
  place_kind TEXT ,
  topic_slug TEXT ,
  topic_label TEXT ,
  topic_kind TEXT ,
  url_id INTEGER 
)`,

  // place_names
  `CREATE TABLE IF NOT EXISTS place_names (
  id SERIAL PRIMARY KEY,
  place_id INTEGER NOT NULL,
  name TEXT NOT NULL,
  normalized TEXT ,
  -- TEXT for matching lang TEXT,
  -- TEXT (e.g., en, fr, zh-Hans) script TEXT,
  -- TEXT ISO 15924 name_kind TEXT,
  -- TEXT | exonym | alias | abbrev | official | common | demonym is_preferred INTEGER,
  -- TEXT is_official INTEGER,
  -- TEXT source TEXT,
  FOREIGN KEY (place_id) REFERENCES places(id) ON DELETE CASCADE
)`,

  // place_page_mappings
  `CREATE TABLE IF NOT EXISTS place_page_mappings (
  id SERIAL PRIMARY KEY,
  place_id INTEGER NOT NULL,
  host TEXT NOT NULL,
  url TEXT NOT NULL,
  page_kind TEXT NOT NULL DEFAULT 'country-hub',
  publisher TEXT ,
  status TEXT NOT NULL DEFAULT 'pending',
  first_seen_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_seen_at TEXT NOT NULL DEFAULT (datetime('now')),
  verified_at TEXT ,
  evidence JSONB ,
  hub_id INTEGER ,
  UNIQUE(place_id, host, page_kind),
  FOREIGN KEY (place_id) REFERENCES places(id) ON DELETE CASCADE,
  FOREIGN KEY (hub_id) REFERENCES place_hubs(id) ON DELETE SET NULL
)`,

  // place_provenance
  `CREATE TABLE IF NOT EXISTS place_provenance (
  id SERIAL PRIMARY KEY,
  place_id INTEGER NOT NULL REFERENCES places(id),
  source TEXT NOT NULL,
  external_id TEXT NOT NULL,
  fetched_at INTEGER ,
  raw_data TEXT ,
  UNIQUE(place_id, source, external_id)
)`,

  // place_sources
  `CREATE TABLE IF NOT EXISTS place_sources (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  version TEXT ,
  url TEXT ,
  license TEXT 
)`,

  // places
  `CREATE TABLE IF NOT EXISTS places (
  id SERIAL PRIMARY KEY,
  kind TEXT NOT NULL,
  -- TEXT | region | city | poi | supranational country_code TEXT,
  -- TEXT alpha-2 when applicable adm1_code TEXT,
  -- TEXT admin code when applicable adm2_code TEXT,
  -- TEXT admin code when applicable population INTEGER,
  timezone TEXT ,
  lat DOUBLE PRECISION ,
  lng DOUBLE PRECISION ,
  bbox TEXT ,
  -- JSONB [west,
  south TEXT ,
  east TEXT ,
  north] TEXT available canonical_name_id INTEGER,
  -- TEXT place_names.id (optional) source TEXT,
  -- TEXT (e.g., restcountries@v3) extra JSON -- JSON blob for source-specific data,
  status TEXT DEFAULT 'current',
  valid_from TEXT ,
  valid_to TEXT ,
  wikidata_qid TEXT ,
  osm_type TEXT ,
  osm_id TEXT ,
  area DOUBLE PRECISION ,
  gdp_usd DOUBLE PRECISION ,
  wikidata_admin_level INTEGER ,
  wikidata_props JSONB ,
  osm_tags JSONB ,
  crawl_depth INTEGER DEFAULT 0,
  priority_score DOUBLE PRECISION ,
  last_crawled_at INTEGER 
)`,

  // planner_patterns
  `CREATE TABLE IF NOT EXISTS planner_patterns (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  pattern_type TEXT NOT NULL,
  pattern_regex TEXT NOT NULL,
  success_count INTEGER DEFAULT 0,
  failure_count INTEGER DEFAULT 0,
  confidence_score DOUBLE PRECISION DEFAULT 0.0,
  last_validated TEXT ,
  metadata TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  UNIQUE(domain, pattern_type, pattern_regex)
)`,

  // planner_stage_events
  `CREATE TABLE IF NOT EXISTS planner_stage_events (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  stage TEXT NOT NULL,
  status TEXT NOT NULL,
  sequence INTEGER ,
  duration_ms INTEGER ,
  details TEXT ,
  FOREIGN KEY(job_id) REFERENCES crawl_jobs(id) ON DELETE CASCADE
)`,

  // priority_config_changes
  `CREATE TABLE IF NOT EXISTS priority_config_changes (
  id SERIAL PRIMARY KEY,
  changed_at TEXT DEFAULT (datetime('now')),
  changed_by TEXT ,
  change_type TEXT NOT NULL,
  old_values TEXT ,
  new_values TEXT ,
  impact_assessment TEXT 
)`,

  // problem_clusters
  `CREATE TABLE IF NOT EXISTS problem_clusters (
  id TEXT PRIMARY KEY,
  job_id TEXT NOT NULL,
  kind TEXT NOT NULL,
  scope TEXT ,
  target TEXT ,
  first_seen TEXT NOT NULL,
  last_seen TEXT NOT NULL,
  occurrence_count INTEGER DEFAULT 1,
  priority_boost DOUBLE PRECISION DEFAULT 0,
  status TEXT DEFAULT 'active',
  cluster_metadata TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
)`,

  // query_telemetry
  `CREATE TABLE IF NOT EXISTS query_telemetry (
  id SERIAL PRIMARY KEY,
  query_type TEXT NOT NULL,
  -- TEXT ,
  'fetch_articles' TEXT ,
  'lookup_place' TEXT ,
  'analyze_domain' TEXT TEXT NOT NULL,
  -- TEXT ,
  'SELECT' TEXT ,
  'INSERT' TEXT ,
  'UPDATE' TEXT ,
  'DELETE' TEXT REAL NOT NULL,
  -- TEXT execution time in milliseconds result_count INTEGER DEFAULT 0,
  -- TEXT of rows returned or affected query_complexity TEXT,
  -- TEXT | 'moderate' | 'complex' (based on query structure) host TEXT,
  -- TEXT being queried (if applicable) job_id TEXT,
  -- TEXT job ID (if applicable) timestamp TEXT NOT NULL DEFAULT (datetime('now')),
  metadata TEXT -- JSON: { table,
  filters TEXT ,
  joins TEXT ,
  error TEXT ,
  etc. TEXT ,
  created_at TEXT 
)`,

  // queue_events
  `CREATE TABLE IF NOT EXISTS queue_events (
  id INTEGER PRIMARY KEY,
  job_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  action TEXT NOT NULL,
  depth INTEGER ,
  host TEXT ,
  reason TEXT ,
  queue_size INTEGER ,
  alias TEXT ,
  queue_origin TEXT ,
  queue_role TEXT ,
  queue_depth_bucket TEXT ,
  url_id INTEGER 
)`,

  // queue_events_enhanced
  `CREATE TABLE IF NOT EXISTS queue_events_enhanced (
  id SERIAL PRIMARY KEY,
  job_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  action TEXT NOT NULL,
  url_id INTEGER ,
  depth INTEGER ,
  host TEXT ,
  reason TEXT ,
  queue_size INTEGER ,
  alias TEXT ,
  queue_origin TEXT ,
  queue_role TEXT ,
  queue_depth_bucket TEXT ,
  priority_score DOUBLE PRECISION ,
  priority_source TEXT ,
  bonus_applied DOUBLE PRECISION ,
  cluster_id TEXT ,
  gap_prediction_score DOUBLE PRECISION ,
  created_at TEXT DEFAULT (datetime('now'))
)`,

  // rate_limits
  `CREATE TABLE IF NOT EXISTS rate_limits (
  id SERIAL PRIMARY KEY,
  domain TEXT UNIQUE NOT NULL,
  interval_ms INTEGER NOT NULL DEFAULT 1000,
  rate_limit_hits INTEGER NOT NULL DEFAULT 0,
  last_hit_at TEXT ,
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
)`,

  // robots_cache
  `CREATE TABLE IF NOT EXISTS robots_cache (
  domain TEXT PRIMARY KEY,
  robots_txt TEXT ,
  fetched_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  http_status INTEGER ,
  etag TEXT ,
  last_modified TEXT ,
  crawl_delay_seconds INTEGER ,
  sitemap_urls TEXT ,
  rules_summary TEXT ,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
)`,

  // schema_metadata
  `CREATE TABLE IF NOT EXISTS schema_metadata (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  details JSONB 
)`,

  // schema_migrations
  `CREATE TABLE IF NOT EXISTS schema_migrations (
  version INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  applied_at TEXT NOT NULL,
  description TEXT ,
  rollback_sql TEXT 
)`,

  // task_events
  `CREATE TABLE IF NOT EXISTS task_events (
  id SERIAL PRIMARY KEY,
  -- TEXT task_type TEXT NOT NULL,
  -- TEXT | 'analysis' | 'geo_import' | 'compression' | ... task_id TEXT NOT NULL,
  -- TEXT or background_task_id -- Ordering for replay seq INTEGER NOT NULL,
  -- TEXT per task (enables cursor-based replay) ts TEXT NOT NULL,
  -- TEXT timestamp -- Event classification event_type TEXT NOT NULL,
  -- TEXT ,
  'milestone' TEXT ,
  'problem' TEXT ,
  'pause' TEXT ,
  'stage:enter' TEXT ,
  etc. TEXT TEXT,
  -- TEXT | 'work' | 'error' | 'metric' | 'control' severity TEXT,
  -- TEXT | 'info' | 'warn' | 'error' -- Scoping (for filtering) scope TEXT,
  -- TEXT ,
  'url:...' TEXT ,
  'stage:discovery' TEXT ,
  etc. TEXT TEXT,
  -- TEXT URL,
  domain TEXT ,
  or TEXT affected -- Payload (structured for extraction) payload TEXT,
  -- JSONB with event-specific data -- Denormalized fields for fast queries duration_ms INTEGER,
  -- TEXT timing event http_status INTEGER,
  -- TEXT fetch event item_count INTEGER -- if counting event (links found, bytes, etc.)
)`,

  // topic_keywords
  `CREATE TABLE IF NOT EXISTS topic_keywords (
  id SERIAL PRIMARY KEY,
  topic TEXT NOT NULL,
  lang TEXT NOT NULL,
  term TEXT NOT NULL,
  normalized TEXT NOT NULL,
  source TEXT ,
  metadata JSONB 
)`,

  // ui_cached_metrics
  `CREATE TABLE IF NOT EXISTS ui_cached_metrics (
  stat_key TEXT PRIMARY KEY,
  payload TEXT NOT NULL,
  generated_at TEXT NOT NULL,
  source_window TEXT ,
  duration_ms INTEGER ,
  max_age_ms INTEGER ,
  error TEXT ,
  metadata JSONB 
)`,

  // ui_themes
  `CREATE TABLE IF NOT EXISTS ui_themes (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  display_name TEXT NOT NULL,
  description TEXT ,
  config TEXT NOT NULL,
  is_default INTEGER NOT NULL DEFAULT 0,
  is_system INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // url_aliases
  `CREATE TABLE IF NOT EXISTS url_aliases (
  id INTEGER PRIMARY KEY,
  classification TEXT ,
  reason TEXT ,
  "exists" INTEGER ,
  checked_at TEXT NOT NULL,
  metadata TEXT ,
  url_exists INTEGER DEFAULT 0,
  url_id INTEGER ,
  alias_url_id INTEGER 
)`,

  // url_categories
  `CREATE TABLE IF NOT EXISTS url_categories (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  description TEXT 
)`,

  // url_category_map
  `CREATE TABLE IF NOT EXISTS url_category_map (
  url_id INTEGER NOT NULL,
  category_id INTEGER NOT NULL,
  PRIMARY KEY (url_id, category_id),
  FOREIGN KEY (url_id) REFERENCES urls(id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES url_categories(id) ON DELETE CASCADE
)`,

  // url_classification_patterns
  `CREATE TABLE IF NOT EXISTS url_classification_patterns (
  id SERIAL PRIMARY KEY,
  domain TEXT NOT NULL,
  pattern_regex TEXT NOT NULL,
  pattern_description TEXT ,
  classification TEXT NOT NULL,
  sample_count INTEGER DEFAULT 0,
  verified_count INTEGER DEFAULT 0,
  correct_count INTEGER DEFAULT 0,
  accuracy DOUBLE PRECISION ,
  last_verified_at TEXT ,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)`,

  // url_classifications
  `CREATE TABLE IF NOT EXISTS url_classifications (
  id SERIAL PRIMARY KEY,
  url_id INTEGER NOT NULL REFERENCES urls(id),
  predicted_classification TEXT NOT NULL,
  confidence DOUBLE PRECISION NOT NULL CHECK(confidence >= 0.0 AND confidence <= 1.0),
  prediction_source TEXT NOT NULL,
  pattern_matched TEXT ,
  similar_url_id INTEGER REFERENCES urls(id),
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  verified_at TEXT ,
  verified_classification TEXT ,
  verification_match INTEGER 
)`,

  // urls
  `CREATE TABLE IF NOT EXISTS urls (
  id SERIAL PRIMARY KEY,
  url TEXT NOT NULL UNIQUE,
  canonical_url TEXT ,
  created_at TEXT ,
  last_seen_at TEXT ,
  analysis TEXT ,
  host TEXT 
)`,

  // webhook_deliveries
  `CREATE TABLE IF NOT EXISTS webhook_deliveries (
  id SERIAL PRIMARY KEY,
  webhook_id INTEGER NOT NULL,
  event_type TEXT NOT NULL,
  payload TEXT NOT NULL,
  status TEXT DEFAULT 'pending',
  attempts INTEGER DEFAULT 0,
  last_attempt_at TEXT ,
  response_code INTEGER ,
  response_body TEXT ,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (webhook_id) REFERENCES webhooks(id) ON DELETE CASCADE
)`,

  // webhooks
  `CREATE TABLE IF NOT EXISTS webhooks (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL,
  name TEXT NOT NULL,
  url TEXT NOT NULL,
  secret TEXT NOT NULL,
  events TEXT NOT NULL,
  enabled INTEGER DEFAULT 1,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP
)`,

];

const INDEX_STATEMENTS = [
  `CREATE INDEX idx_analysis_run_events_run_ts ON analysis_run_events(run_id, ts DESC)`,
  `CREATE INDEX idx_analysis_runs_background_task ON analysis_runs(background_task_id)`,
  `CREATE INDEX idx_analysis_runs_started_at ON analysis_runs(started_at DESC)`,
  `CREATE INDEX idx_analysis_runs_status ON analysis_runs(status, started_at DESC)`,
  `CREATE INDEX idx_api_keys_active ON api_keys(is_active)`,
  `CREATE INDEX idx_api_keys_hash ON api_keys(key_hash)`,
  `CREATE INDEX idx_api_keys_prefix ON api_keys(key_prefix)`,
  `CREATE INDEX idx_article_place_relations_article ON article_place_relations(article_id)`,
  `CREATE INDEX idx_article_place_relations_confidence ON article_place_relations(confidence DESC)`,
  `CREATE INDEX idx_article_place_relations_place ON article_place_relations(place_id)`,
  `CREATE INDEX idx_article_places_place ON article_places(place)`,
  `CREATE INDEX idx_article_places_url_id ON article_places(article_url_id)`,
  `CREATE INDEX idx_article_xpath_patterns_domain ON article_xpath_patterns(domain)`,
  `CREATE UNIQUE INDEX idx_article_xpath_patterns_domain_xpath ON article_xpath_patterns(domain, xpath)`,
  `CREATE INDEX idx_background_tasks_created ON background_tasks(created_at DESC)`,
  `CREATE INDEX idx_background_tasks_status ON background_tasks(status)`,
  `CREATE INDEX idx_background_tasks_type ON background_tasks(task_type)`,
  `CREATE INDEX idx_bucket_entries_bucket ON bucket_entries(bucket_id)`,
  `CREATE INDEX idx_bucket_entries_key ON bucket_entries(entry_key)`,
  `CREATE INDEX idx_compression_buckets_domain 
      ON compression_buckets(domain_pattern)`,
  `CREATE INDEX idx_compression_buckets_finalized 
      ON compression_buckets(finalized_at)`,
  `CREATE INDEX idx_compression_buckets_status ON compression_buckets(status)`,
  `CREATE INDEX idx_compression_buckets_type 
      ON compression_buckets(bucket_type)`,
  `CREATE INDEX idx_compression_buckets_type_status ON compression_buckets(bucket_type, status)`,
  `CREATE INDEX idx_content_analysis_analyzed_at ON content_analysis(analyzed_at)`,
  `CREATE INDEX idx_content_analysis_byline ON content_analysis(byline)`,
  `CREATE INDEX idx_content_analysis_classification ON content_analysis(classification)`,
  `CREATE INDEX idx_content_analysis_content ON content_analysis(content_id)`,
  `CREATE INDEX idx_content_analysis_content_classification ON content_analysis(content_id, classification)`,
  `CREATE INDEX idx_content_analysis_content_id ON content_analysis(content_id)`,
  `CREATE INDEX idx_content_analysis_date ON content_analysis(date)`,
  `CREATE INDEX idx_content_analysis_pending_json ON content_analysis(id) WHERE analysis_json IS NULL`,
  `CREATE INDEX idx_content_analysis_pending_version_null ON content_analysis(id) WHERE analysis_json IS NOT NULL AND analysis_version IS NULL`,
  `CREATE INDEX idx_content_analysis_version ON content_analysis(analysis_version)`,
  `CREATE INDEX idx_content_storage_bucket 
      ON content_storage(compression_bucket_id)`,
  `CREATE INDEX idx_content_storage_category ON content_storage(content_category, content_subtype)`,
  `CREATE INDEX idx_content_storage_http_response ON content_storage(http_response_id)`,
  `CREATE INDEX idx_content_storage_sha256 
      ON content_storage(content_sha256)`,
  `CREATE INDEX idx_content_storage_type 
      ON content_storage(storage_type)`,
  `CREATE INDEX idx_coverage_gaps_job_status ON coverage_gaps(job_id, resolution_status)`,
  `CREATE INDEX idx_coverage_gaps_priority ON coverage_gaps(priority_score DESC)`,
  `CREATE INDEX idx_coverage_gaps_type ON coverage_gaps(gap_type)`,
  `CREATE INDEX idx_coverage_snapshots_coverage ON coverage_snapshots(coverage_percentage DESC)`,
  `CREATE INDEX idx_coverage_snapshots_domain ON coverage_snapshots(domain)`,
  `CREATE INDEX idx_coverage_snapshots_job_time ON coverage_snapshots(job_id, snapshot_time DESC)`,
  `CREATE INDEX idx_crawl_jobs_end_started_desc ON crawl_jobs(ended_at, started_at DESC)`,
  `CREATE INDEX idx_crawl_jobs_paused 
    ON crawl_jobs(paused_at) WHERE paused_at IS NOT NULL
  `,
  `CREATE INDEX idx_crawl_jobs_sort_key ON crawl_jobs((COALESCE(ended_at, started_at)) DESC)`,
  `CREATE INDEX idx_crawl_jobs_started_desc ON crawl_jobs(started_at DESC)`,
  `CREATE INDEX idx_crawl_jobs_status_active ON crawl_jobs(status, ended_at, started_at DESC)`,
  `CREATE INDEX idx_crawl_jobs_timeline ON crawl_jobs(ended_at DESC, started_at DESC)`,
  `CREATE INDEX idx_crawl_jobs_url_id_temp ON crawl_jobs(url_id)`,
  `CREATE INDEX idx_crawl_milestones_job ON crawl_milestones(job_id)`,
  `CREATE INDEX idx_crawl_milestones_job_ts ON crawl_milestones(job_id, ts DESC)`,
  `CREATE INDEX idx_crawl_milestones_kind ON crawl_milestones(kind)`,
  `CREATE INDEX idx_crawl_milestones_scope_kind ON crawl_milestones(scope, kind)`,
  `CREATE INDEX idx_crawl_pause_history_job
    ON crawl_pause_history(job_id, created_at DESC)
  `,
  `CREATE INDEX idx_crawl_problems_job ON crawl_problems(job_id)`,
  `CREATE INDEX idx_crawl_problems_job_ts ON crawl_problems(job_id, ts DESC)`,
  `CREATE INDEX idx_crawl_problems_kind ON crawl_problems(kind)`,
  `CREATE INDEX idx_crawl_queue_domain 
        ON crawl_queue (domain)`,
  `CREATE INDEX idx_crawl_queue_status_priority 
        ON crawl_queue (status, priority DESC, created_at ASC)`,
  `CREATE INDEX idx_crawl_queue_updated
        ON crawl_queue (updated_at) WHERE status = 'in-progress'`,
  `CREATE INDEX idx_crawl_queue_worker 
        ON crawl_queue (worker_id) WHERE status = 'in-progress'`,
  `CREATE INDEX idx_crawl_skip_terms_reason ON crawl_skip_terms(reason)`,
  `CREATE INDEX idx_crawl_state_stage ON gazetteer_crawl_state(stage)`,
  `CREATE INDEX idx_crawl_state_status ON gazetteer_crawl_state(status)`,
  `CREATE INDEX idx_crawl_tasks_job_status ON crawl_tasks(job_id, status, created_at DESC)`,
  `CREATE INDEX idx_crawl_tasks_status ON crawl_tasks(status, created_at DESC)`,
  `CREATE INDEX idx_cross_crawl_confidence ON cross_crawl_knowledge(confidence_level DESC)`,
  `CREATE INDEX idx_cross_crawl_domain_type ON cross_crawl_knowledge(source_domain, knowledge_type)`,
  `CREATE INDEX idx_cross_crawl_usage ON cross_crawl_knowledge(usage_count DESC)`,
  `CREATE INDEX idx_dashboard_metrics_job_name_time ON dashboard_metrics(job_id, metric_name, timestamp DESC)`,
  `CREATE INDEX idx_discovery_events_url ON discovery_events(url_id)`,
  `CREATE INDEX idx_discovery_events_url_discovered ON discovery_events(url_id, discovered_at DESC)`,
  `CREATE INDEX idx_discovery_job ON discovery_events(crawl_job_id)`,
  `CREATE INDEX idx_discovery_url ON discovery_events(url_id, discovered_at DESC)`,
  `CREATE INDEX idx_domain_profiles_confidence
            ON domain_classification_profiles(profile_confidence DESC)`,
  `CREATE INDEX idx_domain_rate_events_domain_time 
    ON domain_rate_events(domain, created_at DESC)
  `,
  `CREATE INDEX idx_domain_rate_limits_updated 
    ON domain_rate_limits(updated_at)
  `,
  `CREATE INDEX idx_domains_host ON domains(host)`,
  `CREATE INDEX idx_domains_last_seen ON domains(last_seen_at)`,
  `CREATE INDEX idx_errors_code ON errors(code)`,
  `CREATE INDEX idx_errors_host ON errors(host)`,
  `CREATE INDEX idx_errors_kind ON errors(kind)`,
  `CREATE INDEX idx_errors_time ON errors(at)`,
  `CREATE INDEX idx_errors_url_id_temp ON errors(url_id)`,
  `CREATE INDEX idx_gap_predictions_job_confidence ON gap_predictions(job_id, confidence_score DESC)`,
  `CREATE INDEX idx_gap_predictions_status ON gap_predictions(validation_status)`,
  `CREATE INDEX idx_http_responses_cache_expires_at ON http_responses(cache_expires_at)`,
  `CREATE INDEX idx_http_responses_cache_key_category ON http_responses(cache_key, cache_category)`,
  `CREATE INDEX idx_http_responses_fetched ON http_responses(fetched_at)`,
  `CREATE INDEX idx_http_responses_status ON http_responses(http_status)`,
  `CREATE INDEX idx_http_responses_url ON http_responses(url_id, fetched_at DESC)`,
  `CREATE INDEX idx_http_responses_url_fetched ON http_responses(url_id, fetched_at)`,
  `CREATE INDEX idx_http_responses_url_status ON http_responses(url_id, http_status)`,
  `CREATE INDEX idx_hub_discoveries_confidence ON hub_discoveries(confidence_score DESC)`,
  `CREATE INDEX idx_hub_discoveries_job_discovered ON hub_discoveries(job_id, discovered_at DESC)`,
  `CREATE INDEX idx_hub_discoveries_method ON hub_discoveries(discovery_method)`,
  `CREATE INDEX idx_hub_validations_domain_status ON hub_validations(domain, validation_status)`,
  `CREATE INDEX idx_hub_validations_expires ON hub_validations(expires_at)`,
  `CREATE INDEX idx_hub_validations_priority ON hub_validations(revalidation_priority DESC)`,
  `CREATE INDEX idx_ingestion_runs_completed ON ingestion_runs(completed_at)`,
  `CREATE INDEX idx_ingestion_runs_source_status ON ingestion_runs(source, status)`,
  `CREATE INDEX idx_integrations_user_id ON integrations(user_id)`,
  `CREATE INDEX idx_knowledge_reuse_job ON knowledge_reuse_events(job_id)`,
  `CREATE INDEX idx_knowledge_reuse_type_success ON knowledge_reuse_events(reuse_type, success_outcome)`,
  `CREATE INDEX idx_layout_masks_updated_at
      ON layout_masks(updated_at)`,
  `CREATE INDEX idx_layout_signatures_level ON layout_signatures(level)`,
  `CREATE INDEX idx_layout_templates_host
      ON layout_templates(host)`,
  `CREATE INDEX idx_links_dst ON links(dst_url_id)`,
  `CREATE INDEX idx_links_dst_url_id_temp ON links(dst_url_id)`,
  `CREATE INDEX idx_links_src ON links(src_url_id)`,
  `CREATE INDEX idx_links_src_url_id_temp ON links(src_url_id)`,
  `CREATE INDEX idx_milestone_achievements_job_achieved ON milestone_achievements(job_id, achieved_at DESC)`,
  `CREATE INDEX idx_milestone_achievements_type ON milestone_achievements(milestone_type)`,
  `CREATE INDEX idx_news_websites_enabled ON news_websites(enabled)`,
  `CREATE INDEX idx_news_websites_parent ON news_websites(parent_domain)`,
  `CREATE INDEX idx_news_websites_stats_updated 
        ON news_websites_stats_cache(last_updated_at)`,
  `CREATE INDEX idx_news_websites_type ON news_websites(website_type)`,
  `CREATE INDEX idx_non_geo_topic_slug ON non_geo_topic_slugs(slug)`,
  `CREATE INDEX idx_page_category_map_category ON page_category_map(category_id)`,
  `CREATE INDEX idx_page_category_map_content ON page_category_map(content_id)`,
  `CREATE INDEX idx_place_attr_attr ON place_attribute_values(attr)`,
  `CREATE INDEX idx_place_attr_source ON place_attribute_values(source)`,
  `CREATE INDEX idx_place_attributes_kind ON place_attributes(attribute_kind)`,
  `CREATE INDEX idx_place_attributes_place ON place_attributes(place_id)`,
  `CREATE INDEX idx_place_attributes_place_attr ON place_attributes(place_id, attribute_kind)`,
  `CREATE INDEX idx_place_attributes_source ON place_attributes(source)`,
  `CREATE INDEX idx_place_external_place ON place_external_ids(place_id)`,
  `CREATE INDEX idx_place_hierarchy_child ON place_hierarchy(child_id)`,
  `CREATE INDEX idx_place_hierarchy_parent ON place_hierarchy(parent_id)`,
  `CREATE INDEX idx_place_hierarchy_relation ON place_hierarchy(relation)`,
  `CREATE INDEX idx_place_hub_audit_attempt ON place_hub_audit(attempt_id)`,
  `CREATE INDEX idx_place_hub_audit_decision ON place_hub_audit(decision)`,
  `CREATE INDEX idx_place_hub_audit_domain ON place_hub_audit(domain)`,
  `CREATE INDEX idx_place_hub_audit_run ON place_hub_audit(run_id)`,
  `CREATE INDEX idx_place_hub_candidates_attempt ON place_hub_candidates(attempt_id)`,
  `CREATE INDEX idx_place_hub_candidates_candidate_url ON place_hub_candidates(candidate_url_id)`,
  `CREATE INDEX idx_place_hub_candidates_domain ON place_hub_candidates(domain)`,
  `CREATE INDEX idx_place_hub_candidates_domain_status ON place_hub_candidates(domain, status)`,
  `CREATE INDEX idx_place_hub_candidates_normalized_url ON place_hub_candidates(normalized_url_id)`,
  `CREATE INDEX idx_place_hub_candidates_place_kind ON place_hub_candidates(place_kind)`,
  `CREATE INDEX idx_place_hub_determinations_domain
        ON place_hub_determinations(domain, created_at DESC)`,
  `CREATE INDEX idx_place_hub_guess_runs_background_task_id
    ON place_hub_guess_runs(background_task_id)`,
  `CREATE INDEX idx_place_hub_guess_runs_started_at
    ON place_hub_guess_runs(started_at)`,
  `CREATE INDEX idx_place_hub_guess_runs_status
    ON place_hub_guess_runs(status)`,
  `CREATE INDEX idx_place_hub_unknown_terms_canonical_url ON place_hub_unknown_terms(canonical_url_id)`,
  `CREATE INDEX idx_place_hub_unknown_terms_url ON place_hub_unknown_terms(url_id)`,
  `CREATE INDEX idx_place_hubs_host ON place_hubs(host)`,
  `CREATE INDEX idx_place_hubs_place ON place_hubs(place_slug)`,
  `CREATE INDEX idx_place_hubs_topic ON place_hubs(topic_slug)`,
  `CREATE INDEX idx_place_hubs_url ON place_hubs(url_id)`,
  `CREATE INDEX idx_place_names_lang ON place_names(lang)`,
  `CREATE INDEX idx_place_names_name ON place_names(name)`,
  `CREATE INDEX idx_place_names_norm ON place_names(normalized)`,
  `CREATE INDEX idx_place_names_place ON place_names(place_id)`,
  `CREATE INDEX idx_place_page_mappings_host_kind ON place_page_mappings(host, page_kind)`,
  `CREATE INDEX idx_place_page_mappings_host_status ON place_page_mappings(host, status, page_kind)`,
  `CREATE INDEX idx_place_page_mappings_place ON place_page_mappings(place_id)`,
  `CREATE INDEX idx_place_page_mappings_status ON place_page_mappings(status)`,
  `CREATE INDEX idx_place_provenance_external ON place_provenance(external_id)`,
  `CREATE INDEX idx_place_provenance_place ON place_provenance(place_id)`,
  `CREATE INDEX idx_place_provenance_place_source ON place_provenance(place_id, source)`,
  `CREATE INDEX idx_place_provenance_source ON place_provenance(source)`,
  `CREATE INDEX idx_places_adm1 ON places(adm1_code)`,
  `CREATE INDEX idx_places_adm2 ON places(adm2_code)`,
  `CREATE INDEX idx_places_canonical_name ON places(canonical_name_id)`,
  `CREATE INDEX idx_places_country ON places(country_code)`,
  `CREATE INDEX idx_places_crawl_depth ON places(crawl_depth)`,
  `CREATE INDEX idx_places_kind ON places(kind)`,
  `CREATE INDEX idx_places_kind_country ON places(kind, country_code)`,
  `CREATE INDEX idx_places_osm ON places(osm_type, osm_id)`,
  `CREATE INDEX idx_places_population ON places(population)`,
  `CREATE INDEX idx_places_priority_score ON places(priority_score)`,
  `CREATE INDEX idx_places_status ON places(status)`,
  `CREATE INDEX idx_places_wikidata_qid ON places(wikidata_qid)`,
  `CREATE INDEX idx_planner_patterns_confidence ON planner_patterns(confidence_score DESC)`,
  `CREATE INDEX idx_planner_patterns_domain_type ON planner_patterns(domain, pattern_type)`,
  `CREATE INDEX idx_planner_patterns_updated ON planner_patterns(updated_at DESC)`,
  `CREATE INDEX idx_planner_stage_events_job ON planner_stage_events(job_id)`,
  `CREATE INDEX idx_planner_stage_events_job_ts ON planner_stage_events(job_id, ts DESC)`,
  `CREATE INDEX idx_planner_stage_job_ts ON planner_stage_events(job_id, ts DESC)`,
  `CREATE INDEX idx_planner_stage_stage ON planner_stage_events(stage, status)`,
  `CREATE INDEX idx_priority_config_changes_ts ON priority_config_changes(changed_at DESC)`,
  `CREATE INDEX idx_problem_clusters_boost ON problem_clusters(priority_boost DESC)`,
  `CREATE INDEX idx_problem_clusters_job_kind ON problem_clusters(job_id, kind)`,
  `CREATE INDEX idx_problem_clusters_status ON problem_clusters(status)`,
  `CREATE INDEX idx_query_telemetry_complexity ON query_telemetry(query_complexity)`,
  `CREATE INDEX idx_query_telemetry_created ON query_telemetry(created_at)`,
  `CREATE INDEX idx_query_telemetry_duration ON query_telemetry(duration_ms)`,
  `CREATE INDEX idx_query_telemetry_host ON query_telemetry(host)`,
  `CREATE INDEX idx_query_telemetry_operation ON query_telemetry(operation)`,
  `CREATE INDEX idx_query_telemetry_timestamp ON query_telemetry(timestamp DESC)`,
  `CREATE INDEX idx_query_telemetry_type ON query_telemetry(query_type)`,
  `CREATE INDEX idx_queue_events_action ON queue_events(action)`,
  `CREATE INDEX idx_queue_events_enhanced_cluster ON queue_events_enhanced(cluster_id)`,
  `CREATE INDEX idx_queue_events_enhanced_host ON queue_events_enhanced(host)`,
  `CREATE INDEX idx_queue_events_enhanced_job_ts ON queue_events_enhanced(job_id, ts DESC)`,
  `CREATE INDEX idx_queue_events_enhanced_priority ON queue_events_enhanced(priority_score DESC)`,
  `CREATE INDEX idx_queue_events_host ON queue_events(host)`,
  `CREATE INDEX idx_queue_events_job ON queue_events(job_id)`,
  `CREATE INDEX idx_queue_events_job_action_id_desc ON queue_events(job_id, action, id DESC)`,
  `CREATE INDEX idx_queue_events_job_id_desc ON queue_events(job_id, id DESC)`,
  `CREATE INDEX idx_queue_events_job_ts ON queue_events(job_id, ts DESC)`,
  `CREATE INDEX idx_queue_events_url_id_temp ON queue_events(url_id)`,
  `CREATE INDEX idx_rate_limits_domain ON rate_limits(domain)`,
  `CREATE INDEX idx_robots_cache_crawl_delay 
    ON robots_cache(domain) WHERE crawl_delay_seconds IS NOT NULL
  `,
  `CREATE INDEX idx_robots_cache_expires 
    ON robots_cache(expires_at)
  `,
  `CREATE INDEX idx_task_events_category ON task_events(task_id, event_category)`,
  `CREATE INDEX idx_task_events_scope ON task_events(task_id, scope)`,
  `CREATE INDEX idx_task_events_severity ON task_events(task_id, severity)`,
  `CREATE INDEX idx_task_events_task_seq ON task_events(task_id, seq)`,
  `CREATE INDEX idx_task_events_task_type ON task_events(task_type, ts)`,
  `CREATE INDEX idx_task_events_ts ON task_events(ts)`,
  `CREATE INDEX idx_task_events_type ON task_events(task_id, event_type)`,
  `CREATE INDEX idx_topic_keywords_lang ON topic_keywords(lang)`,
  `CREATE INDEX idx_ui_cached_metrics_generated_at
  ON ui_cached_metrics (generated_at DESC)`,
  `CREATE INDEX idx_ui_themes_is_default ON ui_themes (is_default DESC)`,
  `CREATE INDEX idx_ui_themes_name ON ui_themes (name)`,
  `CREATE INDEX idx_unknown_terms_host ON place_hub_unknown_terms(host)`,
  `CREATE INDEX idx_unknown_terms_slug ON place_hub_unknown_terms(term_slug)`,
  `CREATE INDEX idx_url_classifications_confidence 
            ON url_classifications(confidence DESC)`,
  `CREATE INDEX idx_url_classifications_predicted 
            ON url_classifications(predicted_classification)`,
  `CREATE INDEX idx_url_classifications_source 
            ON url_classifications(prediction_source)`,
  `CREATE INDEX idx_url_classifications_url 
            ON url_classifications(url_id)`,
  `CREATE INDEX idx_url_classifications_verified 
            ON url_classifications(verified_at)`,
  `CREATE INDEX idx_url_patterns_accuracy 
            ON url_classification_patterns(accuracy DESC)`,
  `CREATE INDEX idx_url_patterns_classification
            ON url_classification_patterns(classification)`,
  `CREATE INDEX idx_url_patterns_domain 
            ON url_classification_patterns(domain)`,
  `CREATE INDEX idx_urls_canonical ON urls(canonical_url)`,
  `CREATE INDEX idx_urls_host ON urls(host)`,
  `CREATE INDEX idx_urls_url ON urls(url)`,
  `CREATE INDEX idx_webhook_deliveries_status ON webhook_deliveries(status)`,
  `CREATE INDEX idx_webhook_deliveries_webhook_id ON webhook_deliveries(webhook_id)`,
  `CREATE INDEX idx_webhooks_enabled ON webhooks(enabled)`,
  `CREATE INDEX idx_webhooks_user_id ON webhooks(user_id)`,
  `CREATE UNIQUE INDEX uniq_country_places ON places(country_code) WHERE kind='country' AND country_code IS NOT NULL`,
  `CREATE UNIQUE INDEX uniq_crawl_skip_terms ON crawl_skip_terms(lang, normalized)`,
  `CREATE UNIQUE INDEX uniq_place_names ON place_names(place_id, normalized, lang, name_kind)`,
  `CREATE UNIQUE INDEX uniq_place_sources ON place_sources(name, version, url, license)`,
  `CREATE UNIQUE INDEX uniq_region_places ON places(country_code, adm1_code) WHERE kind='region' AND country_code IS NOT NULL AND adm1_code IS NOT NULL`,
  `CREATE UNIQUE INDEX uniq_topic_keywords ON topic_keywords(topic, lang, normalized)`,
  `CREATE UNIQUE INDEX uniq_url_alias ON url_aliases(url_id, alias_url_id)`,
  `CREATE UNIQUE INDEX uniq_url_classification_source
            ON url_classifications(url_id, prediction_source)`,
  `CREATE UNIQUE INDEX uniq_url_pattern
            ON url_classification_patterns(domain, pattern_regex)`,
];

const TRIGGER_STATEMENTS = [
];

const VIEW_STATEMENTS = [
  // fetched_urls
  `CREATE VIEW fetched_urls AS
WITH normalized_fetches AS (
  SELECT
    hr.url_id,
    COALESCE(hr.fetched_at, hr.request_started_at) AS ts,
    hr.http_status,
    ca.classification,
    ca.word_count
  FROM http_responses hr
  LEFT JOIN content_storage cs ON cs.http_response_id = hr.id
  LEFT JOIN content_analysis ca ON ca.content_id = cs.id
  WHERE hr.url_id IS NOT NULL
),
aggregated AS (
  SELECT
    url_id,
    MIN(ts) AS first_fetched_at,
    MAX(ts) AS last_fetched_at,
    COUNT(*) AS fetch_count
  FROM normalized_fetches
  GROUP BY url_id
),
latest AS (
  SELECT nf.url_id, nf.http_status, nf.classification, nf.word_count
  FROM normalized_fetches nf
  JOIN (
    SELECT url_id, MAX(ts) AS last_ts
    FROM normalized_fetches
    GROUP BY url_id
  ) newest
    ON newest.url_id = nf.url_id AND newest.last_ts = nf.ts
  GROUP BY nf.url_id
)
SELECT
  u.id AS url_id,
  u.url,
  u.host,
  u.canonical_url AS canonical_url,
  u.created_at AS url_created_at,
  u.last_seen_at AS url_last_seen_at,
  agg.first_fetched_at,
  agg.last_fetched_at,
  agg.fetch_count,
  latest.http_status AS last_http_status,
  latest.classification AS last_classification,
  latest.word_count AS last_word_count
FROM urls u
JOIN aggregated agg ON agg.url_id = u.id
LEFT JOIN latest ON latest.url_id = u.id`,

  // place_hubs_with_urls
  `CREATE VIEW place_hubs_with_urls AS
SELECT
  ph.id,
  ph.host,
  ph.place_slug,
  ph.title,
  ph.first_seen_at,
  ph.last_seen_at,
  ph.nav_links_count,
  ph.article_links_count,
  ph.evidence,
  ph.place_kind,
  ph.topic_slug,
  ph.topic_label,
  ph.topic_kind,
  ph.url_id,
  u.url
FROM place_hubs ph
LEFT JOIN urls u ON u.id = ph.url_id`,

];


function getAllStatements() {
    return [
        ...TABLE_STATEMENTS,
        ...INDEX_STATEMENTS,
        ...TRIGGER_STATEMENTS,
        ...VIEW_STATEMENTS
    ];
}

module.exports = {
    TABLE_STATEMENTS,
    INDEX_STATEMENTS,
    TRIGGER_STATEMENTS,
    VIEW_STATEMENTS,
    getAllStatements
};
