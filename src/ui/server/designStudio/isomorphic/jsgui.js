"use strict";

/**
 * Design Studio reuses the shared isomorphic resolver so that
 * server and client builds stay consistent with Docs Viewer/Data Explorer.
 */

module.exports = require("../../shared/isomorphic/jsgui");
