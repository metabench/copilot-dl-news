"use strict";

/**
 * Art Playground reuses the shared isomorphic resolver so that
 * server and client builds stay consistent with Design Studio/Docs Viewer.
 */

module.exports = require("../../shared/isomorphic/jsgui");
