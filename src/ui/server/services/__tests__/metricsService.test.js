"use strict";

// NOTE: This legacy test used the sqlite driver directly under src/ui/, which is
// forbidden by the SQL boundary guard. The real tests live under /tests now.

describe.skip("metricsService (migrated)", () => {
  test("migrated to /tests", () => {
    expect(true).toBe(true);
  });
});

